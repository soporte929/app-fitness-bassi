---
phase: 14-trainer-completar
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(trainer)/clients/[id]/history/page.tsx
  - app/(trainer)/clients/[id]/page.tsx
autonomous: true
requirements:
  - TRN-02

must_haves:
  truths:
    - "El trainer puede hacer clic en 'Ver historial' en el detalle de un cliente y llegar a una página con las sesiones completadas de ese cliente"
    - "La página del historial del cliente muestra las sesiones en orden cronológico inverso con fecha, nombre del día, volumen y series"
    - "Solo el trainer propietario puede acceder al historial de un cliente (verificación trainer_id)"
    - "Si el cliente no pertenece al trainer, la ruta devuelve 404 (no redirect a login)"
  artifacts:
    - path: "app/(trainer)/clients/[id]/history/page.tsx"
      provides: "Server Component que lista sesiones completadas de un cliente específico"
      exports: ["default async function TrainerClientHistoryPage"]
    - path: "app/(trainer)/clients/[id]/page.tsx"
      provides: "Botón 'Ver historial' convertido en Link con href correcto"
      contains: "Link href.*history"
  key_links:
    - from: "app/(trainer)/clients/[id]/page.tsx"
      to: "app/(trainer)/clients/[id]/history/page.tsx"
      via: "Link href={`/clients/${id}/history`}"
      pattern: "clients.*history"
    - from: "app/(trainer)/clients/[id]/history/page.tsx"
      to: "components/client/history-filters.tsx"
      via: "props: sessions={sessions}"
      pattern: "<HistoryFilters"
---

<objective>
Crear la página de historial de entrenos de un cliente vista desde el trainer, y conectar el botón "Ver historial" del detalle del cliente.

Purpose: TRN-02 permite al trainer inspeccionar el historial de entrenamientos de cualquiera de sus clientes directamente desde la ficha del cliente, cerrando una funcionalidad de supervisión esencial.
Output: app/(trainer)/clients/[id]/history/page.tsx (nueva, Server Component), modificación en app/(trainer)/clients/[id]/page.tsx para convertir el Button en Link.
</objective>

<execution_context>
@/Users/jofreatanet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jofreatanet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# Patrones establecidos de este proyecto
# - params es Promise en Next.js 16: params: Promise<{ id: string }> con await params
# - FK hint obligatorio en workout_sessions → workout_days: !workout_sessions_day_id_fkey
# - Seguridad: .eq('trainer_id', user.id) en la query de clients para verificar propiedad
# - notFound() cuando el recurso no pertenece al usuario (no redirect)
# - Server Components por defecto; 'use client' solo para estado interactivo
# - Envolver siempre en <PageTransition>
</context>

<interfaces>
<!-- Interfaces clave extraídas del codebase -->

De components/client/session-history-card.tsx:
```typescript
export type SessionData = {
  id: string
  started_at: string
  finished_at: string | null
  workout_day: { name: string } | null
  durationLabel: string
  totalVolume: number
  completedSets: number
  trainedMuscles: string[]
  hasPR?: boolean
}

export function SessionHistoryCard({ session, index }: { session: SessionData; index: number }): JSX.Element
```

De components/client/history-filters.tsx:
```typescript
// 'use client' — acepta sessions como prop (data ya cargada en Server Component)
export function HistoryFilters({ sessions }: { sessions: SessionData[] }): JSX.Element
// Filtra por: 'all' | 'week' | 'month' con botones chip
```

De app/(client)/history/page.tsx — helper de duración (copiar directamente):
```typescript
function formatDuration(startedAt: string, finishedAt: string | null): string {
  if (!finishedAt) return '—'
  const diffMs = new Date(finishedAt).getTime() - new Date(startedAt).getTime()
  const minutes = Math.max(0, Math.round(diffMs / 60000))
  if (minutes < 60) return `${minutes}min`
  const hours = Math.floor(minutes / 60)
  const remainingMinutes = minutes % 60
  return remainingMinutes > 0 ? `${hours}h ${remainingMinutes}min` : `${hours}h`
}
```

De app/(trainer)/clients/[id]/page.tsx — líneas 373-380 (botón a convertir):
```tsx
<Card>
  <CardHeader>
    <div className="flex items-center justify-between">
      <p className="text-sm font-semibold text-[var(--text-primary)]">Últimos entrenamientos</p>
      <Button variant="ghost" size="sm">
        Ver historial <ChevronRight className="w-3.5 h-3.5" />
      </Button>   {/* <-- convertir esto en un Link */}
    </div>
  </CardHeader>
```

Patrón de seguridad para clients (de MEMORY.md y clients/[id]/page.tsx):
```typescript
// FK hint para join ambiguo clients → profiles
.select('id, profile:profiles!clients_profile_id_fkey (full_name)')
.eq('id', clientId)
.eq('trainer_id', user.id)  // seguridad: solo clientes propios
.single()
// Si client === null → notFound()
```

Query sessions del cliente (patrón de app/(client)/history/page.tsx adaptado):
```typescript
const { data: rawSessions } = await supabase
  .from('workout_sessions')
  .select(`
    id, started_at, finished_at,
    workout_day:workout_days!workout_sessions_day_id_fkey (name),
    set_logs (
      weight_kg, reps, completed,
      exercises (id, muscle_group)
    )
  `)
  .eq('client_id', client.id)   // usar client.id del row verificado
  .eq('completed', true)
  .order('finished_at', { ascending: false })
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Crear app/(trainer)/clients/[id]/history/page.tsx</name>
  <files>app/(trainer)/clients/[id]/history/page.tsx</files>
  <action>
Crear `app/(trainer)/clients/[id]/history/page.tsx` como Server Component. Es una réplica adaptada de `app/(client)/history/page.tsx` con estas diferencias:
- Obtiene el `clientId` del param de ruta (no del user.id)
- Verifica propiedad con `.eq('trainer_id', user.id)`
- Usa `notFound()` si el cliente no existe o no pertenece al trainer
- Tiene botón de volver atrás con `ArrowLeft` hacia `/clients/${id}`
- NO incluye PR detection (no requerido por criterio de éxito — simplifica implementación)

```tsx
import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'
import { notFound } from 'next/navigation'
import Link from 'next/link'
import { PageTransition } from '@/components/ui/page-transition'
import { HistoryFilters } from '@/components/client/history-filters'
import type { SessionData } from '@/components/client/session-history-card'
import { ArrowLeft, History } from 'lucide-react'

type SessionSetLogRow = {
  weight_kg: number
  reps: number
  completed: boolean
  exercises: {
    id: string
    muscle_group: string | null
  } | null
}

type SessionRow = {
  id: string
  started_at: string
  finished_at: string | null
  workout_day: {
    name: string
  } | null
  set_logs: SessionSetLogRow[] | null
}

function formatDuration(startedAt: string, finishedAt: string | null): string {
  if (!finishedAt) return '—'
  const diffMs = new Date(finishedAt).getTime() - new Date(startedAt).getTime()
  const minutes = Math.max(0, Math.round(diffMs / 60000))
  if (minutes < 60) return `${minutes}min`
  const hours = Math.floor(minutes / 60)
  const remainingMinutes = minutes % 60
  return remainingMinutes > 0 ? `${hours}h ${remainingMinutes}min` : `${hours}h`
}

export default async function TrainerClientHistoryPage({
  params,
}: {
  params: Promise<{ id: string }>
}) {
  const { id } = await params

  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  // Verificar que el cliente pertenece a este trainer
  const { data: client } = await supabase
    .from('clients')
    .select('id, profile:profiles!clients_profile_id_fkey (full_name)')
    .eq('id', id)
    .eq('trainer_id', user.id)
    .single()

  if (!client) notFound()

  const profile = client.profile as { full_name: string } | null
  const clientName = profile?.full_name ?? 'Cliente'

  // Fetch sesiones completadas del cliente
  const { data: rawSessions } = await supabase
    .from('workout_sessions')
    .select(
      `id, started_at, finished_at,
      workout_day:workout_days!workout_sessions_day_id_fkey (name),
      set_logs (
        weight_kg, reps, completed,
        exercises (
          id,
          muscle_group
        )
      )`
    )
    .eq('client_id', client.id)
    .eq('completed', true)
    .order('finished_at', { ascending: false })

  const sessionsFromDb = (rawSessions ?? []) as SessionRow[]

  const sessions: SessionData[] = sessionsFromDb.map((session) => {
    const completedLogs = (session.set_logs ?? []).filter((log) => log.completed)
    const totalVolume = completedLogs.reduce((sum, log) => sum + log.weight_kg * log.reps, 0)
    const trainedMuscles = Array.from(
      new Set(
        completedLogs
          .map((log) => log.exercises?.muscle_group?.trim())
          .filter((muscle): muscle is string => Boolean(muscle))
      )
    )

    return {
      id: session.id,
      started_at: session.started_at,
      finished_at: session.finished_at,
      workout_day: session.workout_day,
      durationLabel: formatDuration(session.started_at, session.finished_at),
      totalVolume,
      completedSets: completedLogs.length,
      trainedMuscles,
    }
  })

  return (
    <PageTransition>
      <div className="px-4 pt-6 pb-8">
        {/* Back link */}
        <Link
          href={`/clients/${id}`}
          className="inline-flex items-center gap-1.5 text-sm text-[var(--text-secondary)] hover:text-[var(--text-primary)] transition-colors mb-5"
        >
          <ArrowLeft className="w-4 h-4" />
          Volver a {clientName}
        </Link>

        <div className="mb-6">
          <h1 className="text-2xl font-bold text-[var(--text-primary)] tracking-tight">
            Historial de {clientName}
          </h1>
          <p className="text-sm text-[var(--text-secondary)] mt-1">
            {sessions.length} {sessions.length === 1 ? 'entrenamiento completado' : 'entrenamientos completados'}
          </p>
        </div>

        {sessions.length === 0 ? (
          <div className="flex flex-col items-center justify-center py-16 text-center">
            <History className="w-12 h-12 text-[var(--text-muted)] mb-3" />
            <p className="text-sm font-medium text-[var(--text-primary)]">Sin entrenamientos</p>
            <p className="text-xs text-[var(--text-muted)] mt-1">
              {clientName} aún no ha completado ningún entrenamiento
            </p>
          </div>
        ) : (
          <HistoryFilters sessions={sessions} />
        )}
      </div>
    </PageTransition>
  )
}
```

**Puntos críticos:**
- `params: Promise<{ id: string }>` con `await params` (Next.js 16)
- FK hint `!workout_sessions_day_id_fkey` obligatorio
- FK hint `!clients_profile_id_fkey` obligatorio
- `.eq('trainer_id', user.id)` para seguridad
- `notFound()` (no redirect) cuando el cliente no pertenece al trainer
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | grep -E "clients.*history|history.*page" | head -20</automated>
  </verify>
  <done>
    - El archivo existe en app/(trainer)/clients/[id]/history/page.tsx
    - Es Server Component (sin 'use client')
    - Usa await params (no params.id directamente)
    - Usa FK hints en ambos joins
    - Tiene verificación trainer_id + notFound()
    - Compila sin errores de TypeScript
  </done>
</task>

<task type="auto">
  <name>Task 2: Convertir botón "Ver historial" en Link en clients/[id]/page.tsx</name>
  <files>app/(trainer)/clients/[id]/page.tsx</files>
  <action>
En `app/(trainer)/clients/[id]/page.tsx`, localizar el botón "Ver historial" (alrededor de la línea 377):

```tsx
// ANTES:
<Button variant="ghost" size="sm">
  Ver historial <ChevronRight className="w-3.5 h-3.5" />
</Button>
```

Reemplazarlo por un `<Link>` con las clases visuales del variant ghost/sm del Button del design system. Necesitamos importar `Link` de `next/link` si no está ya importado.

```tsx
// DESPUÉS:
<Link
  href={`/clients/${id}/history`}
  className="inline-flex items-center gap-1.5 rounded-lg px-3 py-1.5 text-sm text-[var(--text-secondary)] hover:bg-[var(--bg-elevated)] hover:text-[var(--text-primary)] transition-colors font-medium"
>
  Ver historial <ChevronRight className="w-3.5 h-3.5" />
</Link>
```

**Pasos:**
1. Verificar si `Link` de `next/link` ya está importado en el archivo. Si no, añadir el import.
2. Sustituir el `<Button variant="ghost" size="sm">Ver historial...</Button>` por el `<Link>` anterior.
3. Verificar que `id` está disponible en el scope (viene de `await params` al inicio de la función — confirmar mirando cómo se declara la página).
4. No modificar ninguna otra parte del archivo.

**Nota:** Si el componente `Button` sigue siendo usado en otras partes del archivo, NO eliminar su import. Solo quitar este uso específico.
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && grep -n "Ver historial" "app/(trainer)/clients/[id]/page.tsx" | head -5</automated>
  </verify>
  <done>
    - "Ver historial" aparece dentro de un elemento Link (no Button) en el archivo
    - El Link tiene href apuntando a /clients/${id}/history
    - El archivo compila sin errores de TypeScript
    - `npx tsc --noEmit` pasa sin errores nuevos en este archivo
  </done>
</task>

</tasks>

<verification>
```bash
cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi"
npx tsc --noEmit 2>&1 | tail -5
ls "app/(trainer)/clients/[id]/history/page.tsx" && echo "history page exists"
grep -n "Ver historial" "app/(trainer)/clients/[id]/page.tsx"
grep -n "href.*history" "app/(trainer)/clients/[id]/page.tsx"
```
</verification>

<success_criteria>
- app/(trainer)/clients/[id]/history/page.tsx existe y compila sin errores
- En app/(trainer)/clients/[id]/page.tsx, "Ver historial" está dentro de un Link con href a /clients/[id]/history
- `npx tsc --noEmit` pasa sin errores nuevos introducidos por estos cambios
</success_criteria>

<output>
Después de completar este plan, crear `.planning/phases/14-trainer-completar/14-02-SUMMARY.md` con:
- Archivos creados/modificados
- Decisiones tomadas durante implementación
- Cualquier desviación del plan
</output>
