---
phase: 14-trainer-completar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(trainer)/exercises/page.tsx
  - components/trainer/exercises-filter.tsx
  - components/trainer/sidebar.tsx
autonomous: true
requirements:
  - TRN-01
  - TRN-03

must_haves:
  truths:
    - "El trainer puede navegar a /exercises y ver todos los ejercicios de sus planes agrupados por muscle_group"
    - "El trainer puede filtrar los ejercicios por grupo muscular con un selector"
    - "Los links /reports y /settings han desaparecido del sidebar del trainer (sin 404s)"
  artifacts:
    - path: "app/(trainer)/exercises/page.tsx"
      provides: "Server Component que consulta exercises via RLS y renderiza ExercisesFilter"
      exports: ["default async function ExercisesPage"]
    - path: "components/trainer/exercises-filter.tsx"
      provides: "Client Component con useState para filtrado local por muscle_group"
      exports: ["ExercisesFilter"]
    - path: "components/trainer/sidebar.tsx"
      provides: "Sidebar sin entradas /reports ni /settings"
      contains: "navigation sin FileText ni Settings"
  key_links:
    - from: "app/(trainer)/exercises/page.tsx"
      to: "components/trainer/exercises-filter.tsx"
      via: "props: exercises={exercises} muscleGroups={muscleGroups}"
      pattern: "<ExercisesFilter"
    - from: "components/trainer/sidebar.tsx"
      to: "/exercises"
      via: "Link href /exercises permanece; /reports y /settings eliminados"
      pattern: "href.*exercises"
---

<objective>
Construir la página de librería de ejercicios del trainer (/exercises) y limpiar los links rotos del sidebar.

Purpose: TRN-01 y TRN-03 son completados del lado trainer que desbloquean navegación funcional sin 404s y dan acceso a todos los ejercicios catalogados por músculo.
Output: exercises/page.tsx (Server Component), exercises-filter.tsx (Client Component con filtro), sidebar.tsx actualizado sin /reports ni /settings.
</objective>

<execution_context>
@/Users/jofreatanet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jofreatanet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# Patrones establecidos de este proyecto
# - Server Components por defecto; 'use client' solo para estado interactivo
# - Tipos de lib/supabase/types.ts — nunca redeclarar
# - FK hints obligatorios en todos los joins embebidos
# - Envolver siempre en <PageTransition>
# - Iconos exclusivamente de lucide-react
# - SELECT * prohibido — especificar siempre columnas exactas
# - Colores CSS vars: var(--text-primary), var(--text-secondary), var(--bg-surface), etc.
</context>

<interfaces>
<!-- Interfaces clave extraídas del codebase -->

De lib/supabase/types.ts — tipo Exercise:
```typescript
type Exercise = Database['public']['Tables']['exercises']['Row']
// Row: { id: string; day_id: string; name: string; muscle_group: string | null;
//         target_sets: number | null; target_reps: string | null; target_rir: number | null;
//         notes: string | null; order_index: number }
```

De components/trainer/sidebar.tsx — array navigation actual (con bugs):
```typescript
const navigation = [
  {
    section: "Principal",
    items: [
      { label: "Dashboard", href: "/dashboard", icon: LayoutDashboard },
      { label: "Clientes", href: "/clients", icon: Users },
    ],
  },
  {
    section: "Herramientas",
    items: [
      { label: "Rutinas", href: "/routines-templates", icon: ClipboardList },
      { label: "Planes", href: "/plans", icon: Layers },
      { label: "Nutrición", href: "/nutrition-plans", icon: UtensilsCrossed },
      { label: "Ejercicios", href: "/exercises", icon: Dumbbell },
      { label: "Informes", href: "/reports", icon: FileText },   // <-- BORRAR
    ],
  },
  {
    section: "Sistema",
    items: [{ label: "Ajustes", href: "/settings", icon: Settings }],  // <-- BORRAR SECCIÓN ENTERA
  },
]
```

De lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient<Database>>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Crear exercises/page.tsx + exercises-filter.tsx</name>
  <files>app/(trainer)/exercises/page.tsx, components/trainer/exercises-filter.tsx</files>
  <action>
**1. Crear `app/(trainer)/exercises/page.tsx` (Server Component):**

```tsx
import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'
import { PageTransition } from '@/components/ui/page-transition'
import { ExercisesFilter } from '@/components/trainer/exercises-filter'
import type { Database } from '@/lib/supabase/types'

type Exercise = Database['public']['Tables']['exercises']['Row']

export default async function ExercisesPage() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const { data: exercises } = await supabase
    .from('exercises')
    .select('id, name, muscle_group, target_sets, target_reps, target_rir, notes')
    .order('muscle_group', { ascending: true })
    .order('name', { ascending: true })

  const allExercises = (exercises ?? []) as Pick<Exercise, 'id' | 'name' | 'muscle_group' | 'target_sets' | 'target_reps' | 'target_rir' | 'notes'>[]

  // Derivar grupos únicos de la data retornada (no hardcodear)
  const muscleGroups = Array.from(
    new Set(allExercises.map(e => e.muscle_group).filter((g): g is string => Boolean(g)))
  ).sort()

  return (
    <PageTransition>
      <div className="px-4 pt-6 pb-8">
        <div className="mb-6">
          <h1 className="text-2xl font-bold text-[var(--text-primary)] tracking-tight">
            Ejercicios
          </h1>
          <p className="text-sm text-[var(--text-secondary)] mt-1">
            {allExercises.length} {allExercises.length === 1 ? 'ejercicio' : 'ejercicios'}
          </p>
        </div>
        <ExercisesFilter exercises={allExercises} muscleGroups={muscleGroups} />
      </div>
    </PageTransition>
  )
}
```

**2. Crear `components/trainer/exercises-filter.tsx` (Client Component):**

El componente recibe todos los ejercicios del Server Component como props y filtra localmente con useState — NO hace ninguna query Supabase en cliente.

```tsx
'use client'

import { useState } from 'react'
import { cn } from '@/lib/utils'
import { Dumbbell } from 'lucide-react'
import { Card, CardContent } from '@/components/ui/card'

type ExerciseItem = {
  id: string
  name: string
  muscle_group: string | null
  target_sets: number | null
  target_reps: string | null
  target_rir: number | null
  notes: string | null
}

interface Props {
  exercises: ExerciseItem[]
  muscleGroups: string[]
}

export function ExercisesFilter({ exercises, muscleGroups }: Props) {
  const [selected, setSelected] = useState<string | null>(null)

  const filtered = selected
    ? exercises.filter(e => e.muscle_group === selected)
    : exercises

  if (exercises.length === 0) {
    return (
      <div className="flex flex-col items-center justify-center py-16 text-center">
        <Dumbbell className="w-12 h-12 text-[var(--text-muted)] mb-3" />
        <p className="text-sm font-medium text-[var(--text-primary)]">Sin ejercicios</p>
        <p className="text-xs text-[var(--text-muted)] mt-1">
          Crea un plan con ejercicios para verlos aquí
        </p>
      </div>
    )
  }

  return (
    <>
      {/* Chips de filtro por muscle_group */}
      <div className="flex gap-2 mb-5 overflow-x-auto pb-1 flex-wrap">
        <button
          onClick={() => setSelected(null)}
          className={cn(
            'flex-shrink-0 px-4 py-1.5 rounded-full text-sm font-medium transition-all',
            selected === null
              ? 'bg-[var(--text-primary)] text-[var(--bg-base)]'
              : 'bg-[var(--bg-elevated)] text-[var(--text-secondary)] border border-[var(--border)]'
          )}
        >
          Todos
        </button>
        {muscleGroups.map(group => (
          <button
            key={group}
            onClick={() => setSelected(group)}
            className={cn(
              'flex-shrink-0 px-4 py-1.5 rounded-full text-sm font-medium transition-all',
              selected === group
                ? 'bg-[var(--text-primary)] text-[var(--bg-base)]'
                : 'bg-[var(--bg-elevated)] text-[var(--text-secondary)] border border-[var(--border)]'
            )}
          >
            {group}
          </button>
        ))}
      </div>

      {/* Lista de ejercicios */}
      {filtered.length === 0 ? (
        <div className="flex flex-col items-center justify-center py-10 text-center">
          <p className="text-sm text-[var(--text-muted)]">Sin ejercicios en este grupo</p>
        </div>
      ) : (
        <div className="space-y-2 stagger">
          {filtered.map((ex, i) => (
            <div
              key={ex.id}
              className="animate-fade-in"
              style={{ animationDelay: `${i * 40}ms` }}
            >
              <Card>
                <CardContent className="px-4 py-3">
                  <div className="flex items-start justify-between gap-3">
                    <div className="flex-1 min-w-0">
                      <p className="text-sm font-semibold text-[var(--text-primary)] truncate">
                        {ex.name}
                      </p>
                      {ex.muscle_group && (
                        <p className="text-xs text-[var(--text-secondary)] mt-0.5">
                          {ex.muscle_group}
                        </p>
                      )}
                      {ex.notes && (
                        <p className="text-xs text-[var(--text-muted)] mt-1 line-clamp-2">
                          {ex.notes}
                        </p>
                      )}
                    </div>
                    <div className="flex-shrink-0 text-right">
                      {(ex.target_sets || ex.target_reps) && (
                        <p className="text-xs font-medium text-[var(--text-secondary)]">
                          {ex.target_sets ? `${ex.target_sets} series` : ''}
                          {ex.target_sets && ex.target_reps ? ' × ' : ''}
                          {ex.target_reps ? `${ex.target_reps} reps` : ''}
                        </p>
                      )}
                      {ex.target_rir !== null && (
                        <p className="text-xs text-[var(--text-muted)]">RIR {ex.target_rir}</p>
                      )}
                    </div>
                  </div>
                </CardContent>
              </Card>
            </div>
          ))}
        </div>
      )}
    </>
  )
}
```

**Nota:** El RLS en la tabla `exercises` filtra automáticamente por `trainer_id` via `day_id → workout_days → workout_plans → trainer_id = auth.uid()`. No hace falta filtrar manualmente en el Server Component.
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | grep -E "exercises" | head -20</automated>
  </verify>
  <done>
    - app/(trainer)/exercises/page.tsx existe como Server Component con query Supabase sin SELECT *
    - components/trainer/exercises-filter.tsx existe como 'use client' con filtro local
    - Sin errores de TypeScript en ambos archivos
    - No hay ningún fetch de datos en el Client Component
  </done>
</task>

<task type="auto">
  <name>Task 2: Limpiar sidebar — eliminar /reports y /settings</name>
  <files>components/trainer/sidebar.tsx</files>
  <action>
En `components/trainer/sidebar.tsx`:

1. **Eliminar del array `navigation`** la entrada `{ label: "Informes", href: "/reports", icon: FileText }` dentro de la sección "Herramientas".

2. **Eliminar la sección entera "Sistema"** (que solo contiene `{ label: "Ajustes", href: "/settings", icon: Settings }`).

3. **Eliminar los imports** de `FileText` y `Settings` de `lucide-react` — ya no se usan.

El array `navigation` resultante debe quedar:
```typescript
const navigation = [
  {
    section: "Principal",
    items: [
      { label: "Dashboard", href: "/dashboard", icon: LayoutDashboard },
      { label: "Clientes", href: "/clients", icon: Users },
    ],
  },
  {
    section: "Herramientas",
    items: [
      { label: "Rutinas", href: "/routines-templates", icon: ClipboardList },
      { label: "Planes", href: "/plans", icon: Layers },
      { label: "Nutrición", href: "/nutrition-plans", icon: UtensilsCrossed },
      { label: "Ejercicios", href: "/exercises", icon: Dumbbell },
    ],
  },
]
```

El import de lucide-react debe quedar sin `FileText` ni `Settings`:
```typescript
import {
  LayoutDashboard,
  Users,
  Dumbbell,
  ClipboardList,
  Layers,
  UtensilsCrossed,
  LogOut,
  PanelLeftClose,
  PanelLeftOpen,
} from "lucide-react";
```

**No tocar** ninguna otra parte del componente (JSX, funciones, props).
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | grep -E "sidebar" | head -10</automated>
  </verify>
  <done>
    - El array navigation no contiene ninguna entrada con href="/reports" ni href="/settings"
    - Los imports FileText y Settings ya no aparecen en el archivo
    - El archivo compila sin errores de TypeScript
    - El link /exercises permanece intacto en la sección "Herramientas"
  </done>
</task>

</tasks>

<verification>
```bash
cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi"
npx tsc --noEmit 2>&1 | tail -5
grep -n "reports\|settings\|FileText\|Settings" components/trainer/sidebar.tsx | grep -v "//\|ThemeToggle\|Settings.*import" && echo "ERROR: found dead links" || echo "OK: dead links removed"
ls app/\(trainer\)/exercises/page.tsx && echo "exercises page exists"
ls components/trainer/exercises-filter.tsx && echo "exercises-filter exists"
```
</verification>

<success_criteria>
- app/(trainer)/exercises/page.tsx existe y compila sin errores
- components/trainer/exercises-filter.tsx existe y compila sin errores
- components/trainer/sidebar.tsx no contiene /reports ni /settings ni los imports FileText/Settings
- `npx tsc --noEmit` pasa sin errores nuevos introducidos por estos cambios
</success_criteria>

<output>
Después de completar este plan, crear `.planning/phases/14-trainer-completar/14-01-SUMMARY.md` con:
- Archivos creados/modificados
- Decisiones tomadas durante implementación
- Cualquier desviación del plan
</output>
