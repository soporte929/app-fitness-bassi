---
phase: 045-ocultar-nutricion-en-trainer-panel-vista-cliente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(trainer)/clients/[id]/page.tsx
autonomous: false
requirements:
  - P45-SC1
  - P45-SC2

must_haves:
  truths:
    - "En /clients/[id] no aparece el botón 'Asignar plan nutricional' en el header"
    - "En /clients/[id] no aparece el card 'Plan nutricional' en la columna lateral"
    - "El card 'Plan activo' (entrenamiento) sigue visible en la columna lateral"
    - "La página compila sin errores TypeScript"
  artifacts:
    - path: "app/(trainer)/clients/[id]/page.tsx"
      provides: "Página de detalle de cliente sin UI de nutrición"
      contains: "AssignPlanButton"
  key_links:
    - from: "app/(trainer)/clients/[id]/page.tsx"
      to: "Promise.all (5 elementos)"
      via: "Destructuring posicional — debe coincidir exactamente"
      pattern: "\\[weightLogsRes, measurementsRes, sessionsRes, activePlanRes, plansRes\\]"
---

<objective>
Eliminar toda la UI y lógica de nutrición de la página de detalle de cliente del panel del entrenador (`/clients/[id]`).

Purpose: Completar el ocultamiento de la feature de nutrición iniciado en Quick Tasks 1 y 3. El trainer no debe ver ni acceder a funcionalidad de planes nutricionales desde la vista de cliente.
Output: `page.tsx` editado sin imports, queries, variables ni JSX relacionados con nutrición. Los tres ficheros auxiliares (`assign-nutrition-plan-modal.tsx`, `edit-nutrition-plan-modal.tsx`, `nutrition-actions.ts`) se conservan intactos (patrón establecido: ocultar, no borrar).
</objective>

<execution_context>
@/Users/jofreatanet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jofreatanet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/045-ocultar-nutricion-en-trainer-panel-vista-cliente/045-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminar imports, queries y variables de nutrición de page.tsx</name>
  <files>app/(trainer)/clients/[id]/page.tsx</files>
  <action>
Editar `app/(trainer)/clients/[id]/page.tsx` eliminando exactamente los siguientes elementos:

**1. Imports a eliminar (líneas 14-18 aprox.):**
- Línea 14: `import { EditNutritionPlanModal } from './edit-nutrition-plan-modal'`
- Línea 16: `import { AssignNutritionPlanModal } from './assign-nutrition-plan-modal'`
- Línea 17: `import type { NutritionTemplate } from './nutrition-actions'`
- Línea 18: en `import { ArrowLeft, Flame, ChevronRight, ClipboardList } from 'lucide-react'` → eliminar `Flame,` para dejar `import { ArrowLeft, ChevronRight, ClipboardList } from 'lucide-react'`

**2. Reducir el Promise.all de 7 a 5 elementos (línea 46 aprox.):**

Antes:
```typescript
const [weightLogsRes, measurementsRes, sessionsRes, activePlanRes, plansRes, activeNutritionPlanRaw, nutritionTemplatesRes] = await Promise.all([
  // ...5 queries existentes...
  supabase
    .from('nutrition_plans' as any)
    .select('*, meals:nutrition_plan_meals(*)')
    .eq('client_id', id)
    .eq('active', true)
    .maybeSingle(),
  supabase
    .from('nutrition_plans' as any)
    .select('id, name, kcal_target, protein_target_g, carbs_target_g, fat_target_g, meals_count, diet_type')
    .eq('trainer_id', user.id)
    .eq('is_template', true)
    .order('created_at', { ascending: false }),
])
```

Después (eliminar los dos últimos elementos del array Y los dos últimos nombres del destructuring):
```typescript
const [weightLogsRes, measurementsRes, sessionsRes, activePlanRes, plansRes] = await Promise.all([
  // ...5 queries sin cambios...
])
```

CRÍTICO: el destructuring es posicional — eliminar exactamente las dos últimas promesas y los dos últimos nombres.

**3. Eliminar variables de nutrición (líneas 110-116 aprox.):**
- Línea 110: `const nutritionTemplates = (nutritionTemplatesRes.data ?? []) as unknown as NutritionTemplate[]`
- Líneas 112-116: bloque completo:
```typescript
const activeNutritionPlanData = activeNutritionPlanRaw.data
const activeNutritionPlan = activeNutritionPlanData ? {
  ...(activeNutritionPlanData as any),
  meals: ((activeNutritionPlanData as any).meals || []).sort((a: any, b: any) => a.order_index - b.order_index)
} : null
```

**CONSERVAR sin cambios:** `calculateNutrition` y su uso (líneas 169-175). Esa función calcula `nutrition.ffm` para el StatCard "Masa libre grasa" — no es UI de planes nutricionales.

Tras la edición, verificar que ninguna de estas cadenas aparece en el fichero:
`AssignNutritionPlanModal`, `EditNutritionPlanModal`, `NutritionTemplate`, `nutrition-actions`, `activeNutritionPlan`, `nutritionTemplates`, `activeNutritionPlanRaw`, `nutritionTemplatesRes`, `nutrition_plans`, `nutrition_plan_meals`, `Flame`, `UtensilsCrossed`
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>El fichero page.tsx no contiene ninguna de las cadenas prohibidas listadas arriba. `npx tsc --noEmit` completa sin errores en los ficheros modificados.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminar JSX de nutrición (botón header y card lateral)</name>
  <files>app/(trainer)/clients/[id]/page.tsx</files>
  <action>
Continuar editando `app/(trainer)/clients/[id]/page.tsx` eliminando el JSX de nutrición:

**1. Botón en el header (líneas 256-260 aprox.):**

Antes:
```tsx
<div className="flex gap-2 flex-wrap flex-shrink-0">
  <Link href={`/clients/${id}/revisions`} ...>...</Link>
  <AssignNutritionPlanModal
    clientId={rawClient.id}
    clientName={clientName}
    templates={nutritionTemplates}
  />
  <AssignPlanButton clientId={rawClient.id} plans={trainerPlans} />
</div>
```

Después (eliminar solo el bloque `<AssignNutritionPlanModal .../>`, conservar Link de Revisiones y AssignPlanButton):
```tsx
<div className="flex gap-2 flex-wrap flex-shrink-0">
  <Link href={`/clients/${id}/revisions`} ...>...</Link>
  <AssignPlanButton clientId={rawClient.id} plans={trainerPlans} />
</div>
```

**2. Card de nutrición en la columna lateral (líneas 437-513 aprox.):**

La columna lateral tiene esta estructura:
```tsx
{/* Columna lateral — nutrición */}
<div className="xl:w-72 space-y-4 flex-shrink-0">
  <Card>  {/* Card de nutrición — ELIMINAR ESTE CARD COMPLETO */}
    <CardHeader>...</CardHeader>
    <CardContent>...</CardContent>
  </Card>
  {/* Plan activo */}
  <Card>  {/* CONSERVAR ÍNTEGRO */}
    <CardContent>...</CardContent>
  </Card>
</div>
```

Eliminar el primer `<Card>` completo (el de "Plan nutricional", líneas 438-513 aprox.) conservando la `<div className="xl:w-72 ...">` y el segundo `<Card>` de "Plan activo" (líneas 515-542).

El comentario `{/* Columna lateral — nutrición */}` puede actualizarse a `{/* Columna lateral */}` o eliminarse.

**NUNCA tocar** el bloque Card de "Plan activo" (el que muestra `activePlan.name`, `weeklyWorkoutsCompleted`, etc.).
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `npm run build` completa con 0 errores.
    - Ninguna de las cadenas prohibidas aparece en page.tsx.
    - El Card "Plan activo" sigue presente en la columna lateral.
    - La página /clients/[id] se renderiza sin la sección de nutrición.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación visual — ausencia de UI de nutrición</name>
  <what-built>Eliminación completa de UI de nutrición en /clients/[id]. Cambios aplicados: 4 imports eliminados, Promise.all reducido de 7 a 5 promesas, 3 variables eliminadas, botón AssignNutritionPlanModal eliminado del header, Card "Plan nutricional" eliminado de la columna lateral. Build limpio.</what-built>
  <how-to-verify>
    1. Ejecutar `npm run dev`
    2. Navegar a `/clients/{id}` de cualquier cliente
    3. Verificar que NO aparece botón "Asignar plan nutricional" en el header (junto a "Revisiones" y "Asignar plan")
    4. Verificar que NO aparece card "Plan nutricional" en la columna lateral derecha
    5. Verificar que SÍ aparece card "Plan activo" en la columna lateral derecha
    6. Verificar que los StatCards (Peso, % Grasa, Masa libre grasa, Adherencia) siguen presentes
    7. Verificar que los gráficos de Peso y Cintura siguen presentes
  </how-to-verify>
  <action>Verificar visualmente que la página /clients/[id] no muestra UI de nutrición tras ejecutar npm run dev.</action>
  <verify>
    <automated>MANUAL — inspección visual en navegador</automated>
  </verify>
  <done>Usuario confirma ausencia de botón y card de nutrición, presencia del card Plan activo y correcta compilación.</done>
  <resume-signal>Escribe "aprobado" o describe los problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
Build limpio (`npm run build` → 0 errores) + inspección visual confirmando ausencia de UI de nutrición en /clients/[id].

Búsqueda de cadenas prohibidas en page.tsx:
```bash
grep -n "AssignNutritionPlanModal\|EditNutritionPlanModal\|NutritionTemplate\|nutrition-actions\|activeNutritionPlan\|nutritionTemplates\|nutrition_plans\|nutrition_plan_meals\|Flame" "app/(trainer)/clients/[id]/page.tsx"
```
Resultado esperado: sin coincidencias.
</verification>

<success_criteria>
- P45-SC1: En /clients/[id] no aparece ningún enlace, botón ni sección relacionada con nutrición
- P45-SC2: Los ficheros auxiliares de nutrición existen en disco pero no se importan desde page.tsx (no son accesibles desde la UI)
- Build limpio: `npm run build` → 0 errores TypeScript o de compilación
- El card "Plan activo" (entrenamiento) permanece intacto en la columna lateral
</success_criteria>

<output>
After completion, create `.planning/phases/045-ocultar-nutricion-en-trainer-panel-vista-cliente/045-01-SUMMARY.md`
</output>
