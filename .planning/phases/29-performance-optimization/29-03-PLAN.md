---
phase: 29-performance-optimization
plan: 03
type: execute
wave: 2
depends_on:
  - 29-01
  - 29-02
files_modified:
  - supabase/migrations/add-performance-indexes.sql
autonomous: false
requirements:
  - PERF-03
  - PERF-04
  - PERF-05
user_setup:
  - service: supabase
    why: "Crear índices de base de datos requiere acceso al SQL Editor de Supabase"
    dashboard_config:
      - task: "Ejecutar script SQL con los 4 CREATE INDEX CONCURRENTLY"
        location: "Supabase Dashboard > SQL Editor"
must_haves:
  truths:
    - "Existen índices en client_measurements.client_id, workout_sessions.client_id, food_log.client_id, set_logs.session_id"
    - "Las queries filtradas por estas columnas usan los índices (EXPLAIN ANALYZE lo confirma)"
    - "Los índices se crearon con CONCURRENTLY — sin downtime ni bloqueo de writes"
    - "El build de Next.js completa sin errores tras todos los cambios de la fase"
    - "TTFB de dashboard, today y progress es < 2s medido con Lighthouse en producción local"
  artifacts:
    - path: "supabase/migrations/add-performance-indexes.sql"
      provides: "Script idempotente con los 4 CREATE INDEX CONCURRENTLY IF NOT EXISTS"
      contains: "idx_client_measurements_client_id"
  key_links:
    - from: "supabase/migrations/add-performance-indexes.sql"
      to: "Supabase PostgreSQL"
      via: "checkpoint:human-action — ejecutar en SQL Editor"
      pattern: "CREATE INDEX CONCURRENTLY"
---

<objective>
Crear los 4 índices de PostgreSQL en las columnas más frecuentemente filtradas del proyecto. El script es idempotente (`IF NOT EXISTS`) y usa `CONCURRENTLY` para evitar bloqueos en producción. Verificar build limpio y medir TTFB para confirmar SC-04 (< 2s).

Purpose: Los índices DB son el fix de menor riesgo y mayor impacto en query time. Las columnas client_id y session_id aparecen en todas las queries críticas pero pueden no tener índice explícito si no fueron creadas con una FK constraint que lo genere automáticamente.
Output: Script SQL en `supabase/migrations/` + índices aplicados en Supabase + build limpio confirmado + baseline TTFB medido.
</objective>

<execution_context>
@/Users/jofreatanet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jofreatanet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-performance-optimization/29-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear script SQL de índices</name>
  <files>supabase/migrations/add-performance-indexes.sql</files>
  <action>
Crear el archivo `supabase/migrations/add-performance-indexes.sql` con el siguiente contenido exacto:

```sql
-- Phase 29: Performance Optimization
-- Índices en columnas frecuentemente filtradas
-- Idempotente: IF NOT EXISTS — seguro de re-ejecutar
-- CONCURRENTLY: sin bloqueo de writes en producción

-- Verificar índices existentes antes de crear (opcional, para diagnóstico):
-- SELECT tablename, indexname, indexdef
-- FROM pg_indexes
-- WHERE tablename IN ('client_measurements', 'workout_sessions', 'food_log', 'set_logs')
-- ORDER BY tablename, indexname;

CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_client_measurements_client_id
  ON public.client_measurements (client_id);

CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_workout_sessions_client_id
  ON public.workout_sessions (client_id);

CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_food_log_client_id
  ON public.food_log (client_id);

CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_set_logs_session_id
  ON public.set_logs (session_id);
```

Verificar que el directorio `supabase/migrations/` existe antes de crear. Si no existe, crearlo.

NOTA: Este archivo es documentación del estado de la DB. Los índices se aplican manualmente en el checkpoint siguiente, no via Supabase CLI en este proyecto.
  </action>
  <verify>
    <automated>test -f "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi/supabase/migrations/add-performance-indexes.sql" && grep -c "CREATE INDEX" "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi/supabase/migrations/add-performance-indexes.sql"</automated>
  </verify>
  <done>El archivo existe y contiene exactamente 4 sentencias CREATE INDEX CONCURRENTLY IF NOT EXISTS.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar build completo sin errores</name>
  <files></files>
  <action>
Ejecutar un build de producción completo para confirmar que los cambios de la fase (unstable_cache en dashboard y progress, revalidateTag en actions) no introducen errores de compilación ni warnings de cookies context leak.

```bash
cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx next build 2>&1 | tail -30
```

El output debe terminar con `✓ Compiled successfully` o similar. Si hay errores:
- Errores de TypeScript en archivos modificados por la fase → corregir antes de continuar
- Warning sobre `unstable_cache` y cookies → es esperado si Next.js detecta que se pasan cookies accidentalmente; verificar que `createAdminClient()` se usa (no `createClient()`) dentro de las funciones cacheadas
- Cualquier otro error → diagnosticar y corregir

NO continuar al checkpoint de índices si el build falla.
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>El build de Next.js completa sin errores. La salida muestra compilación exitosa de todas las rutas incluyendo /(trainer)/dashboard y /(client)/progress.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Ejecutar script de índices en Supabase SQL Editor</name>
  <what-built>Script SQL con 4 índices CONCURRENTLY listo en supabase/migrations/add-performance-indexes.sql</what-built>
  <how-to-verify>
    1. Abrir Supabase Dashboard: https://app.supabase.com
    2. Seleccionar el proyecto Fitness Bassi
    3. Ir a: SQL Editor (menú izquierdo)
    4. Copiar y pegar el contenido de `supabase/migrations/add-performance-indexes.sql`
    5. Ejecutar (botón Run o Cmd+Enter)
    6. Verificar que no hay errores en el output
    7. (Opcional) Verificar que los índices existen ejecutando esta query de diagnóstico:
       ```sql
       SELECT tablename, indexname
       FROM pg_indexes
       WHERE indexname IN (
         'idx_client_measurements_client_id',
         'idx_workout_sessions_client_id',
         'idx_food_log_client_id',
         'idx_set_logs_session_id'
       )
       ORDER BY tablename;
       ```
       Debe devolver 4 filas.

    NOTA: `CONCURRENTLY` puede tardar algunos segundos en tablas con muchos datos. Es normal.
    NOTA: Si algún índice ya existía con ese nombre, `IF NOT EXISTS` lo salta sin error.
  </how-to-verify>
  <action>Abrir Supabase Dashboard > SQL Editor, pegar el contenido de supabase/migrations/add-performance-indexes.sql y ejecutarlo. Verificar con la query de diagnóstico que los 4 índices existen.</action>
  <files>supabase/migrations/add-performance-indexes.sql</files>
  <verify>En Supabase SQL Editor: SELECT count(*) FROM pg_indexes WHERE indexname IN ('idx_client_measurements_client_id','idx_workout_sessions_client_id','idx_food_log_client_id','idx_set_logs_session_id') — debe devolver 4.</verify>
  <done>Los 4 índices existen en Supabase y la query de diagnóstico devuelve 4 filas.</done>
  <resume-signal>Escribe "índices aplicados" cuando los 4 índices existan en Supabase, o describe cualquier error encontrado</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Medir TTFB con Lighthouse para confirmar PERF-04 (< 2s)</name>
  <what-built>Cache de datos (unstable_cache) + índices DB aplicados. Todas las optimizaciones de la fase están activas.</what-built>
  <how-to-verify>
    Medir el tiempo de carga de las 3 páginas críticas para confirmar que cargan en menos de 2 segundos.

    **Opción A — Local con next build (recomendado para baseline limpio):**
    1. En terminal:
       ```bash
       cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi"
       npx next build && npx next start
       ```
    2. Abrir Chrome en modo incógnito
    3. Abrir DevTools > Lighthouse
    4. Para cada una de estas páginas, ejecutar un audit con "Performance" activado:
       - http://localhost:3000/dashboard  (trainer)
       - http://localhost:3000/today      (client)
       - http://localhost:3000/progress   (client)
    5. Anotar el valor de **TTFB** (Time to First Byte) en el panel de Network timing
       - También válido: el campo "Server response time" en Lighthouse > Diagnostics
    6. Criterio de éxito: TTFB < 2000ms para las 3 páginas

    **Opción B — Vercel Dashboard (si está desplegado):**
    1. Ir a Vercel Dashboard > tu proyecto > Analytics
    2. Revisar TTFB en las rutas /dashboard, /today, /progress
    3. Criterio idéntico: < 2000ms

    **Segunda visita (cache caliente):**
    - Después del primer load, recargar la página (Cmd+R) y medir TTFB de nuevo
    - En la segunda visita (cache activo), el TTFB debería ser notablemente menor
    - Si la primera visita supera 2s pero la segunda no → el cache funciona, considera aceptable

    NOTA: En desarrollo (`next dev`) el cache no funciona igual — usar `next build && next start`.
  </how-to-verify>
  <resume-signal>Escribe los valores de TTFB medidos (ej. "dashboard: 800ms, today: 600ms, progress: 900ms — todos OK") o describe si alguna página supera 2s y el contexto (primera vs segunda visita)</resume-signal>
</task>

</tasks>

<verification>
Verificación del script SQL generado:
```bash
cat "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi/supabase/migrations/add-performance-indexes.sql"
```

Los 4 índices deben existir en Supabase (verificado en el checkpoint con la query de diagnóstico).

Build limpio confirmado (Task 2).

TTFB < 2s confirmado por el usuario en Task 4.
</verification>

<success_criteria>
- `supabase/migrations/add-performance-indexes.sql` existe con 4 sentencias CREATE INDEX CONCURRENTLY IF NOT EXISTS
- Los 4 índices existen en Supabase: idx_client_measurements_client_id, idx_workout_sessions_client_id, idx_food_log_client_id, idx_set_logs_session_id
- El script se ejecutó sin errores
- `npx next build` completa sin errores (SC-05: no regresiones)
- TTFB < 2s para dashboard, today y progress confirmado por el usuario (PERF-04)
</success_criteria>

<output>
Después de completar, crear `.planning/phases/29-performance-optimization/29-03-SUMMARY.md`
</output>
