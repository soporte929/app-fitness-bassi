---
phase: 29-performance-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(client)/progress/page.tsx
  - app/(client)/progress/actions.ts
autonomous: true
requirements:
  - PERF-02
  - PERF-05
must_haves:
  truths:
    - "La página /progress carga más rápido en visitas recurrentes (datos desde cache)"
    - "Al registrar peso o medidas, la página /progress se revalida y muestra los nuevos datos"
    - "No hay regresiones — error handling y empty states siguen funcionando tras añadir cache"
  artifacts:
    - path: "app/(client)/progress/page.tsx"
      provides: "Progress con unstable_cache — getClientProgressData cacheada, tag progress-{clientId}"
      contains: "unstable_cache"
    - path: "app/(client)/progress/actions.ts"
      provides: "revalidateTag en logClientMeasurementAction"
      contains: "revalidateTag"
  key_links:
    - from: "app/(client)/progress/page.tsx"
      to: "unstable_cache en next/cache"
      via: "getClientProgressData(client.id)"
      pattern: "unstable_cache"
    - from: "app/(client)/progress/actions.ts"
      to: "Data Cache de Next.js"
      via: "revalidateTag(`progress-${clientId}`)"
      pattern: "revalidateTag"
---

<objective>
Añadir unstable_cache a la página /progress del cliente para que las queries a client_measurements y workout_sessions se cacheen 30 segundos, y añadir revalidateTag en la action de logging para invalidar el cache cuando el cliente registra nuevos datos.

Purpose: La página /progress hace queries sobre toda la historia de mediciones y sesiones del cliente — potencialmente muchas filas. Con cache de 30s, las visitas repetidas no pagan el coste de Supabase. Con revalidateTag en la action, los datos se refrescan inmediatamente al registrar.
Output: progress/page.tsx con getClientProgressData cacheada + progress/actions.ts con revalidateTag.
</objective>

<execution_context>
@/Users/jofreatanet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jofreatanet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-performance-optimization/29-RESEARCH.md

<interfaces>
<!-- Interfaces clave del código existente -->

De app/(client)/progress/page.tsx (queries actuales — líneas 24-42):
```typescript
// Ya usa Promise.all correctamente — solo hay que envolver en unstable_cache
const [measurementsResult, sessionsResult] = await Promise.all([
  supabase
    .from('client_measurements')
    .select('measured_at, weight_kg, body_fat_pct, waist_cm, hip_cm, chest_cm, arm_cm, thigh_cm')
    .eq('client_id', client.id)
    .order('measured_at', { ascending: true }),
  supabase
    .from('workout_sessions')
    .select(`id, started_at, finished_at, completed, set_logs!set_logs_session_id_fkey (id, weight_kg, reps, exercise:exercises!set_logs_exercise_id_fkey (id, name))`)
    .eq('client_id', client.id)
    .order('started_at', { ascending: true }),
])
```

De app/(client)/progress/actions.ts (mutation actual):
```typescript
export async function logClientMeasurementAction(clientId: string, payload: {...})
// usa createAdminClient() — seguro para unstable_cache
// tiene revalidatePath('/progress'), falta revalidateTag
```

Tipos exportados de progress-charts:
```typescript
import type { WeightLog, Measurement, SessionForProgress } from '@/components/client/progress-charts'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extraer queries de progress en getClientProgressData con unstable_cache</name>
  <files>app/(client)/progress/page.tsx</files>
  <action>
En progress/page.tsx:

1. Añadir imports al top:
```typescript
import { unstable_cache } from 'next/cache'
import { createAdminClient } from '@/lib/supabase/admin'
```

2. Crear función `getClientProgressData` fuera del componente, usando `unstable_cache`:

```typescript
const getClientProgressData = unstable_cache(
  async (clientId: string) => {
    const supabase = createAdminClient()

    const [measurementsResult, sessionsResult] = await Promise.all([
      supabase
        .from('client_measurements')
        .select('measured_at, weight_kg, body_fat_pct, waist_cm, hip_cm, chest_cm, arm_cm, thigh_cm')
        .eq('client_id', clientId)
        .order('measured_at', { ascending: true }),
      supabase
        .from('workout_sessions')
        .select(
          `id, started_at, finished_at, completed,
          set_logs!set_logs_session_id_fkey (
            id, weight_kg, reps,
            exercise:exercises!set_logs_exercise_id_fkey (id, name)
          )`
        )
        .eq('client_id', clientId)
        .order('started_at', { ascending: true }),
    ])

    return { measurementsResult, sessionsResult }
  },
  ['client-progress-data'],
  { revalidate: 30, tags: ['client-progress'] }
)
```

3. En el componente `ProgressPage()`:
   - Mantener `supabase.auth.getUser()` directamente (no cachear auth)
   - Mantener la query de `clients` directamente (necesita `user.id` y devuelve `client.id` y `target_weight_kg`)
   - Reemplazar el `Promise.all` directo por: `const { measurementsResult, sessionsResult } = await getClientProgressData(client.id)`
   - El resto del componente (error handling, transformaciones, render) permanece sin cambios

CRÍTICO: NO cachear `supabase.auth.getUser()` ni la query de `clients`. Estas son ligeras y deben ser siempre frescas para garantizar autenticación y autorización.

CRÍTICO: El error handling existente (`if (measurementsResult.error || sessionsResult.error)`) debe permanecer intacto — `getClientProgressData` devuelve los objetos de resultado completos (con `.error` y `.data`) para que el error check siga funcionando igual.
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "progress/page|error TS" | head -20</automated>
  </verify>
  <done>progress/page.tsx compila sin errores TypeScript. `unstable_cache` importado, `getClientProgressData` definida fuera del componente con TTL 30s y tag `client-progress`. El error handling existente sigue intacto.</done>
</task>

<task type="auto">
  <name>Task 2: Añadir revalidateTag en logClientMeasurementAction</name>
  <files>app/(client)/progress/actions.ts</files>
  <action>
En app/(client)/progress/actions.ts:

1. Añadir `revalidateTag` al import existente de `next/cache`:
```typescript
import { revalidatePath, revalidateTag } from 'next/cache'
```

2. En `logClientMeasurementAction`, después de `revalidatePath('/progress')`, añadir:
```typescript
revalidateTag('client-progress')
```

El tag `'client-progress'` invalida todas las entradas del cache (de todos los clientes). Esto es aceptable — solo hay un cliente autenticado por request, y el cache es keyed por `clientId` de todas formas. Si en el futuro hay preocupación de performance, usar `revalidateTag(\`progress-${clientId}\`)` — pero entonces el tag en `unstable_cache` también debe actualizarse.

IMPORTANTE: NO eliminar `revalidatePath('/progress')` — es complementario al revalidateTag.

Nota: El archivo usa `err: any` en el catch — no modificar ese patrón existente (es un TS error pre-existente no relacionado con esta tarea).
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "progress/actions|error TS" | head -20</automated>
  </verify>
  <done>progress/actions.ts compila. `logClientMeasurementAction` llama `revalidateTag('client-progress')` después de `revalidatePath('/progress')`.</done>
</task>

</tasks>

<verification>
```bash
cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | tail -5
```

Verificar que los cambios están presentes:
```bash
grep -n "unstable_cache\|getClientProgressData\|revalidateTag" \
  "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi/app/(client)/progress/page.tsx" \
  "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi/app/(client)/progress/actions.ts"
```
</verification>

<success_criteria>
- `app/(client)/progress/page.tsx` contiene `unstable_cache` con TTL 30s y tag `client-progress`
- La función `getClientProgressData` usa `createAdminClient()` (no el cliente SSR con cookies)
- El error handling (`measurementsResult.error || sessionsResult.error`) sigue funcionando
- `app/(client)/progress/actions.ts` llama `revalidateTag('client-progress')` en `logClientMeasurementAction`
- TypeScript sin errores en ambos archivos
</success_criteria>

<output>
Después de completar, crear `.planning/phases/29-performance-optimization/29-02-SUMMARY.md`
</output>
