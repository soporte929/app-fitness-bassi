---
phase: 17
plan: 2
wave: 2
depends_on: [17-01]
files_modified:
  - app/(client)/layout.tsx
  - app/(auth)/login/page.tsx
  - components/ui/loading-screen.tsx
  - components/client/today-exercises-progress.tsx
  - app/(trainer)/clients/[id]/revisions/page.tsx
  - app/(trainer)/clients/[id]/revisions/new/page.tsx
  - app/(trainer)/clients/[id]/revisions/feedback-editor.tsx
  - app/(trainer)/clients/clients-list.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "No inline style contains hardcoded dark hex colors (#191919, #212121, #111111, #2a2a2a)"
    - "All replaced colors use CSS variables (var(--bg-base), var(--bg-surface), var(--bg-elevated), var(--border))"
    - "Components render correctly in both dark and light mode"
  artifacts:
    - "All files in files_modified use CSS var references instead of hardcoded hex"
---

# Plan 17.2: Replace hardcoded dark colors — Layouts & Core Components

<objective>
Eliminate all hardcoded dark hex colors from layout files, the login page, loading screen, and trainer revision pages. Replace with CSS variables that respond to theme changes.

Purpose: Without this, toggling to light mode leaves these components with dark backgrounds, making the app unusable in light mode.
Output: All listed files use CSS variables instead of hardcoded hex, and look correct in both themes.
</objective>

<context>
Load for context:
- app/globals.css (CSS variable definitions for both themes)
- app/(client)/layout.tsx
- components/ui/loading-screen.tsx
- components/client/today-exercises-progress.tsx
- app/(auth)/login/page.tsx
- app/(trainer)/clients/[id]/revisions/page.tsx
- app/(trainer)/clients/[id]/revisions/new/page.tsx
- app/(trainer)/clients/[id]/revisions/feedback-editor.tsx
- app/(trainer)/clients/clients-list.tsx
</context>

<tasks>

<task type="auto">
  <name>Fix client layout and loading screen</name>
  <files>
    app/(client)/layout.tsx
    components/ui/loading-screen.tsx
    components/client/today-exercises-progress.tsx
  </files>
  <action>
    **Color mapping:**
    - `#111111` → `var(--bg-base)` (outermost wrapper darkest bg)
    - `#191919` → `var(--bg-base)` (main page bg)
    - `rgba(255,255,255,0.06)` → `var(--border)` (border lines)

    1. **`app/(client)/layout.tsx`:**
       - Line 11: Replace `style={{ background: '#111111' }}` with `style={{ background: 'var(--bg-base)' }}` — or better, use `className="bg-[var(--bg-base)]"` and remove the inline style.
       - Line 14: Replace `style={{ background: '#191919' }}` with `style={{ background: 'var(--bg-base)' }}`.
       - Line 18: Replace `style={{ height: '44px', background: '#191919', borderBottom: '1px solid rgba(255,255,255,0.06)' }}` with `style={{ height: '44px', background: 'var(--bg-base)', borderBottom: '1px solid var(--border)' }}`.

    2. **`components/ui/loading-screen.tsx`:**
       - Line 81: Replace `style={{ background: '#191919' }}` with `style={{ background: 'var(--bg-base)' }}`.

    3. **`components/client/today-exercises-progress.tsx`:**
       - Line 109: Replace `style={{ background: '#191919', borderBottom: '1px solid rgba(255,255,255,0.06)' }}` with `style={{ background: 'var(--bg-base)', borderBottom: '1px solid var(--border)' }}`.
       - Line 198 (if `#212121`): Replace with `var(--bg-surface)`.

    AVOID: Do NOT change the layout structure — only swap color values.
    AVOID: Do NOT remove `min-h-screen`, `max-w-[430px]`, or other size constraints.
  </action>
  <verify>Toggle theme — client app should have correct background in both modes.</verify>
  <done>Client layout, loading screen, and exercises progress use CSS vars. No hardcoded hex colors remain in these files.</done>
</task>

<task type="auto">
  <name>Fix login page and trainer revision pages</name>
  <files>
    app/(auth)/login/page.tsx
    app/(trainer)/clients/[id]/revisions/page.tsx
    app/(trainer)/clients/[id]/revisions/new/page.tsx
    app/(trainer)/clients/[id]/revisions/feedback-editor.tsx
    app/(trainer)/clients/clients-list.tsx
  </files>
  <action>
    **Color mapping (consistent with globals.css):**
    - `#191919` → `var(--bg-base)`
    - `#1f1f1f` → `var(--bg-base)` (gradient center)
    - `#212121` → `var(--bg-surface)`
    - `#e8e8e6` → `var(--text-primary)`
    - `rgba(255,255,255,0.06/0.07/0.08)` → `var(--border)`

    1. **`app/(auth)/login/page.tsx`:**
       - Line 49: Replace `background: "radial-gradient(ellipse at center, #1f1f1f 0%, #191919 70%)"` with `background: "radial-gradient(ellipse at center, var(--bg-surface) 0%, var(--bg-base) 70%)"`.
       - Line 135: Replace `background: "#212121"` with `background: "var(--bg-surface)"`.

    2. **`app/(trainer)/clients/[id]/revisions/page.tsx`:**
       - Line 59: Replace `style={{ background: '#191919', color: '#e8e8e6' }}` with `style={{ background: 'var(--bg-base)', color: 'var(--text-primary)' }}`.
       - Lines 82, 101: Replace `style={{ background: '#e8e8e6', color: '#191919' }}` with `style={{ background: 'var(--accent)', color: 'var(--accent-text)' }}` (these are buttons/CTAs — use accent colors).
       - Line 116: Replace `background: '#212121'` with `background: 'var(--bg-surface)'`.

    3. **`app/(trainer)/clients/[id]/revisions/new/page.tsx`:**
       - Line 58: Replace `style={{ background: '#191919', color: '#e8e8e6' }}` with `style={{ background: 'var(--bg-base)', color: 'var(--text-primary)' }}`.
       - Lines 82, 118, 167: Replace `style={{ background: '#212121', border: ... }}` with `style={{ background: 'var(--bg-surface)', border: '1px solid var(--border)' }}`.
       - Line 203: Replace `style={{ background: '#e8e8e6', color: '#191919' }}` with `style={{ background: 'var(--accent)', color: 'var(--accent-text)' }}`.

    4. **`app/(trainer)/clients/[id]/revisions/feedback-editor.tsx`:**
       - Line 80: Replace `style={{ background: '#e8e8e6', color: '#191919' }}` with `style={{ background: 'var(--accent)', color: 'var(--accent-text)' }}`.

    5. **`app/(trainer)/clients/clients-list.tsx`:**
       - Line 226: Replace `style={{ background: '#212121', border: '1px solid rgba(255,255,255,0.07)' }}` with `style={{ background: 'var(--bg-surface)', border: '1px solid var(--border)' }}`.

    AVOID: Do NOT change `.login-input` styles — those are defined in globals.css and already use some vars. They will be addressed in a separate globals.css update if needed.
  </action>
  <verify>Toggle theme — login page and revision pages render correctly in both modes.</verify>
  <done>Login page and all revision/clients pages use CSS variables instead of hardcoded dark hex values.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] `grep -r "#191919" --include="*.tsx"` in modified files returns zero results
- [ ] `grep -r "#111111" --include="*.tsx"` in modified files returns zero results
- [ ] `grep -r "#212121"` in modified files returns zero results
- [ ] Light mode looks correct on client app, login, and trainer revision pages
- [ ] Dark mode still looks correct (no visual regressions)
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
