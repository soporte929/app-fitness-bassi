---
phase: 01-workout-session
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/(client)/today/page.tsx
  - components/client/active-session-banner.tsx
autonomous: true
requirements:
  - WORKOUT-06
  - WORKOUT-07

must_haves:
  truths:
    - "Navigating to /today while a session is active redirects to /workout/[sessionId]"
    - "When no session is active, /today shows the same empty state as before"
    - "Active session banner button navigates to /workout/[sessionId] instead of /today"
    - "Active session banner no longer appears when the user is on the workout page"
  artifacts:
    - path: "app/(client)/today/page.tsx"
      provides: "today page with active-session redirect"
      contains: "redirect(`/workout/"
    - path: "components/client/active-session-banner.tsx"
      provides: "Banner linking to /workout/[sessionId]"
      contains: "router.push(`/workout/"
  key_links:
    - from: "app/(client)/today/page.tsx"
      to: "app/(client)/workout/[sessionId]/page.tsx"
      via: "redirect('/workout/' + session.id) when incomplete session found"
      pattern: "redirect.*workout"
    - from: "components/client/active-session-banner.tsx"
      to: "app/(client)/workout/[sessionId]/page.tsx"
      via: "router.push('/workout/' + activeSession.id)"
      pattern: "router.push.*workout"
---

<objective>
Wire the remaining navigation so that every entry point leads to /workout/[sessionId]. Two small, targeted changes: today/page.tsx redirects to the active session, and the active session banner links to it.

Purpose: Without these changes, the workout page is orphaned — today/ still shows the old in-page workout UI and the banner still points to /today. These changes make /workout/[sessionId] the canonical and only URL for active workouts.

Output: Updated app/(client)/today/page.tsx + updated components/client/active-session-banner.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workout-session/01-CONTEXT.md
@.planning/phases/01-workout-session/01-01-SUMMARY.md

<interfaces>
<!-- Current file contents the executor must understand before editing. -->

Current app/(client)/today/page.tsx structure (read the file before editing):
- Lines 1-8: imports (CheckCircle2, Flame, finishWorkout, TodayExercisesProgress all imported)
- Lines 10-24: calcStreak() function definition
- Lines 26-91: TodayPage component, including streakResult parallel fetch and streak variable
- Lines 76-91: `if (!session)` empty state return — KEEP THIS UNCHANGED
- Lines 93-166: full workout rendering block (exercises, setLogs, TodayExercisesProgress, finish button) — REMOVE THIS

After the redirect is added and dead code removed, today/page.tsx has two branches only:
1. session exists → redirect to /workout/[sessionId] (new)
2. no session → show empty state (unchanged)

Cleanup is NOT optional — it is mandatory. The workout rendering block (lines 93-166) becomes
dead code once the redirect is added. Dead code with unused imports causes TypeScript errors.

Cleanup checklist (ALL of these must be removed together — removing any subset causes TS errors):
- Remove: calcStreak() function (lines 10-24)
- Remove: streakResult from the Promise.all (line 58-64 in current file)
- Remove: streak variable and dateLabel variable (lines 67-74)
- Remove: streak JSX block (lines 143-148, the Flame + "Racha X días" block)
- Remove: entire workout rendering block (lines 93-166, i.e. the exercisesResult/setLogsResult fetches + TodayExercisesProgress + finish button)
- Remove imports: CheckCircle2, Flame, finishWorkout, TodayExercisesProgress
- Keep imports: createClient, redirect, Link, PageTransition, Button, Dumbbell

Rationale: The empty state (the only remaining render path) does not use streak, Flame, dateLabel,
finishWorkout, TodayExercisesProgress, or CheckCircle2. Leaving any of these as unused imports
will cause TypeScript strict-mode errors.

From components/client/active-session-banner.tsx — Lines to change:

Line ~126 (hide condition):
```typescript
// CURRENT:
if (!activeSession || pathname === '/today') return null
// CHANGE TO:
if (!activeSession || pathname === '/today' || pathname.startsWith('/workout')) return null
```

Line ~131 (onClick handler):
```typescript
// CURRENT:
onClick={() => router.push('/today')}
// CHANGE TO:
onClick={() => router.push(`/workout/${activeSession.id}`)}
```

These are the ONLY two changes to active-session-banner.tsx. Do not touch any other logic.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redirect today/ to active workout session + remove dead code</name>
  <files>app/(client)/today/page.tsx</files>
  <action>
Read the current app/(client)/today/page.tsx, then rewrite it with the following changes:

1. Add the redirect when a session exists. After `const session = sessionResult.data`, add:
```typescript
if (session) {
  redirect(`/workout/${session.id}`)
}
```
This goes BEFORE the existing `if (!session)` empty state block.

2. Remove ALL dead code and unused imports. Because the only remaining render path is the
empty state, the following must ALL be removed (removing a subset will cause TS errors):

   Imports to remove: CheckCircle2, Flame, finishWorkout, TodayExercisesProgress
   Code to remove: calcStreak() function, streakResult fetch (from Promise.all), streak variable,
   dateLabel variable, the Flame "Racha X días" JSX block, and the entire workout rendering block
   (exercisesResult / setLogsResult / dayResult / lastSessionResult / exercisesWithSets / finishAction
   / the TodayExercisesProgress JSX / the finish button form).

   Imports to keep: createClient, redirect, Link, PageTransition, Button, Dumbbell

3. Simplify the Promise.all: since streakResult is removed, the parallel fetch becomes a single
   call (no need for Promise.all wrapper):
   ```typescript
   const sessionResult = await supabase
     .from('workout_sessions')
     .select('id, day_id, started_at')
     .eq('client_id', client.id)
     .eq('completed', false)
     .gte('started_at', todayStart)
     .lte('started_at', todayEnd)
     .maybeSingle()
   const session = sessionResult.data
   ```

The final today/page.tsx should be ~40 lines: auth check, client fetch, today-window session check,
redirect if session, empty state JSX.
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>today/page.tsx contains redirect('/workout/' + session.id) when session found, does NOT import Flame/finishWorkout/TodayExercisesProgress/CheckCircle2, and npx tsc --noEmit reports zero errors across the project.</done>
</task>

<task type="auto">
  <name>Task 2: Update active session banner to link to /workout/[sessionId]</name>
  <files>components/client/active-session-banner.tsx</files>
  <action>
Read the current components/client/active-session-banner.tsx and make two targeted changes only:

Change 1 — Update the hide condition:
```typescript
// BEFORE:
if (!activeSession || pathname === '/today') return null

// AFTER:
if (!activeSession || pathname === '/today' || pathname.startsWith('/workout')) return null
```
Reason: Banner should hide on the workout page itself (user is already there), and on /today
(redirect will send them to /workout anyway).

Change 2 — Update the onClick handler:
```typescript
// BEFORE:
onClick={() => router.push('/today')}

// AFTER:
onClick={() => router.push(`/workout/${activeSession.id}`)}
```

No other changes. Keep all session polling logic, elapsed timer, set progress bar, and rest
timer countdown exactly as-is.
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>active-session-banner.tsx onClick routes to /workout/[sessionId], hide condition includes pathname.startsWith('/workout'), and npx tsc --noEmit reports zero errors across the project.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit` from project root — zero TypeScript errors across the entire project
2. today/page.tsx contains `redirect(\`/workout/${session.id}\`)` and does NOT import TodayExercisesProgress, finishWorkout, Flame, or CheckCircle2
3. active-session-banner.tsx onClick contains `/workout/${activeSession.id}` and hide condition includes `/workout`
4. No hardcoded colors introduced (use CSS vars: var(--text-primary), var(--bg-surface), etc.)
</verification>

<success_criteria>
- Visiting /today with an active session redirects to /workout/[sessionId] (not rendered in-page)
- Visiting /today without a session shows empty state with "No tienes entreno programado" and link to /routines
- Tapping the active session banner navigates to /workout/[sessionId]
- Banner does not appear when already on /workout/* (no double banner)
- TypeScript strict mode: zero new errors (verified by full npx tsc --noEmit, not file-scoped grep)
- All 7 WORKOUT requirements satisfied end-to-end:
  - WORKOUT-01: Start from routines → /workout/[sessionId] (Plan 01)
  - WORKOUT-02: Exercises visible with previous weights (Plan 01)
  - WORKOUT-03: Set logging works (Plan 01, reusing ExerciseCard)
  - WORKOUT-04: Rest timer appears after completing a set (RestTimer in layout, already works)
  - WORKOUT-05: Finish workout redirects to /history (Plan 01, reusing finishWorkout)
  - WORKOUT-06: Active session banner links to /workout/[sessionId] (this plan)
  - WORKOUT-07: /today redirects to /workout/[sessionId] when session active (this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/01-workout-session/01-02-SUMMARY.md`
</output>
