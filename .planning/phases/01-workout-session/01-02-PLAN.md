---
phase: 01-workout-session
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/(client)/today/page.tsx
  - components/client/active-session-banner.tsx
autonomous: true
requirements:
  - WORKOUT-06
  - WORKOUT-07

must_haves:
  truths:
    - "Navigating to /today while a session is active redirects to /workout/[sessionId]"
    - "When no session is active, /today shows the same empty state as before"
    - "Active session banner button navigates to /workout/[sessionId] instead of /today"
    - "Active session banner no longer appears when the user is on the workout page"
  artifacts:
    - path: "app/(client)/today/page.tsx"
      provides: "today page with active-session redirect"
      contains: "redirect(`/workout/"
    - path: "components/client/active-session-banner.tsx"
      provides: "Banner linking to /workout/[sessionId]"
      contains: "router.push(`/workout/"
  key_links:
    - from: "app/(client)/today/page.tsx"
      to: "app/(client)/workout/[sessionId]/page.tsx"
      via: "redirect('/workout/' + session.id) when incomplete session found"
      pattern: "redirect.*workout"
    - from: "components/client/active-session-banner.tsx"
      to: "app/(client)/workout/[sessionId]/page.tsx"
      via: "router.push('/workout/' + activeSession.id)"
      pattern: "router.push.*workout"
---

<objective>
Wire the remaining navigation so that every entry point leads to /workout/[sessionId]. Two small, targeted changes: today/page.tsx redirects to the active session, and the active session banner links to it.

Purpose: Without these changes, the workout page is orphaned — today/ still shows the old in-page workout UI and the banner still points to /today. These changes make /workout/[sessionId] the canonical and only URL for active workouts.

Output: Updated app/(client)/today/page.tsx + updated components/client/active-session-banner.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workout-session/01-CONTEXT.md
@.planning/phases/01-workout-session/01-01-SUMMARY.md

<interfaces>
<!-- Current file contents the executor must understand before editing. -->

From app/(client)/today/page.tsx — Current active-session branch (lines 49-167):
The page currently fetches an incomplete session for today (lines 49-57) then, if found (line 76 negated — `if (!session)`), falls through to render the full workout UI with TodayExercisesProgress.

Change needed: when `session` exists, add `redirect('/workout/' + session.id)` BEFORE falling through to the full render. The existing `if (!session)` empty state block stays unchanged.

After the change, the full workout rendering block (lines 93-166) can be removed since /workout/[sessionId] now handles it — but this is optional cleanup. At minimum: add the redirect so today never shows the workout UI.

From components/client/active-session-banner.tsx — Lines to change:

Line 126 (hide condition):
```typescript
// CURRENT:
if (!activeSession || pathname === '/today') return null
// CHANGE TO: also hide on /workout/* pages so the banner doesn't appear during active workout
if (!activeSession || pathname === '/today' || pathname.startsWith('/workout')) return null
```

Line 131 (onClick handler):
```typescript
// CURRENT:
onClick={() => router.push('/today')}
// CHANGE TO:
onClick={() => router.push(`/workout/${activeSession.id}`)}
```

These are the ONLY two changes to active-session-banner.tsx. Do not touch any other logic.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redirect today/ to active workout session</name>
  <files>app/(client)/today/page.tsx</files>
  <action>
Read the current app/(client)/today/page.tsx and make a targeted edit.

After the session detection block (the `sessionResult` / `const session = sessionResult.data` lines), add a redirect when a session exists:

```typescript
// After: const session = sessionResult.data
// Add:
if (session) {
  redirect(`/workout/${session.id}`)
}
```

This goes BEFORE the existing `if (!session)` block that renders the empty state.

After adding this redirect, the code that renders the active workout (lines 93 onwards with exercisesResult, setLogsResult, etc.) is now dead code — remove it to keep the file clean. The page should have two branches only:
1. session exists → redirect to /workout/[sessionId]
2. no session → show empty state (unchanged)

Keep the streak fetch (streakResult) and the dateLabel — but since the workout rendering is removed, the streak display can also be removed from the JSX since the page now only shows the empty state. Keep the empty state JSX unchanged (Dumbbell icon, "No tienes entreno programado para hoy", link to /routines).

The final today/page.tsx should be significantly shorter — fetching only the incomplete session and then either redirecting or showing the empty state.

Imports to remove after cleanup: `finishWorkout`, `TodayExercisesProgress`, `CheckCircle2`, `Flame` (if streak removed). Keep: `createClient`, `redirect`, `Link`, `PageTransition`, `Button`, `Dumbbell`.
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | grep -E "today/page" | head -10</automated>
  </verify>
  <done>today/page.tsx contains redirect('/workout/' + session.id) when session found, TypeScript reports no errors on this file, and the file no longer contains TodayExercisesProgress or finishWorkout imports.</done>
</task>

<task type="auto">
  <name>Task 2: Update active session banner to link to /workout/[sessionId]</name>
  <files>components/client/active-session-banner.tsx</files>
  <action>
Read the current components/client/active-session-banner.tsx and make two targeted changes only:

Change 1 — Update the hide condition (line ~126):
```typescript
// BEFORE:
if (!activeSession || pathname === '/today') return null

// AFTER:
if (!activeSession || pathname === '/today' || pathname.startsWith('/workout')) return null
```
Reason: Banner should hide on the workout page itself (user is already there), and on /today (redirect will send them to /workout anyway).

Change 2 — Update the onClick handler (line ~131):
```typescript
// BEFORE:
onClick={() => router.push('/today')}

// AFTER:
onClick={() => router.push(`/workout/${activeSession.id}`)}
```

No other changes. Keep all session polling logic, elapsed timer, set progress bar, and rest timer countdown exactly as-is.
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | grep -E "active-session-banner" | head -10</automated>
  </verify>
  <done>active-session-banner.tsx onClick routes to /workout/[sessionId], hide condition includes pathname.startsWith('/workout'), TypeScript reports no errors on this file.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit` from project root — zero TypeScript errors across the entire project
2. today/page.tsx contains `redirect(\`/workout/${session.id}\`)` and does NOT import TodayExercisesProgress or finishWorkout
3. active-session-banner.tsx onClick contains `/workout/${activeSession.id}` and hide condition includes `/workout`
4. No hardcoded colors introduced (use CSS vars: var(--text-primary), var(--bg-surface), etc.)
</verification>

<success_criteria>
- Visiting /today with an active session redirects to /workout/[sessionId] (not rendered in-page)
- Visiting /today without a session shows empty state with "No tienes entreno programado" and link to /routines
- Tapping the active session banner navigates to /workout/[sessionId]
- Banner does not appear when already on /workout/* (no double banner)
- TypeScript strict mode: zero new errors
- All 7 WORKOUT requirements satisfied end-to-end:
  - WORKOUT-01: Start from routines → /workout/[sessionId] ✓ (Plan 01)
  - WORKOUT-02: Exercises visible with previous weights ✓ (Plan 01)
  - WORKOUT-03: Set logging works ✓ (Plan 01, reusing ExerciseCard)
  - WORKOUT-04: Rest timer appears after completing a set ✓ (RestTimer in layout, already works)
  - WORKOUT-05: Finish workout redirects to /history ✓ (Plan 01, reusing finishWorkout)
  - WORKOUT-06: Active session banner links to /workout/[sessionId] ✓ (this plan)
  - WORKOUT-07: /today redirects to /workout/[sessionId] when session active ✓ (this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/01-workout-session/01-02-SUMMARY.md`
</output>
