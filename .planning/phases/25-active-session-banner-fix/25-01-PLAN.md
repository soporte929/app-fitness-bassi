---
phase: 25-active-session-banner-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/client/finish-workout-button.tsx
  - components/client/active-session-banner.tsx
  - app/(client)/today/actions.ts
  - app/(client)/workout/[sessionId]/page.tsx
autonomous: false
requirements:
  - BUG-03

must_haves:
  truths:
    - "Al pulsar Finalizar entrenamiento, el banner desaparece en menos de 1 segundo"
    - "El banner sigue apareciendo cuando hay una sesion activa real en curso"
    - "El banner no reaparece despues de finalizar (no hay regresion por re-fetch)"
  artifacts:
    - path: "components/client/finish-workout-button.tsx"
      provides: "Dispatcher del evento workoutFinished antes de llamar a la Server Action"
      contains: "window.dispatchEvent(new CustomEvent('workoutFinished'))"
    - path: "components/client/active-session-banner.tsx"
      provides: "Listener que limpia el estado del banner al recibir workoutFinished"
      contains: "window.addEventListener('workoutFinished', handleFinished)"
    - path: "app/(client)/today/actions.ts"
      provides: "Server Action finishWorkout que marca completed=true y redirige a /history"
      contains: "redirect('/history')"
  key_links:
    - from: "components/client/finish-workout-button.tsx"
      to: "window"
      via: "window.dispatchEvent(new CustomEvent('workoutFinished'))"
      pattern: "dispatchEvent.*workoutFinished"
    - from: "components/client/active-session-banner.tsx"
      to: "window"
      via: "window.addEventListener('workoutFinished', handleFinished)"
      pattern: "addEventListener.*workoutFinished"
    - from: "app/(client)/workout/[sessionId]/page.tsx"
      to: "components/client/finish-workout-button.tsx"
      via: "finishAction = finishWorkout.bind(null, session.id); <FinishWorkoutButton action={finishAction} />"
      pattern: "FinishWorkoutButton"
---

<objective>
Retroactive verification of BUG-03 fix: confirmar que la implementacion del evento personalizado `workoutFinished` ya presente en el codigo resuelve los tres criterios de exito del bug.

Purpose: BUG-03 fue resuelto en el Emergency Hotfix v5.0 (commit 7e4e6e3). Esta fase crea el PLAN.md retroactivo y el VERIFICATION.md que certifica la solucion.

Output: VERIFICATION.md en `.planning/phases/25-active-session-banner-fix/` que confirma BUG-03 resuelto.
</objective>

<execution_context>
@/Users/jofreatanet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jofreatanet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Implementacion actual verificada en el codebase. NO hay que modificar ningun archivo de codigo. -->

From components/client/finish-workout-button.tsx (COMPLETO — no tocar):
```tsx
'use client'

import { Button } from '@/components/ui/button'
import { CheckCircle2 } from 'lucide-react'

type Props = {
  action: () => Promise<void>
}

export function FinishWorkoutButton({ action }: Props) {
  async function handleClick() {
    // Dispatch event BEFORE the server action so the banner clears immediately
    window.dispatchEvent(new CustomEvent('workoutFinished'))
    // Execute the server action (which will redirect to /history)
    await action()
  }

  return (
    <form action={handleClick}>
      <Button type="submit" className="w-full" size="lg">
        <CheckCircle2 className="w-5 h-5" />
        Finalizar entrenamiento
      </Button>
    </form>
  )
}
```

From components/client/active-session-banner.tsx — evento listener (COMPLETO — no tocar):
```tsx
// Listen for immediate workout completion signal (dispatched by FinishWorkoutButton)
useEffect(() => {
  function handleFinished() {
    setActiveSession(null)
    setTotalSets(0)
    setCompletedSets(0)
  }
  window.addEventListener('workoutFinished', handleFinished)
  return () => window.removeEventListener('workoutFinished', handleFinished)
}, [])
```

From app/(client)/today/actions.ts — finishWorkout (COMPLETO — no tocar):
```ts
export async function finishWorkout(sessionId: string, _formData?: FormData): Promise<void> {
  const supabase = await createClient()
  const { error } = await supabase
    .from('workout_sessions')
    .update({ completed: true, finished_at: new Date().toISOString() })
    .eq('id', sessionId)
  if (!error) {
    redirect('/history')
  }
}
```

From app/(client)/workout/[sessionId]/page.tsx — wire-up (COMPLETO — no tocar):
```tsx
const finishAction = finishWorkout.bind(null, session.id)
// ...
<FinishWorkoutButton action={finishAction} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar implementacion del fix BUG-03 en los cuatro archivos</name>
  <files>components/client/finish-workout-button.tsx, components/client/active-session-banner.tsx, app/(client)/today/actions.ts, app/(client)/workout/[sessionId]/page.tsx</files>
  <action>
    Leer los cuatro archivos listados y confirmar que cada uno contiene exactamente los patrones requeridos por la solucion. No modificar ningun archivo. Documentar el estado actual en tu SUMMARY final.

    Verificaciones a confirmar en cada archivo:

    1. components/client/finish-workout-button.tsx:
       - Es un Client Component ('use client')
       - handleClick contiene `window.dispatchEvent(new CustomEvent('workoutFinished'))` ANTES del `await action()`
       - Renderiza un Button de @/components/ui/button con type="submit"

    2. components/client/active-session-banner.tsx:
       - Contiene un useEffect(() => { ... }, []) con window.addEventListener('workoutFinished', handleFinished)
       - handleFinished llama a setActiveSession(null), setTotalSets(0), setCompletedSets(0)
       - El cleanup hace window.removeEventListener('workoutFinished', handleFinished)
       - El polling de 10s en el efecto principal depende de [pathname]

    3. app/(client)/today/actions.ts:
       - finishWorkout actualiza workout_sessions con { completed: true, finished_at: new Date().toISOString() }
       - Llama a redirect('/history') si no hay error
       - La query usa .eq('id', sessionId) — no actualiza sesiones de otros clientes

    4. app/(client)/workout/[sessionId]/page.tsx:
       - Importa FinishWorkoutButton de @/components/client/finish-workout-button
       - Construye finishAction = finishWorkout.bind(null, session.id)
       - Renderiza FinishWorkoutButton action={finishAction}

    Si alguna verificacion falla (el patron no esta presente), anotar el desvio exacto en tu analisis. Si todo esta correcto, continuar a Task 2.
  </action>
  <verify>
    Lectura exitosa de los cuatro archivos con los patrones confirmados. Si algun archivo no contiene el patron esperado, el executor debe reportarlo antes de generar el VERIFICATION.md.
  </verify>
  <done>
    Los cuatro archivos contienen exactamente la implementacion descrita. El fix de BUG-03 esta presente en el codebase tal como fue introducido en el commit 7e4e6e3.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirmar comportamiento en la app — banner desaparece al finalizar</name>
  <files>app/(client)/workout/[sessionId]/page.tsx</files>
  <action>
    Pausa para verificacion manual del comportamiento del banner en la app. El executor debe presentar al usuario los pasos de verificacion y esperar confirmacion antes de continuar con Task 3.
  </action>
  <what-built>Verificacion de codigo del fix BUG-03 — custom event dispatch en FinishWorkoutButton + listener en ActiveSessionBanner.</what-built>
  <how-to-verify>
    Confirma manualmente que el bug esta resuelto en la app:

    1. Asegurate de que hay una sesion de entrenamiento activa (o inicia una desde /today).
    2. Navega a la sesion activa desde /today.
    3. Confirma que el banner "Entrenamiento activo" es visible en la parte inferior de la pantalla.
    4. Pulsa "Finalizar entrenamiento".
    5. Observa: el banner debe desaparecer ANTES de que la navegacion a /history sea visible (menos de 1 segundo, idealmente inmediato).
    6. Una vez en /history, confirma que el banner no reaparece (espera 15 segundos para cubrir el ciclo de polling).
    7. Navega a /today y confirma que el banner no aparece (la sesion esta completada).

    Resultado esperado:
    - Paso 5: banner desaparece instantaneamente al pulsar el boton
    - Paso 6: banner sigue ausente durante todo el tiempo en /history
    - Paso 7: banner ausente en /today tras finalizar
  </how-to-verify>
  <verify>Usuario ha confirmado los tres criterios de comportamiento.</verify>
  <done>Los tres criterios de exito de BUG-03 verificados manualmente por el usuario.</done>
  <resume-signal>Escribe "aprobado" si los tres criterios se cumplen, o describe el comportamiento observado si hay alguna regresion (ej. "el banner reaparece 2s despues").</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Crear VERIFICATION.md confirmando BUG-03 resuelto</name>
  <files>.planning/phases/25-active-session-banner-fix/25-VERIFICATION.md</files>
  <action>
    Crear el archivo `.planning/phases/25-active-session-banner-fix/25-VERIFICATION.md` con el siguiente contenido, completando la fecha actual y el resultado del UAT del checkpoint anterior.

    El archivo debe contener estas secciones:

    Cabecera: fase 25-active-session-banner-fix, fecha de verificacion (hoy), metodo "Code inspection + manual UAT", commit 7e4e6e3.

    Requirements Coverage: tabla con BUG-03 marcado como PASS.

    Success Criteria Verification con tres entradas:
    - SC-1: Banner desaparece en menos de 1s — PASS — evidencia: CustomEvent sincrono en handleClick antes de await action(), ActiveSessionBanner llama a setActiveSession(null) inmediatamente
    - SC-2: Banner aparece correctamente con sesion activa — PASS — evidencia: polling cada 10s en useEffect con dep [pathname] sigue operativo, el evento workoutFinished solo se dispara al pulsar el boton
    - SC-3: Banner no reaparece tras finalizar — PASS — evidencia: finishWorkout actualiza completed=true, fetchSession filtra .eq('completed', false) por lo que Supabase no devuelve la sesion en el siguiente polling

    Code Inspection: tabla con los cinco patrones verificados en Task 1, todos con "YES":
    - finish-workout-button.tsx: window.dispatchEvent antes de await action()
    - active-session-banner.tsx: window.addEventListener('workoutFinished') en useEffect deps []
    - active-session-banner.tsx: setActiveSession(null) + setTotalSets(0) + setCompletedSets(0) en handleFinished
    - today/actions.ts: update completed=true + redirect('/history')
    - workout/[sessionId]/page.tsx: FinishWorkoutButton con finishAction = finishWorkout.bind(null, session.id)

    UAT Sign-off: resultado del checkpoint (texto confirmado por el usuario en Task 2).

    Architecture Note: explicacion de por que CustomEvent es la eleccion correcta — los dos componentes no pueden compartir estado via props por distancia de layout, no requiere Zustand ni Context ni Supabase Realtime, dispatchEvent es sincrono garantizando que el listener ejecuta antes del render del redirect, sin nuevas dependencias de paquetes.
  </action>
  <verify>
    El archivo `.planning/phases/25-active-session-banner-fix/25-VERIFICATION.md` existe y contiene todas las secciones descritas.
  </verify>
  <done>
    VERIFICATION.md creado con los tres criterios de exito verificados como PASS y la evidencia de inspeccion de codigo documentada. BUG-03 queda certificado como resuelto.
  </done>
</task>

</tasks>

<verification>
BUG-03 esta resuelto cuando:
1. components/client/finish-workout-button.tsx — evento dispatched antes de await action()
2. components/client/active-session-banner.tsx — listener limpia estado inmediatamente
3. Manual: banner desaparece al pulsar el boton, no reaparece en el siguiente ciclo de polling
4. 25-VERIFICATION.md existe con los tres criterios como PASS
</verification>

<success_criteria>
- Los cuatro archivos contienen los patrones requeridos de la implementacion custom event
- El checkpoint humano confirma comportamiento correcto en la app
- .planning/phases/25-active-session-banner-fix/25-VERIFICATION.md existe y documenta BUG-03 como PASS
- BUG-03 aparece cubierto en la traceability
</success_criteria>

<output>
After completion, create `.planning/phases/25-active-session-banner-fix/25-01-SUMMARY.md` following the summary template.
</output>
