---
phase: 15-bug-fixes-logic-corrections
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(client)/layout.tsx
  - app/(client)/nutrition/page.tsx
  - components/client/nutrition/FoodSearchModal.tsx
  - app/manifest.ts
autonomous: true
requirements:
  - FEAT-01
  - FEAT-03
  - FEAT-04
  - FEAT-05

must_haves:
  truths:
    - "El botón + de Registro Libre está dentro del recuadro de la sección, no flotando por encima del contenido"
    - "El cliente puede alternar entre modo oscuro y claro desde la app cliente"
    - "Los headers de la app cliente permanecen visibles al hacer scroll (sticky)"
    - "La app tiene un manifest PWA con nombre Fitness Bassi y iconos correctos"
  artifacts:
    - path: "app/manifest.ts"
      provides: "PWA manifest con name, short_name, icons, theme_color, start_url"
      exports: ["default function manifest"]
    - path: "app/(client)/layout.tsx"
      provides: "Header con ThemeToggle añadido"
      contains: "ThemeToggle"
    - path: "components/client/nutrition/FoodSearchModal.tsx"
      provides: "FAB eliminado — trigger convertido a botón inline"
  key_links:
    - from: "app/(client)/layout.tsx"
      to: "ThemeToggle component"
      via: "import from components/ui/theme-toggle"
      pattern: "ThemeToggle"
    - from: "app/(client)/nutrition/page.tsx"
      to: "FoodSearchModal inline trigger"
      via: "prop triggerClassName o similar"
      pattern: "FoodSearchModal"
---

<objective>
Polish de UI para la app cliente: mover el FAB flotante de Registro Libre al interior de su sección, añadir ThemeToggle al layout del cliente, verificar y asegurar sticky headers en páginas de lista, y crear el manifest PWA para que la app aparezca correctamente al añadir a pantalla de inicio.

Purpose: Mejoras de UX visible — el FAB flotante tapa contenido, el toggle de tema falta en la app cliente, y la app no tiene identidad PWA.
Output: FoodSearchModal con trigger inline, ThemeToggle en layout cliente, manifest.ts creado.
</objective>

<execution_context>
@/Users/jofreatanet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jofreatanet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-bug-fixes-logic-corrections/15-RESEARCH.md
</context>

<interfaces>
<!-- Interfaces existentes que el executor necesita -->

De app/(client)/layout.tsx (estado actual):
```tsx
// Header solo tiene logo, sin ThemeToggle
<header
  className="sticky top-0 z-30 flex items-center px-4"
  style={{ height: '44px', background: '#191919', borderBottom: '1px solid rgba(255,255,255,0.06)' }}
>
  <Image src="/2.png" alt="Fitness Bassi" width={28} height={28} ... />
</header>
```

De components/trainer/sidebar.tsx (referencia — ThemeToggle ya funciona aquí):
```tsx
import { ThemeToggle } from "@/components/ui/theme-toggle";
// ...
<div className="px-2 py-2 border-t border-[var(--border)]">
  <ThemeToggle />
</div>
```

De components/client/nutrition/FoodSearchModal.tsx (estado actual — FAB flotante):
```tsx
// El problema: button con position: fixed que flota sobre todo
<button
  onClick={() => setIsOpen(true)}
  className="fixed z-50 flex items-center justify-center rounded-full shadow-xl ..."
  style={{ bottom: 80, right: 16, width: 56, height: 56, background: '#fb8500' }}
>
  <Plus className="w-6 h-6 text-white" strokeWidth={2.5} />
</button>
```

De app/(client)/nutrition/page.tsx (estado actual — FoodSearchModal al final del JSX):
```tsx
// FoodSearchModal está al final, separado de la sección "Registro libre"
<section className="mb-6">
  <div className="flex items-center justify-between mb-3">
    <p className="text-xs font-medium text-[#6b7fa3] tracking-wide uppercase">Registro libre</p>
    <AIFoodParserModal clientId={client.id} />  {/* AI modal inline en el header */}
  </div>
  {/* ... lista de logs libres ... */}
</section>
<FoodSearchModal clientId={client.id} dateStr={currentDateString} />  {/* FAB flotante */}
```

Meta de Next.js App Router:
```typescript
// app/manifest.ts
import type { MetadataRoute } from 'next'
export default function manifest(): MetadataRoute.Manifest { ... }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Añadir ThemeToggle al layout cliente y verificar sticky headers (FEAT-03, FEAT-04)</name>
  <files>app/(client)/layout.tsx</files>
  <action>
    En app/(client)/layout.tsx:

    1. Añadir import: `import { ThemeToggle } from '@/components/ui/theme-toggle'`

    2. Modificar el header para que tenga `justify-between` y añadir ThemeToggle a la derecha:
       ```tsx
       <header
         className="sticky top-0 z-30 flex items-center justify-between px-4"
         style={{ height: '44px', background: '#191919', borderBottom: '1px solid rgba(255,255,255,0.06)' }}
       >
         <Image src="/2.png" alt="Fitness Bassi" width={28} height={28} className="object-contain" style={{ mixBlendMode: 'screen' }} />
         <ThemeToggle />
       </header>
       ```

    FEAT-04 — Sticky headers en páginas de lista:
    El layout del cliente ya tiene `sticky top-0 z-30` en el header global — esto está correcto.
    Los headers de las páginas individuales (today, routines, history, progress, nutrition) son títulos h1 dentro del contenido scrollable — no necesitan ser sticky ya que el header del layout es el que hace sticky y provee la barra superior.
    No se requieren cambios adicionales para FEAT-04 si el header de layout ya funciona como sticky.
    Si al revisar alguna página específica tiene un segundo header que debería ser sticky, añadir `sticky top-[44px] z-20` a ese elemento. Priorizar today/page.tsx e history/page.tsx si tienen headers de sección.
  </action>
  <verify>Build pasa: `cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | grep "layout"` — sin errores en layout.tsx</verify>
  <done>layout.tsx tiene ThemeToggle importado y renderizado en el header a la derecha del logo. El header ya era sticky top-0.</done>
</task>

<task type="auto">
  <name>Task 2: Mover trigger de FoodSearchModal inline a la sección Registro Libre (FEAT-01)</name>
  <files>components/client/nutrition/FoodSearchModal.tsx, app/(client)/nutrition/page.tsx</files>
  <action>
    FEAT-01: El botón + de FoodSearchModal es actualmente un FAB flotante (`position: fixed`) que flota por encima de todo el contenido. Debe convertirse en un botón inline dentro de la sección "Registro libre".

    En components/client/nutrition/FoodSearchModal.tsx:
    1. Añadir una prop `trigger?: React.ReactNode` al componente
    2. Si `trigger` está presente, renderizarlo como el elemento que abre el modal (en lugar del FAB)
    3. Si `trigger` NO está presente (fallback), mantener el FAB por compatibilidad
    4. El modal (sheet bottom) sigue igual — solo cambia el trigger

    Implementación en FoodSearchModal:
    ```tsx
    export function FoodSearchModal({
      clientId,
      dateStr,
      trigger,
    }: {
      clientId: string
      dateStr: string
      trigger?: React.ReactNode
    }) {
      // ...estado existente...
      return (
        <>
          {trigger ? (
            <div onClick={() => setIsOpen(true)} className="cursor-pointer">
              {trigger}
            </div>
          ) : (
            // FAB original como fallback
            <button
              onClick={() => setIsOpen(true)}
              className="fixed z-50 flex items-center justify-center rounded-full shadow-xl transition-transform active:scale-95 hover:brightness-110"
              style={{ bottom: 80, right: 16, width: 56, height: 56, background: '#fb8500' }}
            >
              <Plus className="w-6 h-6 text-white" strokeWidth={2.5} />
            </button>
          )}
          {/* modal sheet existente sin cambios */}
        </>
      )
    }
    ```

    En app/(client)/nutrition/page.tsx:
    1. Mover `<FoodSearchModal>` de su posición al final del JSX a dentro de la sección "Registro libre", junto a AIFoodParserModal
    2. Pasar un `trigger` inline al FoodSearchModal:
       ```tsx
       <section className="mb-6">
         <div className="flex items-center justify-between mb-3">
           <p className="text-xs font-medium text-[#6b7fa3] tracking-wide uppercase">Registro libre</p>
           <div className="flex items-center gap-2">
             <AIFoodParserModal clientId={client.id} />
             <FoodSearchModal
               clientId={client.id}
               dateStr={currentDateString}
               trigger={
                 <button className="w-8 h-8 rounded-full bg-[rgba(255,255,255,0.08)] border border-[rgba(255,255,255,0.1)] flex items-center justify-center text-[#e8e8e6] hover:bg-[rgba(255,255,255,0.12)] transition-colors">
                   <Plus className="w-4 h-4" strokeWidth={2.5} />
                 </button>
               }
             />
           </div>
         </div>
         {/* ... logs libres ... */}
       </section>
       ```
    3. Eliminar el `<FoodSearchModal>` suelto al final del JSX (ya que está ahora dentro de la sección)
    4. Añadir `import { Plus } from 'lucide-react'` a nutrition/page.tsx si no está ya importado
  </action>
  <verify>Build pasa: `cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | grep -E "nutrition|FoodSearch"` — sin errores</verify>
  <done>FoodSearchModal acepta prop trigger. En nutrition/page.tsx el FoodSearchModal está dentro de la sección Registro Libre con un botón inline circular. El FAB flotante ya no existe en la vista de nutrición.</done>
</task>

<task type="auto">
  <name>Task 3: Crear manifest PWA (FEAT-05)</name>
  <files>app/manifest.ts</files>
  <action>
    Crear `app/manifest.ts` usando el API built-in de Next.js App Router (`MetadataRoute.Manifest`).

    ```typescript
    import type { MetadataRoute } from 'next'

    export default function manifest(): MetadataRoute.Manifest {
      return {
        name: 'Fitness Bassi',
        short_name: 'Bassi',
        description: 'Sistema profesional de entrenamiento personalizado',
        icons: [
          {
            src: '/icon-192.png',
            sizes: '192x192',
            type: 'image/png',
            purpose: 'any maskable',
          },
          {
            src: '/icon-512.png',
            sizes: '512x512',
            type: 'image/png',
            purpose: 'any maskable',
          },
        ],
        theme_color: '#111111',
        background_color: '#111111',
        display: 'standalone',
        start_url: '/today',
        scope: '/',
        orientation: 'portrait',
      }
    }
    ```

    NOTA SOBRE ICONOS: Los archivos `/public/icon-192.png` y `/public/icon-512.png` no existen aún. El manifest los referencia, pero sin los PNGs la PWA no mostrará icono personalizado (usará el favicon por defecto). Crear placeholders usando el logo existente `/public/Black and Yellow Square Fitness Logo.png`:
    - Copiar `/public/Black and Yellow Square Fitness Logo.png` a `/public/icon-192.png` y `/public/icon-512.png` si el logo tiene al menos 192px. Si no existe una forma programática de hacerlo, añadir un comentario TODO en el manifest: `// TODO: replace /public/icon-192.png and /public/icon-512.png with proper sized PNGs from design assets`
    - No instalar ninguna librería de procesamiento de imágenes — si no se puede copiar el archivo directamente, dejar el manifest listo y anotar en SUMMARY que los iconos necesitan generarse manualmente.

    El manifest.ts es suficiente para que Next.js sirva `/manifest.json` automáticamente. No es necesario añadir `<link rel="manifest">` en el layout — Next.js lo hace automáticamente al detectar `app/manifest.ts`.
  </action>
  <verify>Build pasa: `cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | grep manifest` — sin errores. Confirmar que `app/manifest.ts` existe con `ls "app/manifest.ts"`</verify>
  <done>app/manifest.ts creado. Next.js lo sirve en /manifest.json. Iconos referenciados (PNG files pueden necesitar generación manual).</done>
</task>

</tasks>

<verification>
Después de ejecutar los 3 tasks:
1. `cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit` — sin errores
2. Build: `npx next build 2>&1 | tail -20` — completa sin errores
3. Verificación en `/manifest.json` en dev: `npx next dev &` luego `curl http://localhost:3000/manifest.json` — debe retornar JSON con name, icons
4. Visual: ThemeToggle visible en header de la app cliente
5. Visual: En /nutrition, el botón + está dentro del recuadro de "Registro libre", no flotando
</verification>

<success_criteria>
- FEAT-01: FoodSearchModal no tiene FAB flotante en /nutrition. Trigger inline dentro de la sección Registro Libre.
- FEAT-03: ThemeToggle importado y visible en app/(client)/layout.tsx header
- FEAT-04: Header de layout cliente es sticky top-0 (ya lo era — confirmado sin cambios adicionales requeridos)
- FEAT-05: app/manifest.ts creado. /manifest.json servido automáticamente por Next.js.
</success_criteria>

<output>
Después de completar, crear `.planning/phases/15-bug-fixes-logic-corrections/15-03-SUMMARY.md`
</output>
