---
phase: 15-bug-fixes-logic-corrections
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(client)/today/page.tsx
  - components/client/exercise-card.tsx
  - components/client/rest-timer.tsx
  - app/(client)/profile/page.tsx
  - app/(client)/profile/actions.ts
  - app/(client)/profile/edit-profile-form.tsx
autonomous: false
requirements:
  - BUG-03
  - FEAT-02
  - FEAT-06

must_haves:
  truths:
    - "El cliente puede pausar y reanudar el temporizador de descanso durante una sesión activa"
    - "El cliente puede editar su nombre completo y URL de foto de perfil desde /profile"
    - "Los cambios de perfil se guardan y reflejan inmediatamente en la página"
    - "El formulario de RoutineBuilder no pierde estado al navegar entre tabs (verificado)"
  artifacts:
    - path: "app/(client)/profile/edit-profile-form.tsx"
      provides: "Formulario editable para full_name y avatar_url"
      exports: ["EditProfileForm"]
    - path: "app/(client)/profile/actions.ts"
      provides: "Server Action updateProfileAction(full_name, avatar_url)"
      exports: ["updateProfileAction"]
    - path: "components/client/exercise-card.tsx"
      provides: "Botón de pausa de timer local (no DB)"
  key_links:
    - from: "app/(client)/profile/edit-profile-form.tsx"
      to: "updateProfileAction"
      via: "llamada directa desde el cliente"
      pattern: "updateProfileAction"
    - from: "updateProfileAction"
      to: "supabase profiles table"
      via: "supabase.from('profiles').update()"
      pattern: "profiles.*update"
---

<objective>
Añadir capacidades de edición de perfil del cliente (full_name + avatar_url), implementar pausa de timer en sesión activa (estado local — sin migración DB), y verificar/confirmar que el RoutineBuilder no pierde estado al navegar entre steps.

Purpose: FEAT-06 es una necesidad básica de personalización. FEAT-02 mejora el UX de entrenamiento activo. BUG-03 es una verificación para confirmar si el bug existe o fue resuelto.
Output: EditProfileForm con Server Action, botón pause en exercise-card, BUG-03 verificado.
</objective>

<execution_context>
@/Users/jofreatanet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jofreatanet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-bug-fixes-logic-corrections/15-RESEARCH.md
</context>

<interfaces>
<!-- Interfaces existentes que el executor necesita -->

De app/(client)/profile/page.tsx (estado actual — solo lectura):
```tsx
// Muestra datos del perfil en modo lectura, sin formulario de edición
// Tiene LogoutButton como único componente interactivo
// Tiene acceso a: profile.full_name, profile.email, profile.avatar_url
// user.id disponible para auth
```

De lib/supabase/types.ts (tabla profiles):
```typescript
// profiles: { id: string; email: string; full_name: string | null; role: 'client' | 'trainer'; avatar_url: string | null }
// UPDATE permissions: el user solo puede actualizar su propio perfil (profile_id = auth.uid())
type Profile = Database['public']['Tables']['profiles']['Row']
```

De app/(client)/today/actions.ts (patrón de Server Action existente):
```typescript
// Patrón: 'use server', createClient(), getUser(), supabase.from(...).update()
export async function finishWorkout(id: string, _formData?: FormData): Promise<void>
export async function saveSetLog(data: {...}): Promise<void>
```

De components/client/exercise-card.tsx (referencia — tiene timer y estado de entrenamiento):
```tsx
'use client'
// Componente que gestiona el estado local de series y timer de descanso
// El timer se controla con estado local — pausa es local también
// Añadir estado: isPaused: boolean y botón toggle
```

Patrón de timer en exercise-card.tsx (estado actual):
```tsx
// RestTimer existe en components/client/rest-timer.tsx — componente global
// exercise-card.tsx llama al timer a través de algún mecanismo (context o event)
// Para FEAT-02: añadir estado local isPaused y modificar el countdown para que no avance si paused
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verificar BUG-03 (RoutineBuilder form state) y añadir pausa de timer (FEAT-02)</name>
  <files>components/client/exercise-card.tsx, components/client/rest-timer.tsx</files>
  <action>
    BUG-03 — Verificación de RoutineBuilder:
    - Leer app/(trainer)/routines-templates/new/page.tsx para confirmar la implementación actual del RoutineBuilder.
    - Verificar que los steps del wizard son tabs (no páginas separadas) y que el estado persiste en un useState del componente padre.
    - Si el bug no existe (estado persiste correctamente), documentar en el SUMMARY que BUG-03 está resuelto sin cambios.
    - Si el bug existe, añadir un TODO comment en el archivo indicando la causa raíz y que requiere fix en una fase posterior (no bloquear este plan).

    FEAT-02 — Pausa de timer en sesión activa (estado local):
    - Leer components/client/exercise-card.tsx y components/client/rest-timer.tsx para entender cómo funciona el timer actualmente.
    - El timer de descanso se gestiona en rest-timer.tsx con un Context o estado global. Añadir soporte de pausa SOLO con estado local en el componente RestTimer.

    En components/client/rest-timer.tsx (si tiene el countdown):
    1. Añadir estado `const [isPaused, setIsPaused] = useState(false)`
    2. En el useEffect del countdown: `if (isPaused) return` antes de decrementar
    3. Añadir botón de pausa/reanudar junto al timer visual:
       ```tsx
       <button
         onClick={() => setIsPaused(p => !p)}
         className="text-xs text-[var(--text-secondary)] hover:text-[var(--text-primary)] flex items-center gap-1 mt-1"
       >
         {isPaused ? <Play className="w-3.5 h-3.5" /> : <Pause className="w-3.5 h-3.5" />}
         {isPaused ? 'Reanudar' : 'Pausar'}
       </button>
       ```
    4. Resetear isPaused a false cuando el timer se reinicia (cuando llega a 0 o cuando se inicia un nuevo descanso)

    Si el timer está en exercise-card.tsx en lugar de rest-timer.tsx, aplicar el mismo patrón allí.
    Importar `Play, Pause` de `lucide-react`.
    NO añadir ningún campo a workout_sessions — solo estado local en React.
  </action>
  <verify>Build pasa: `cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | grep -E "rest-timer|exercise-card"` — sin errores en los archivos relevantes</verify>
  <done>BUG-03 verificado (documentado en SUMMARY). RestTimer o exercise-card tiene botón Pausar/Reanudar que detiene el countdown sin modificar la DB.</done>
</task>

<task type="auto">
  <name>Task 2: Perfil editable del cliente — Server Action + formulario (FEAT-06)</name>
  <files>app/(client)/profile/actions.ts, app/(client)/profile/edit-profile-form.tsx, app/(client)/profile/page.tsx</files>
  <action>
    FEAT-06: El cliente puede editar su nombre completo y URL de foto desde /profile.

    1. Crear `app/(client)/profile/actions.ts`:
    ```typescript
    'use server'
    import { createClient } from '@/lib/supabase/server'
    import { revalidatePath } from 'next/cache'

    export async function updateProfileAction(data: {
      full_name: string
      avatar_url: string | null
    }): Promise<{ success: boolean; error?: string }> {
      const supabase = await createClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) return { success: false, error: 'No autenticado' }

      const { error } = await supabase
        .from('profiles')
        .update({
          full_name: data.full_name.trim() || null,
          avatar_url: data.avatar_url?.trim() || null,
        })
        .eq('id', user.id)

      if (error) return { success: false, error: error.message }
      revalidatePath('/profile')
      return { success: true }
    }
    ```

    2. Crear `app/(client)/profile/edit-profile-form.tsx`:
    - Componente `'use client'` que recibe `initial: { full_name: string | null; avatar_url: string | null }`
    - Estado local para los campos full_name y avatar_url
    - Botón "Editar" que alterna entre modo lectura y modo edición (toggle inline)
    - En modo edición: inputs para nombre y URL de foto, botones "Guardar" y "Cancelar"
    - Al guardar: llamar `updateProfileAction` con `useTransition`, mostrar estado loading
    - En modo lectura: mostrar los valores actuales con el icono User
    - Si avatar_url existe y es una URL válida, mostrar preview de la imagen (small img tag con onError para fallback)

    3. Actualizar `app/(client)/profile/page.tsx`:
    - Importar y usar `EditProfileForm` en la sección de datos del perfil
    - Pasar `initial={{ full_name: profile?.full_name ?? null, avatar_url: profile?.avatar_url ?? null }}`
    - La sección del perfil actualmente muestra nombre como texto estático — reemplazar con EditProfileForm

    Nota: NO crear un bucket de Supabase Storage. El campo avatar_url acepta cualquier URL externa (ej. URL de Gravatar, URL de imagen pública). Esta es la decisión tomada en la investigación para simplificar.
  </action>
  <verify>Build pasa: `cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | grep -E "profile"` — sin errores en los archivos de profile</verify>
  <done>actions.ts exporta updateProfileAction. edit-profile-form.tsx exporta EditProfileForm con toggle edición/lectura. profile/page.tsx usa EditProfileForm con datos reales del perfil.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacion humana — pausa de timer, edicion de perfil, BUG-03</name>
  <what-built>
    - BUG-03: RoutineBuilder state verificado (documentado)
    - FEAT-02: Botón Pausar/Reanudar en el timer de descanso de la app cliente
    - FEAT-06: Formulario editable de perfil en /profile con nombre y URL de foto
  </what-built>
  <how-to-verify>
    1. En la app cliente, iniciar una sesión de entrenamiento (/today). Completar una serie para activar el timer de descanso. Verificar que aparece el botón "Pausar" y que al hacer click el countdown se detiene. Al hacer click en "Reanudar" debe continuar.
    2. En la app cliente, navegar a /profile. Verificar que hay un botón o icono de editar. Al hacer click, aparecen inputs para nombre y URL de foto. Editar el nombre, guardar, y verificar que el nuevo nombre aparece.
    3. En la app trainer, navegar a /routines-templates/new. Añadir un ejercicio a un día. Cambiar al siguiente step. Volver al step anterior. Verificar que el ejercicio añadido sigue visible (BUG-03 verificado).
  </how-to-verify>
  <resume-signal>Escribe "aprobado" si todo funciona, o describe los problemas encontrados</resume-signal>
  <action>Verificación manual de las funcionalidades implementadas en los tasks anteriores.</action>
  <verify>Aprobado por el usuario tras revisión manual de los 3 puntos descritos en how-to-verify.</verify>
  <done>Usuario confirma que pausa de timer, edición de perfil y verificación BUG-03 funcionan correctamente.</done>
</task>

</tasks>

<verification>
Después de ejecutar los tasks automáticos:
1. `cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit` — sin errores
2. Test de pausa: /today → iniciar serie → descanso → botón Pausar visible y funcional
3. Test de perfil: /profile → editar nombre → guardar → nombre actualizado
4. Test BUG-03: /routines-templates/new → añadir día/ejercicio → cambiar steps → estado persiste
</verification>

<success_criteria>
- BUG-03: Documentado como resuelto (o como pendiente con causa raíz si aún existe)
- FEAT-02: Timer de descanso tiene estado isPaused. Botón Pausar/Reanudar visible durante el countdown.
- FEAT-06: updateProfileAction en actions.ts. EditProfileForm en edit-profile-form.tsx. profile/page.tsx usa EditProfileForm.
- Build completo sin errores TypeScript.
</success_criteria>

<output>
Después de completar, crear `.planning/phases/15-bug-fixes-logic-corrections/15-04-SUMMARY.md`
</output>
