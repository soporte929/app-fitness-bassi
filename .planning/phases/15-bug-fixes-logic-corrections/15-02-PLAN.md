---
phase: 15-bug-fixes-logic-corrections
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(trainer)/clients/[id]/assign-plan-button.tsx
  - app/(trainer)/clients/[id]/page.tsx
autonomous: true
requirements:
  - LOGIC-01
  - LOGIC-02

must_haves:
  truths:
    - "El botón en el detalle del cliente dice 'Asignar plan' (no 'Asignar rutina')"
    - "El dropdown de asignación lista los Planes del entrenador (tabla plans), no las rutinas/templates"
    - "Al asignar un plan se llama assignPlanToClientAction (tabla client_plans), no clonePlanToClientAction"
    - "El flujo completo es visible: Rutinas → Planes → Asignación"
    - "La sección 'Plan activo' del cliente muestra el plan correcto desde client_plans"
  artifacts:
    - path: "app/(trainer)/clients/[id]/assign-plan-button.tsx"
      provides: "Nuevo componente que reemplaza AssignRoutineButton — usa plans table y assignPlanToClientAction"
      exports: ["AssignPlanButton"]
    - path: "app/(trainer)/clients/[id]/page.tsx"
      provides: "Página actualizada: query de plans en vez de workout_plans, usa AssignPlanButton"
      contains: "AssignPlanButton"
  key_links:
    - from: "app/(trainer)/clients/[id]/page.tsx"
      to: "supabase plans table"
      via: "query .from('plans').eq('trainer_id', user.id)"
      pattern: "from\\('plans'\\)"
    - from: "AssignPlanButton"
      to: "assignPlanToClientAction"
      via: "import from clients/actions.ts"
      pattern: "assignPlanToClientAction"
    - from: "app/(trainer)/clients/[id]/page.tsx"
      to: "AssignPlanButton"
      via: "replace AssignRoutineButton import"
      pattern: "AssignPlanButton"
---

<objective>
Corregir el flujo de negocio crítico LOGIC-01/LOGIC-02: actualmente el botón "Asignar rutina" en el detalle del cliente clona directamente un `workout_plan` template al cliente (flujo incorrecto). El flujo correcto es Rutinas → Planes → Asignación. El entrenador debe asignar un Plan (de la tabla `plans`) al cliente, no una rutina directamente.

Purpose: Este es el flujo central del entrenador — la confusión entre rutinas y planes afecta la coherencia de todo el módulo de entreno.
Output: Nuevo componente AssignPlanButton que usa la tabla plans y assignPlanToClientAction. Page.tsx actualizado para usar el flujo correcto.
</objective>

<execution_context>
@/Users/jofreatanet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jofreatanet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-bug-fixes-logic-corrections/15-RESEARCH.md
</context>

<interfaces>
<!-- Interfaces existentes que el executor necesita -->

De app/(trainer)/clients/actions.ts (ya existe, no modificar):
```typescript
export async function assignPlanToClientAction(
  planId: string,
  clientId: string
): Promise<{ success: boolean; error?: string }> {
  // Deactivates existing active plan, then inserts into client_plans
  // client_plans: { client_id, plan_id, active: true }
}
```

De app/(trainer)/clients/[id]/assign-routine-button.tsx (reemplazar — solo leer para copiar la UI):
```typescript
type TemplateOption = {
  id: string
  name: string
  description: string | null
  days_per_week: number
  total_exercises: number
}
export function AssignRoutineButton({ clientId, templates }: Props) {
  // Llama clonePlanToClientAction — INCORRECTO
  // UI pattern: botón toggle → overlay + dropdown con opciones
}
```

Query correcta para planes del entrenador (usar en page.tsx):
```typescript
const { data: trainerPlans } = await supabase
  .from('plans')
  .select('id, name, description, phase, level')
  .eq('trainer_id', user.id)
  .eq('active', true)
  .order('created_at', { ascending: false })
```

Tipo PlanOption para AssignPlanButton:
```typescript
type PlanOption = {
  id: string
  name: string
  description: string | null
  phase: string | null
  level: string | null
}
```

De app/(trainer)/plans/[planId]/assign-client-dropdown.tsx (referencia de UI existente):
```tsx
// El dropdown que muestra clientes usa similar pattern UI
// AssignPlanButton debe tener UI similar al AssignRoutineButton original
// pero listando plans y usando assignPlanToClientAction
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Crear AssignPlanButton — nuevo componente con flujo correcto</name>
  <files>app/(trainer)/clients/[id]/assign-plan-button.tsx</files>
  <action>
    Crear nuevo archivo `app/(trainer)/clients/[id]/assign-plan-button.tsx` como componente `'use client'`.

    El componente debe:
    1. Recibir props `{ clientId: string; plans: PlanOption[] }` donde PlanOption = `{ id: string; name: string; description: string | null; phase: string | null; level: string | null }`
    2. Mostrar un botón "Asignar plan" (no "Asignar rutina")
    3. Al hacer click, mostrar un dropdown overlay con la lista de planes disponibles
    4. Para cada plan mostrar: nombre, descripción (si existe), fase/nivel como badge
    5. Al seleccionar un plan, llamar `assignPlanToClientAction(plan.id, clientId)` de `@/app/(trainer)/clients/actions`
    6. Usar `useTransition` para estado pending + `useRouter().refresh()` al completar
    7. Mostrar mensaje de error si falla
    8. Si `plans.length === 0`, mostrar "No hay planes disponibles. Crea un plan en /plans primero."

    UI pattern (copiar de assign-routine-button.tsx pero sin el template clone):
    - Botón trigger: `Button variant="secondary" size="sm"` → texto "Asignar plan"
    - Overlay: `fixed inset-0 z-40 bg-black/40` como backdrop
    - Dropdown: `absolute top-full right-0 mt-2 z-50 min-w-[280px] max-h-[70vh] overflow-y-auto bg-[#2a2a2a] rounded-xl shadow-xl border border-[rgba(255,255,255,0.08)] p-3 space-y-2`
    - Cada opción: card con nombre del plan, descripción, badge de fase, botón "Asignar"

    IMPORTANTE: No importar ni usar `clonePlanToClientAction`. Solo `assignPlanToClientAction`.
  </action>
  <verify>Build pasa: `cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | grep "assign-plan-button"` — sin errores en el archivo nuevo</verify>
  <done>Archivo creado. AssignPlanButton exportado. Llama assignPlanToClientAction (no clonePlanToClientAction). Botón dice "Asignar plan".</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar clients/[id]/page.tsx — usar plans table y AssignPlanButton</name>
  <files>app/(trainer)/clients/[id]/page.tsx</files>
  <action>
    Modificar `app/(trainer)/clients/[id]/page.tsx`:

    1. Reemplazar la query de templates (workout_plans) en el Promise.all:
       ANTES:
       ```typescript
       supabase
         .from('workout_plans')
         .select(`id, name, description, days_per_week, workout_days (exercises (id))`)
         .eq('trainer_id', user.id)
         .eq('is_template', true)
         .eq('active', true)
         .order('created_at', { ascending: false }),
       ```
       DESPUÉS:
       ```typescript
       supabase
         .from('plans')
         .select('id, name, description, phase, level')
         .eq('trainer_id', user.id)
         .eq('active', true)
         .order('created_at', { ascending: false }),
       ```

    2. Actualizar la desestructuración del Promise.all — renombrar `templatesRes` a `plansRes`.

    3. Actualizar el procesamiento de datos:
       ANTES:
       ```typescript
       const templates = ((templatesRes.data ?? []) as unknown as Array<{...}>).map(...)
       ```
       DESPUÉS:
       ```typescript
       type PlanOption = { id: string; name: string; description: string | null; phase: string | null; level: string | null }
       const trainerPlans = (plansRes.data ?? []) as PlanOption[]
       ```

    4. Actualizar el import: reemplazar `import { AssignRoutineButton } from './assign-routine-button'` con `import { AssignPlanButton } from './assign-plan-button'`

    5. En el JSX, reemplazar el uso de `AssignRoutineButton`:
       ANTES: `<AssignRoutineButton clientId={rawClient.id} templates={templates} />`
       DESPUÉS: `<AssignPlanButton clientId={rawClient.id} plans={trainerPlans} />`

    6. NO eliminar el archivo assign-routine-button.tsx (puede ser útil como referencia). Solo dejar de importarlo en page.tsx.

    7. Asegurarse de que la variable `templates` y cualquier referencia a ella en el resto del archivo se actualice a `trainerPlans`. Buscar todas las referencias a `templates` para no dejar ninguna rota.
  </action>
  <verify>Build pasa sin errores: `cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | tail -20`</verify>
  <done>page.tsx importa AssignPlanButton. Query usa tabla plans con fields (id, name, description, phase, level). No hay referencias a templates (workout_plans) en el flujo de asignación. Build sin errores TypeScript.</done>
</task>

</tasks>

<verification>
Después de ejecutar los 2 tasks:
1. `cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit` — sin errores
2. Grep para confirmar que clonePlanToClientAction no se importa en clients/[id]/: `grep -r "clonePlanToClientAction" "app/(trainer)/clients/[id]/"` — sin resultados
3. Grep para confirmar el flujo correcto: `grep -r "assignPlanToClientAction" "app/(trainer)/clients/[id]/"` — debe encontrar el import en assign-plan-button.tsx
4. Verificación visual: navegar a /clients/[id] y confirmar que el botón dice "Asignar plan" y lista planes de la tabla plans
</verification>

<success_criteria>
- LOGIC-01: El flujo de asignación en el UI del detalle del cliente usa planes (tabla plans), no rutinas/templates directamente
- LOGIC-02: Botón dice "Asignar plan". AssignPlanButton llama assignPlanToClientAction que inserta en client_plans. La query en page.tsx es de tabla plans con filtro trainer_id + active
- Build completo sin errores TypeScript en los archivos modificados
</success_criteria>

<output>
Después de completar, crear `.planning/phases/15-bug-fixes-logic-corrections/15-02-SUMMARY.md`
</output>
