---
phase: 20-integration-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(trainer)/clients/[id]/nutrition-actions.ts
  - app/(client)/nutrition/actions.ts
  - app/(client)/nutrition/free-log-actions.ts
  - app/(client)/nutrition/NutritionFreeLogSheet.tsx
  - app/(client)/nutrition/add-meal-fab.tsx
  - app/(client)/nutrition/nutrition-checklist.tsx
autonomous: true
requirements:
  - CNUTR-03
  - V41-05
  - V41-06

must_haves:
  truths:
    - "Tras asignar un plan desde el modal, el cliente ve las comidas con sus alimentos y cantidades en /nutrition"
    - "Los archivos legacy (NutritionFreeLogSheet, add-meal-fab, nutrition-checklist, free-log-actions) no existen en el repo"
    - "actions.ts no exporta createNutritionLogAction ni deleteNutritionLogAction"
  artifacts:
    - path: "app/(trainer)/clients/[id]/nutrition-actions.ts"
      provides: "assignNutritionTemplateToClientAction con clone de meal_plan_items"
      contains: "meal_plan_items"
    - path: "app/(client)/nutrition/actions.ts"
      provides: "sin exports createNutritionLogAction / deleteNutritionLogAction"
  key_links:
    - from: "app/(trainer)/clients/[id]/nutrition-actions.ts"
      to: "meal_plan_items table"
      via: "supabase.from('meal_plan_items').select + insert"
      pattern: "meal_plan_items.*insert"
---

<objective>
Dos correcciones independientes agrupadas por proximidad: (1) el modal de asignación de planes no clona meal_plan_items — el cliente ve cáscaras vacías; (2) eliminar archivos legacy y dead code que ya no se usan.

Purpose: CNUTR-03 (cliente ve alimentos en sus comidas del día) y V41-06 (modal de asignación funcional de extremo a extremo). V41-05 completado con la limpieza de legacy.
Output: meal_plan_items se clonan al asignar un template, 4 archivos legacy eliminados, 2 funciones muertas eliminadas de actions.ts.
</objective>

<execution_context>
@/Users/jofreatanet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jofreatanet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-integration-bug-fixes/20-RESEARCH.md
</context>

<interfaces>
<!-- Contratos relevantes. Extraídos del codebase. -->

From app/(trainer)/clients/[id]/nutrition-actions.ts — assignNutritionTemplateToClientAction (estado actual):
```typescript
// Tras clonar nutrition_plan_meals (lines ~154-181), la función llama:
// revalidatePath(`/clients/${clientId}`) y return { success: true }
// FALTA: clone de meal_plan_items entre el bloque de meals y el return
```

From lib/supabase/types.ts — meal_plan_items Row/Insert:
```typescript
// Row
{
  id: string
  plan_id: string
  meal_number: number
  food_id: string | null
  dish_id: string | null
  option_slot: string | null   // 'A' | 'B' | 'C' | null
  grams: number | null
}
// Insert (subset de Row, id y created_at opcionales)
{
  plan_id: string
  meal_number: number
  food_id?: string | null
  dish_id?: string | null
  option_slot?: string | null
  grams?: number | null
}
```

Patrón de clone ya usado en el mismo archivo para nutrition_plan_meals:
```typescript
const { data: templateMeals } = await supabase
  .from('nutrition_plan_meals' as any)
  .select('name, kcal_per_100g, ...')
  .eq('plan_id', templatePlanId)
  .order('order_index', { ascending: true })

if (templateMeals && (templateMeals as any[]).length > 0) {
  const mealsToInsert = (templateMeals as any[]).map((meal) => ({
    plan_id: newPlanId,
    ...
  }))
  const { error: mealsError } = await supabase
    .from('nutrition_plan_meals' as any)
    .insert(mealsToInsert)
  if (mealsError) return { success: false, error: '...' }
}
```

Archivos a eliminar (confirmado sin imports activos):
- app/(client)/nutrition/free-log-actions.ts
- app/(client)/nutrition/NutritionFreeLogSheet.tsx
- app/(client)/nutrition/add-meal-fab.tsx
- app/(client)/nutrition/nutrition-checklist.tsx

Funciones a eliminar de app/(client)/nutrition/actions.ts:
- createNutritionLogAction (lines 16-49) — escribe a nutrition_logs
- deleteNutritionLogAction (lines 51-67) — body vacío, dead code
- CreateNutritionLogInput type (lines 7-14) — solo usado por createNutritionLogAction
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Clonar meal_plan_items en assignNutritionTemplateToClientAction</name>
  <files>app/(trainer)/clients/[id]/nutrition-actions.ts</files>
  <action>
En `app/(trainer)/clients/[id]/nutrition-actions.ts`, añadir el import del tipo Database si no existe, y añadir el bloque de clone de meal_plan_items.

Localizar el bloque que acaba con:
```typescript
  if (mealsError) {
    return { success: false, error: 'Error clonando comidas: ' + mealsError.message }
  }
}
```
...seguido por el `revalidatePath` y el `return { success: true }`.

Insertar entre ese bloque y el `revalidatePath` el siguiente código:

```typescript
// Clone meal_plan_items from template
const { data: templateItems } = await supabase
  .from('meal_plan_items' as any)
  .select('meal_number, food_id, dish_id, option_slot, grams')
  .eq('plan_id', templatePlanId)

if (templateItems && (templateItems as any[]).length > 0) {
  const itemsToInsert = (templateItems as any[]).map((item) => ({
    plan_id: newPlanId,
    meal_number: item.meal_number,
    food_id: item.food_id ?? null,
    dish_id: item.dish_id ?? null,
    option_slot: item.option_slot ?? null,
    grams: item.grams ?? null,
  }))

  const { error: itemsError } = await supabase
    .from('meal_plan_items' as any)
    .insert(itemsToInsert)

  if (itemsError) {
    return { success: false, error: 'Error clonando items del plan: ' + itemsError.message }
  }
}
```

IMPORTANTE: Usar `templatePlanId` (no `newPlanId`) en el SELECT — clonar FROM template, TO new plan. Nombre de variables explícito para no confundir dirección de clone.
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | grep "nutrition-actions" | head -10 || echo "No TS errors in nutrition-actions.ts"</automated>
  </verify>
  <done>La función assignNutritionTemplateToClientAction contiene bloque de clone de meal_plan_items después del clone de nutrition_plan_meals y antes del revalidatePath. TypeScript sin errores nuevos.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminar archivos legacy y dead code de actions.ts</name>
  <files>app/(client)/nutrition/actions.ts, app/(client)/nutrition/free-log-actions.ts, app/(client)/nutrition/NutritionFreeLogSheet.tsx, app/(client)/nutrition/add-meal-fab.tsx, app/(client)/nutrition/nutrition-checklist.tsx</files>
  <action>
**Paso A — Eliminar archivos legacy (usar Bash rm):**
```bash
rm "app/(client)/nutrition/free-log-actions.ts"
rm "app/(client)/nutrition/NutritionFreeLogSheet.tsx"
rm "app/(client)/nutrition/add-meal-fab.tsx"
rm "app/(client)/nutrition/nutrition-checklist.tsx"
```

Antes de eliminar, verificar que ninguno es importado activamente:
```bash
grep -r "free-log-actions\|NutritionFreeLogSheet\|add-meal-fab\|nutrition-checklist" app/ components/ --include="*.tsx" --include="*.ts" | grep -v "node_modules"
```
Si algún archivo aparece importado (aparte de sí mismo), investigar y desconectar primero.

**Paso B — Limpiar actions.ts:**

En `app/(client)/nutrition/actions.ts`, eliminar:
1. El type `CreateNutritionLogInput` (líneas 7-14)
2. La función `createNutritionLogAction` completa (líneas 16-49)
3. La función `deleteNutritionLogAction` completa (líneas 51-67)

El archivo debe quedar empezando en el tipo `ClientNutritionContextResult` (actualmente línea 69).

Verificar que no hay ningún import de `createNutritionLogAction` o `deleteNutritionLogAction` en otros archivos antes de eliminar:
```bash
grep -r "createNutritionLogAction\|deleteNutritionLogAction" app/ components/ --include="*.tsx" --include="*.ts" | grep -v "actions.ts"
```
Si existe algún import, eliminarlo/actualizarlo primero.

**Paso C — Eliminar tmp_actions_check.ts si existe:**
```bash
rm -f "app/(client)/nutrition/tmp_actions_check.ts"
```
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && ls app/\(client\)/nutrition/ && npx tsc --noEmit 2>&1 | grep -v "node_modules" | grep -i "error" | head -20 || echo "Build OK"</automated>
  </verify>
  <done>Los 4 archivos legacy no existen en el filesystem. actions.ts no exporta createNutritionLogAction ni deleteNutritionLogAction. TypeScript build sin errores nuevos. `ls app/(client)/nutrition/` no muestra free-log-actions.ts, NutritionFreeLogSheet.tsx, add-meal-fab.tsx, nutrition-checklist.tsx.</done>
</task>

</tasks>

<verification>
1. `grep -r "meal_plan_items" app/(trainer)/clients/[id]/nutrition-actions.ts` → aparece (clone step añadido)
2. `ls app/(client)/nutrition/` → no contiene free-log-actions.ts, NutritionFreeLogSheet.tsx, add-meal-fab.tsx, nutrition-checklist.tsx
3. `grep "createNutritionLogAction\|deleteNutritionLogAction" app/(client)/nutrition/actions.ts` → no results
4. `npx tsc --noEmit` → 0 errores nuevos introducidos
5. `grep -r "free-log-actions\|NutritionFreeLogSheet\|add-meal-fab\|nutrition-checklist" app/ components/` → no results
</verification>

<success_criteria>
- assignNutritionTemplateToClientAction clona meal_plan_items con templatePlanId → newPlanId (dirección correcta)
- 4 archivos legacy eliminados del filesystem
- 2 funciones muertas + 1 type eliminados de actions.ts
- TypeScript build sin errores nuevos en ningún archivo del proyecto
</success_criteria>

<output>
Después de completar, crear `.planning/phases/20-integration-bug-fixes/20-02-SUMMARY.md`
</output>
