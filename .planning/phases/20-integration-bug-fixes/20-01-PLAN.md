---
phase: 20-integration-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(client)/nutrition/actions.ts
  - components/client/nutrition/AIFoodParserModal.tsx
  - app/(client)/nutrition/page.tsx
autonomous: true
requirements:
  - AI-03
  - CNUTR-01
  - CNUTR-02
  - CNUTR-05
  - V41-05

must_haves:
  truths:
    - "El cliente confirma macros estimados por IA y el alimento aparece en las barras de progreso del mismo día"
    - "El alimento registrado via IA aparece en la sección Registro libre de la página de nutrición"
    - "El registro manual (paso fallback) también escribe a food_log, no a nutrition_logs"
  artifacts:
    - path: "app/(client)/nutrition/actions.ts"
      provides: "logAIFoodEntryAction — escribe saved_dishes + food_log"
      exports: ["logAIFoodEntryAction"]
    - path: "components/client/nutrition/AIFoodParserModal.tsx"
      provides: "Modal con prop dateStr, handleSave usa logAIFoodEntryAction"
      contains: "logAIFoodEntryAction"
  key_links:
    - from: "components/client/nutrition/AIFoodParserModal.tsx"
      to: "app/(client)/nutrition/actions.ts"
      via: "logAIFoodEntryAction import"
      pattern: "logAIFoodEntryAction"
    - from: "app/(client)/nutrition/actions.ts"
      to: "saved_dishes + food_log"
      via: "supabase.from('saved_dishes').insert → supabase.from('food_log').insert"
      pattern: "saved_dishes.*insert"
    - from: "app/(client)/nutrition/page.tsx"
      to: "components/client/nutrition/AIFoodParserModal.tsx"
      via: "prop dateStr={currentDateString}"
      pattern: "dateStr=\\{currentDateString\\}"
---

<objective>
Fix el AI food parser que escribe en nutrition_logs (tabla legacy) en lugar de food_log. El cliente confirma los macros estimados y el alimento debe aparecer en las barras de progreso del día.

Purpose: AI-03 y CNUTR-01/02/05 están rotos porque AIFoodParserModal llama a createNutritionFreeLogAction que inserta en nutrition_logs, pero getClientNutritionContextAction solo lee food_log.
Output: Nueva acción logAIFoodEntryAction (saved_dishes + food_log), modal rewired con dateStr, page.tsx pasa prop.
</objective>

<execution_context>
@/Users/jofreatanet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jofreatanet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-integration-bug-fixes/20-RESEARCH.md
</context>

<interfaces>
<!-- Tipos y contratos que el ejecutor necesita. Extraídos del codebase. -->

From app/(client)/nutrition/ai-actions.ts:
```typescript
export type MacroEstimate = {
  kcal: number
  protein_g: number
  carbs_g: number
  fat_g: number
  description: string
}
```

From lib/supabase/types.ts — saved_dishes Insert:
```typescript
// saved_dishes.Insert requiere trainer_id: string (NOT NULL, FK a profiles.id)
// Pasar user.id (profile_id del cliente autenticado) — es válido como FK
{
  trainer_id: string      // obligatorio — pasar user.id
  name: string
  kcal_per_100g: number
  protein_per_100g: number
  carbs_per_100g: number
  fat_per_100g: number
  id?: string
  created_at?: string
}
```

From lib/supabase/types.ts — food_log Insert:
```typescript
{
  client_id: string
  logged_date: string     // formato 'YYYY-MM-DD'
  grams: number
  food_id?: string | null
  dish_id?: string | null
  meal_number?: number | null
  id?: string
  created_at?: string
}
```

From app/(client)/nutrition/actions.ts — logFreeFoodAction (patrón a seguir):
```typescript
export async function logFreeFoodAction(
  clientId: string,
  itemId: string,
  type: 'food' | 'dish',
  grams: number,
  dateStr: string
): Promise<{ success: boolean; error?: string }>
// Autentica usuario → verifica ownership cliente → inserta food_log → revalidatePath('/nutrition')
```

From components/client/nutrition/AIFoodParserModal.tsx — firma actual (a cambiar):
```typescript
export function AIFoodParserModal({ clientId }: { clientId: string })
// handleSave llama createNutritionFreeLogAction — INCORRECTO
// page.tsx actualmente: <AIFoodParserModal clientId={client.id} />
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Añadir logAIFoodEntryAction a actions.ts</name>
  <files>app/(client)/nutrition/actions.ts</files>
  <action>
Añadir al final de app/(client)/nutrition/actions.ts (después de generateWeeklyShoppingListAction) la nueva función exportada `logAIFoodEntryAction`.

Lógica:
1. Recibe `(clientId: string, estimate: MacroEstimate, dateStr: string): Promise<{ success: boolean; error?: string }>`
2. Importar `MacroEstimate` from `'./ai-actions'` al inicio del archivo (añadir al bloque de imports existente)
3. Autenticar usuario con `createClient()` → `supabase.auth.getUser()`
4. Verificar ownership del cliente: `.from('clients').select('id').eq('profile_id', user.id).eq('id', clientId).maybeSingle()`
5. Insertar en saved_dishes:
   ```typescript
   const { data: dish, error: dishError } = await supabase
     .from('saved_dishes')
     .insert({
       trainer_id: user.id,  // client's profile_id — válido como FK
       name: estimate.description,
       kcal_per_100g: estimate.kcal,
       protein_per_100g: estimate.protein_g,
       carbs_per_100g: estimate.carbs_g,
       fat_per_100g: estimate.fat_g,
     })
     .select('id')
     .single()
   if (dishError || !dish) return { success: false, error: dishError?.message ?? 'Error creando entrada IA' }
   ```
6. Insertar en food_log con grams=100 (factor=1, así los macros coinciden exactamente con el estimate):
   ```typescript
   const { error: logError } = await supabase.from('food_log').insert({
     client_id: ownClient.id,
     logged_date: dateStr,
     dish_id: dish.id,
     food_id: null,
     grams: 100,
     meal_number: null,
   })
   if (logError) return { success: false, error: logError.message }
   ```
7. `revalidatePath('/nutrition')` y `return { success: true }`

No eliminar las funciones existentes `createNutritionLogAction` y `deleteNutritionLogAction` en este plan (se eliminan en plan 02). Solo añadir.
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | grep -i "actions.ts" | head -20 || echo "No TS errors in actions.ts"</automated>
  </verify>
  <done>logAIFoodEntryAction exportada desde actions.ts, tipado correcto (no `any` en la firma), TypeScript no reporta errores en el archivo</done>
</task>

<task type="auto">
  <name>Task 2: Rewire AIFoodParserModal + pasar dateStr desde page.tsx</name>
  <files>components/client/nutrition/AIFoodParserModal.tsx, app/(client)/nutrition/page.tsx</files>
  <action>
**En components/client/nutrition/AIFoodParserModal.tsx:**

1. Cambiar import: reemplazar
   ```typescript
   import { createNutritionFreeLogAction } from '@/app/(client)/nutrition/free-log-actions'
   ```
   por
   ```typescript
   import { logAIFoodEntryAction } from '@/app/(client)/nutrition/actions'
   import type { MacroEstimate } from '@/app/(client)/nutrition/ai-actions'
   ```
   (MacroEstimate ya está importado desde ai-actions — verificar si ya existe el import y solo añadir logAIFoodEntryAction si es necesario)

2. Cambiar la firma del componente:
   ```typescript
   // ANTES:
   export function AIFoodParserModal({ clientId }: { clientId: string })
   // DESPUÉS:
   export function AIFoodParserModal({ clientId, dateStr }: { clientId: string; dateStr: string })
   ```

3. Cambiar `handleSave` para los dos paths (confirm y fallback):
   ```typescript
   const handleSave = () => {
     startTransition(async () => {
       const macroData: MacroEstimate = {
         kcal: Number(manualKcal) || 0,
         protein_g: Number(manualProtein) || 0,
         carbs_g: Number(manualCarbs) || 0,
         fat_g: Number(manualFat) || 0,
         description: estimate?.description || description || 'Alimento libre',
       }
       await logAIFoodEntryAction(clientId, macroData, dateStr)
       closeModal()
     })
   }
   ```
   Esto cubre tanto el paso confirm (estimate existe, manualKcal puede tener ediciones del usuario) como el paso fallback (estimate es null, usa description como nombre).

**En app/(client)/nutrition/page.tsx:**

4. Cambiar la línea que renderiza AIFoodParserModal (actualmente en ~línea 111):
   ```typescript
   // ANTES:
   <AIFoodParserModal clientId={client.id} />
   // DESPUÉS:
   <AIFoodParserModal clientId={client.id} dateStr={currentDateString} />
   ```
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | grep -E "(AIFoodParserModal|page\.tsx)" | head -20 || echo "No TS errors"</automated>
  </verify>
  <done>AIFoodParserModal acepta prop dateStr, handleSave llama logAIFoodEntryAction en ambos pasos (confirm y fallback), page.tsx pasa currentDateString como dateStr. TypeScript sin errores en los tres archivos.</done>
</task>

</tasks>

<verification>
1. Build sin errores TS: `npx tsc --noEmit` completa sin errores en los archivos modificados
2. `grep -r "createNutritionFreeLogAction" components/client/nutrition/AIFoodParserModal.tsx` → no results (import eliminado)
3. `grep "logAIFoodEntryAction" components/client/nutrition/AIFoodParserModal.tsx` → aparece
4. `grep "dateStr" app/\(client\)/nutrition/page.tsx` → aparece `dateStr={currentDateString}`
5. `grep "saved_dishes" app/\(client\)/nutrition/actions.ts` → aparece en logAIFoodEntryAction
</verification>

<success_criteria>
- logAIFoodEntryAction existe en actions.ts y sigue el patrón de logFreeFoodAction (auth → client verify → insert saved_dishes → insert food_log → revalidate)
- AIFoodParserModal.tsx no importa createNutritionFreeLogAction ni free-log-actions
- page.tsx pasa dateStr al modal
- TypeScript sin errores nuevos en los tres archivos
</success_criteria>

<output>
Después de completar, crear `.planning/phases/20-integration-bug-fixes/20-01-SUMMARY.md`
</output>
