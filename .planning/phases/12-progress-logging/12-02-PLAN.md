---
phase: 12
plan: 2
---

# Plan 12.2: Measurement Logging Modals

## Objective
Enable clients to directly record body weight, body fat %, and tape measurements from the /progress page, utilizing the new unified data action.

## Context
- `.planning/ROADMAP.md` (Phase 12)
- `app/(client)/progress/page.tsx`
- `app/(client)/progress/actions.ts`

## Tasks

<task type="auto">
  <name>Create LogWeightModal Component</name>
  <files>
    - `components/client/progress/LogWeightModal.tsx` (create)
  </files>
  <action>
    - Create a client component with a sliding drawer/modal (using existing `<Drawer>`/`<Sheet>` patterns if possible, or standard custom modal UI).
    - Provide a form with `weight_kg` (required) and `body_fat_pct` (optional).
    - Implement a loading state and submit form to `logClientMeasurementAction(clientId, { weight_kg: w, body_fat_pct: bf })`.
    - Once successful, close the modal seamlessly. Use a generic toast or state response.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Clients have a floating/embedded dialog to supply only their weight metrics.</done>
</task>

<task type="auto">
  <name>Create LogMeasurementsModal Component</name>
  <files>
    - `components/client/progress/LogMeasurementsModal.tsx` (create)
  </files>
  <action>
    - Create an analogous component specifically for: `waist_cm`, `hip_cm`, `chest_cm`, `arm_cm`, `thigh_cm` (all optional).
    - Similar styling, calls `logClientMeasurementAction(clientId, { ...medidas })`, and gracefully closes upon completion.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>A specialized modal gathers standard anatomic tape measurements intuitively.</done>
</task>

<task type="auto">
  <name>Inject Modals into Progress View</name>
  <files>
    - `app/(client)/progress/page.tsx`
  </files>
  <action>
    - Adjust the `<div className="flex items-center justify-between mb-5">` to accommodate action buttons.
    - Render a "Registrar peso" and a "Medidas" button. They can open the corresponding modal.
    - Since modales are strictly client-side, make sure they receive the `client.id` prop and manage their own local `<button onClick={() => setOpen()}>` states internally, keeping the primary page clean.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>User naturally encounters the entry points precisely in the context where progress charts render.</done>
</task>

## Success Criteria
- [ ] Clicking "Registrar peso" opens a form for weight & fat tracking.
- [ ] Clicking "Registrar medidas" opens a form for anatomical details.
- [ ] Submitting either closes the modal and instantly refreshes the parent's data (thanks to `revalidatePath('/progress')`).
