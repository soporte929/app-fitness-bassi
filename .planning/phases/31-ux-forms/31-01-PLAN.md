---
phase: 31-ux-forms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/trainer/routine-builder.tsx
autonomous: false
requirements:
  - SC-1
  - SC-2
  - SC-3

must_haves:
  truths:
    - "El selector de tipo en Step 1 muestra solo el botón 'Plantilla rutina', no 'Plan para cliente'"
    - "El botón 'Plan para cliente' ha sido eliminado del formulario de nueva rutina"
    - "El nav de pasos muestra: 1. Info básica → 2. Ejercicios → 3. Días"
    - "El botón 'Guardar rutina' aparece en el Step 3 (Días), que es el último paso"
    - "Navegando con Siguiente se pasa de Info básica a Ejercicios, y de Ejercicios a Días"
  artifacts:
    - path: "components/trainer/routine-builder.tsx"
      provides: "RoutineBuilder con los tres cambios de UX aplicados"
      contains: "Plantilla rutina"
  key_links:
    - from: "Step nav (línea ~696)"
      to: "Step content blocks"
      via: "currentStep === N"
      pattern: "stepButton.*Ejercicios.*stepButton.*Días"
---

<objective>
Aplicar tres cambios quirúrgicos de UX en `RoutineBuilder`:

1. Renombrar "Template global" → "Plantilla rutina"
2. Eliminar el botón "Plan para cliente" del selector de tipo
3. Reordenar los pasos: Info básica → Ejercicios → Días (actualmente Info → Días → Ejercicios)

Purpose: Eliminar confusión de nomenclatura tras Phase 30 (que bloqueó la asignación directa rutina→cliente) y hacer el flujo de creación más natural (ejercicios antes de agrupar en días).

Output: `components/trainer/routine-builder.tsx` modificado con los tres cambios.
</objective>

<execution_context>
@/Users/jofreatanet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jofreatanet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-ux-forms/31-RESEARCH.md

<interfaces>
<!-- Estructura actual de routine-builder.tsx relevante para este plan -->

Step nav (líneas 695–699):
```tsx
<div className="flex flex-wrap gap-2">
  {stepButton(1, '1. Info básica')}
  {stepButton(2, '2. Días')}
  {stepButton(3, '3. Ejercicios')}
</div>
```

Selector de tipo en Step 1 (líneas 773–807):
```tsx
<div className="grid grid-cols-2 gap-2">
  <button type="button" onClick={() => setMode('template')} ...>
    Template global           {/* ← renombrar */}
  </button>
  <button type="button" onClick={() => setMode('client')} ...>
    Plan para cliente         {/* ← eliminar este botón */}
  </button>
</div>
```

Límites de los bloques de contenido:
- Step 2 (Días): líneas 837–897 → `{currentStep === 2 && !structureLocked && (<> lista de días, botón Añadir día </>)}`
- Step 3 (Ejercicios + Save): líneas 899–989 → `{currentStep === 3 && !structureLocked && (<div> day tabs, ExerciseRow list, Save button </div>)}`

Botón Guardar rutina: líneas 968–987, dentro del bloque Step 3, bajo comentario `{/* Save button — prominent at bottom of step 3 */}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Renombrar "Template global" y eliminar botón "Plan para cliente"</name>
  <files>components/trainer/routine-builder.tsx</files>
  <action>
Hacer dos ediciones en la sección del selector de Tipo (Step 1, líneas ~773–807):

**Cambio A — Renombrar texto:**
Cambiar el texto dentro del primer `<button>` del selector:
```
Template global
```
por:
```
Plantilla rutina
```

**Cambio B — Eliminar segundo botón:**
Eliminar el `<button>` completo que llama `setMode('client')` (el que contiene "Plan para cliente"), incluyendo todas sus clases, atributos y contenido.

**Cambio C — Ajustar grid:**
El contenedor `<div className="grid grid-cols-2 gap-2">` ahora tendrá un único hijo. Cambiar a `<div className="grid grid-cols-1 gap-2">`.

MANTENER intacto el bloque `{state.mode === 'client' && (<Select …>)}` que aparece después — ese bloque sigue siendo válido para cuando el modo venga por searchParams.

MANTENER intacto el mensaje de edición deshabilitada (`isEditing && <p>El tipo y cliente asignado no se pueden cambiar en edición.</p>`).
  </action>
  <verify>
    <automated>grep -n "Plan para cliente" /Users/jofreatanet/Desktop/Fitness\ bassi/app-fitness-bassi/components/trainer/routine-builder.tsx | wc -l</automated>
  </verify>
  <done>
- El texto "Template global" ya no aparece en el archivo
- El texto "Plantilla rutina" aparece en su lugar
- El texto "Plan para cliente" ha desaparecido del archivo
- `grep "Plan para cliente" components/trainer/routine-builder.tsx` devuelve 0 resultados
  </done>
</task>

<task type="auto">
  <name>Tarea 2: Reordenar pasos — step nav y bloques de contenido</name>
  <files>components/trainer/routine-builder.tsx</files>
  <action>
Aplicar el reordenamiento en tres sub-pasos:

**Sub-paso A — Step nav:**
En las líneas ~696–698, cambiar los labels de los stepButtons:
```tsx
// De:
{stepButton(1, '1. Info básica')}
{stepButton(2, '2. Días')}
{stepButton(3, '3. Ejercicios')}

// A:
{stepButton(1, '1. Info básica')}
{stepButton(2, '2. Ejercicios')}
{stepButton(3, '3. Días')}
```

**Sub-paso B — Intercambiar bloques de contenido:**
El bloque que actualmente se renderiza con `currentStep === 2` (contiene la lista de días y el botón "Añadir día") debe pasar a renderizarse con `currentStep === 3`.

El bloque que actualmente se renderiza con `currentStep === 3` (contiene day tabs, ExerciseRow list y el botón "Guardar rutina") debe pasar a renderizarse con `currentStep === 2`.

CRÍTICO: El botón "Guardar rutina" (`<div className="pt-2 border-t border-[var(--border)]">` con el `<button onClick={save}>`) debe quedar dentro del bloque de Días (nuevo Step 3), NO en el bloque de Ejercicios (nuevo Step 2).

Resultado esperado tras el intercambio:
```tsx
{/* STEP 2 — Ejercicios (day tabs + ExerciseRow list, SIN save button) */}
{currentStep === 2 && !structureLocked && (
  <div className="space-y-4">
    {/* scrollable day tabs */}
    {/* exercise list for active day */}
    {/* NO save button aquí */}
  </div>
)}

{/* STEP 3 — Días (lista de días + Añadir día + save button) */}
{currentStep === 3 && !structureLocked && (
  <>
    {/* lista de días con ArrowUp/ArrowDown/Trash2 */}
    <Button onClick={addDay} …>Añadir día</Button>
    {/* Save button va AQUÍ */}
    <div className="pt-2 border-t border-[var(--border)]">
      <button type="button" onClick={save} …>Guardar rutina</button>
    </div>
  </>
)}
```

**Sub-paso C — Verificar comentarios de sección:**
Actualizar los comentarios `{/* ── STEP 2 ──… */}` y `{/* ── STEP 3 ──… */}` y el comentario `{/* Save button — prominent at bottom of step 3 */}` para que reflejen el nuevo contenido de cada bloque.

NO cambiar nada en el footer de navegación (botones Anterior/Siguiente) — la condición `currentStep === 3` para deshabilitar Siguiente sigue siendo correcta ya que el último step sigue siendo 3.
  </action>
  <verify>
    <automated>grep -n "stepButton" /Users/jofreatanet/Desktop/Fitness\ bassi/app-fitness-bassi/components/trainer/routine-builder.tsx</automated>
  </verify>
  <done>
- `grep "stepButton" components/trainer/routine-builder.tsx` muestra los tres labels en orden: '1. Info básica', '2. Ejercicios', '3. Días'
- El bloque `currentStep === 2` contiene day tabs y ExerciseRow (no la lista de días con ArrowUp/ArrowDown)
- El bloque `currentStep === 3` contiene la lista de días y el botón "Guardar rutina"
- El botón "Guardar rutina" aparece dentro del bloque `currentStep === 3`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Los tres cambios de UX en RoutineBuilder:
1. Texto "Template global" → "Plantilla rutina" en el selector de tipo
2. Botón "Plan para cliente" eliminado del Step 1
3. Pasos reordenados: Info básica → Ejercicios → Días (con el botón Guardar en el último paso)
  </what-built>
  <how-to-verify>
1. Arrancar el servidor en local: `npm run dev`
2. Ir a `/routines-templates/new` en el navegador
3. **Step 1 — Verificar SC-1 y SC-2:**
   - Confirmar que el selector de Tipo muestra solo UN botón con el texto "Plantilla rutina"
   - Confirmar que NO hay ningún botón con el texto "Plan para cliente"
4. **Steps 2 y 3 — Verificar SC-3:**
   - Hacer click en "Siguiente" — debe aparecer la pantalla de Ejercicios (day tabs + lista de ejercicios por día)
   - Hacer click en "Siguiente" de nuevo — debe aparecer la pantalla de Días (lista de días con opciones mover/eliminar + botón "Añadir día")
   - Confirmar que el botón "Guardar rutina" aparece en este último paso (Días), no en Ejercicios
5. Hacer click en "Anterior" y confirmar que se vuelve correctamente a Ejercicios, luego a Info básica
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe cualquier problema encontrado</resume-signal>
</task>

</tasks>

<verification>
- `grep "Plan para cliente" components/trainer/routine-builder.tsx` devuelve 0 resultados
- `grep "Template global" components/trainer/routine-builder.tsx` devuelve 0 resultados
- `grep "Plantilla rutina" components/trainer/routine-builder.tsx` devuelve al menos 1 resultado
- `grep -A1 "stepButton(2" components/trainer/routine-builder.tsx` muestra '2. Ejercicios'
- `grep -A1 "stepButton(3" components/trainer/routine-builder.tsx` muestra '3. Días'
- El botón "Guardar rutina" está dentro del bloque `currentStep === 3`
</verification>

<success_criteria>
- SC-1: El botón "Plan para cliente" no existe en `/routines-templates/new` (ninguna mención en el archivo)
- SC-2: El selector de tipo muestra "Plantilla rutina" en lugar de "Template global"
- SC-3: Navegando por el formulario el orden es: 1. Info básica → 2. Ejercicios → 3. Días, con el botón "Guardar rutina" visible en el Step 3
</success_criteria>

<output>
Tras completar la verificación, crear `.planning/phases/31-ux-forms/31-01-SUMMARY.md` con:
- Cambios realizados (los tres cambios de UX)
- Líneas modificadas en routine-builder.tsx
- Resultado de la verificación manual
</output>
