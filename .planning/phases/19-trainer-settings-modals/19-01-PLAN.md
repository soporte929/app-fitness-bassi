---
phase: 19-trainer-settings-modals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(trainer)/clients/[id]/page.tsx
  - app/(trainer)/clients/[id]/nutrition-actions.ts
  - app/(trainer)/clients/[id]/assign-nutrition-plan-modal.tsx
autonomous: true
requirements:
  - V41-06

must_haves:
  truths:
    - "El header de /clients/[id] muestra un botón 'Asignar plan nutricional' junto al de Revisiones"
    - "Al pulsar el botón se abre un Dialog overlay listando las plantillas nutrition_plans del entrenador (is_template=true)"
    - "El Dialog permite seleccionar una plantilla y elegir una fecha de inicio con un input nativo"
    - "Al confirmar, el plan se asigna al cliente (is_template=false, created_at=startDate) y la página se refresca sin navegar"
    - "Si no hay plantillas, el Dialog muestra un empty state con link a /nutrition-plans"
  artifacts:
    - path: "app/(trainer)/clients/[id]/assign-nutrition-plan-modal.tsx"
      provides: "Client Component — Dialog modal con lista de templates + date picker + Server Action call"
      exports: ["AssignNutritionPlanModal"]
    - path: "app/(trainer)/clients/[id]/nutrition-actions.ts"
      provides: "Server Action assignNutritionTemplateToClientAction"
      exports: ["assignNutritionTemplateToClientAction", "SaveNutritionPlanPayload"]
    - path: "app/(trainer)/clients/[id]/page.tsx"
      provides: "Server Component — fetches nutrition templates + renders AssignNutritionPlanModal"
      contains: "AssignNutritionPlanModal"
  key_links:
    - from: "app/(trainer)/clients/[id]/page.tsx"
      to: "nutrition_plans (is_template=true)"
      via: "supabase query in Promise.all"
      pattern: "eq.*is_template.*true"
    - from: "app/(trainer)/clients/[id]/assign-nutrition-plan-modal.tsx"
      to: "assignNutritionTemplateToClientAction"
      via: "useTransition + Server Action call"
      pattern: "startTransition.*assignNutritionTemplateToClientAction"
    - from: "assignNutritionTemplateToClientAction"
      to: "nutrition_plans + nutrition_plan_meals"
      via: "deactivate-then-insert + clone meals"
      pattern: "update.*active.*false.*insert"
---

<objective>
Adds the "Asignar plan nutricional" button to the client detail page header and implements the Dialog modal that lists the trainer's nutrition plan templates, captures a start date, and assigns the selected plan to the client via a Server Action.

Purpose: Replaces the need for the trainer to navigate to /nutrition-plans and manually assign plans — the assignment workflow is now inline in the client detail page.
Output: AssignNutritionPlanModal component + assignNutritionTemplateToClientAction Server Action + updated page.tsx to fetch templates and render the modal.
</objective>

<execution_context>
@/Users/jofreatanet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jofreatanet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-trainer-settings-modals/19-RESEARCH.md

<interfaces>
<!-- Interfaces existentes que el executor necesita. Extraídas del codebase. -->

From app/(trainer)/clients/[id]/page.tsx (Server Component — MODIFICAR):
```typescript
// page.tsx ya tiene este patrón de Promise.all — añadir nutritionTemplatesRes aquí:
const [weightLogsRes, measurementsRes, sessionsRes, activePlanRes, plansRes, activeNutritionPlanRaw] = await Promise.all([...])

// Ya importa AssignPlanButton — añadir AssignNutritionPlanModal al import
import { AssignPlanButton } from './assign-plan-button'

// Header section (línea ~239): área donde van los botones de acción del cliente
<div className="flex gap-2 flex-shrink-0">
  <Link href={`/clients/${id}/revisions`} ...>Revisiones</Link>
  <AssignPlanButton clientId={rawClient.id} plans={trainerPlans} />
  {/* AÑADIR AssignNutritionPlanModal AQUÍ */}
</div>
```

From app/(trainer)/clients/[id]/nutrition-actions.ts (Server Action file — MODIFICAR):
```typescript
'use server'
import { createClient } from '@/lib/supabase/server'
import { revalidatePath } from 'next/cache'

// Tipo existente — mantener
export type SaveNutritionPlanPayload = { clientId: string; planId?: string; ... }
export async function saveNutritionPlanAction(payload: SaveNutritionPlanPayload) { ... }

// AÑADIR nueva función al mismo archivo:
export async function assignNutritionTemplateToClientAction(
  templatePlanId: string,
  clientId: string,
  clientName: string,
  startDate: string
): Promise<{ success: boolean; error?: string }>
```

From app/(trainer)/clients/[id]/edit-nutrition-plan-modal.tsx (patrón UI a replicar):
```typescript
// Patrón overlay — replicar exactamente:
<div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/60 backdrop-blur-sm animate-fade-in">
  <div className="bg-[var(--bg-surface)] border border-[var(--border)] rounded-xl w-full max-w-2xl max-h-[90vh] overflow-y-auto shadow-2xl flex flex-col">
    <div className="sticky top-0 bg-[var(--bg-surface)] z-10 p-5 border-b border-[var(--border)] flex items-center justify-between">
      <h2>...</h2>
      <button onClick={() => setOpen(false)}><X className="w-5 h-5" /></button>
    </div>
    <div className="p-5 space-y-6">...</div>
  </div>
</div>
```

From lib/supabase/types.ts (tipos nutrition_plans):
```typescript
// nutrition_plans Row incluye:
type NutritionTemplate = Pick<
  Database['public']['Tables']['nutrition_plans']['Row'],
  'id' | 'name' | 'kcal_target' | 'protein_target_g' | 'carbs_target_g' | 'fat_target_g' | 'meals_count' | 'diet_type'
>

// nutrition_plan_meals Row incluye:
// name, kcal_per_100g, protein_per_100g, carbs_per_100g, fat_per_100g, default_grams, meal_time, order_index
```

From STATE.md (decisiones relevantes):
- [Phase 10]: start_date stored as created_at in nutrition_plans
- [Phase 10]: Deactivate existing active plans before inserting new one (deactivate-then-insert pattern)
- [Phase 10]: is_template: false cuando se asigna a un cliente
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Action — assignNutritionTemplateToClientAction</name>
  <files>app/(trainer)/clients/[id]/nutrition-actions.ts</files>
  <action>
    Añadir la nueva función `assignNutritionTemplateToClientAction` al final del archivo existente `nutrition-actions.ts`. NO tocar la función `saveNutritionPlanAction` existente.

    La nueva función debe:
    1. Verificar auth con `supabase.auth.getUser()` — return `{ success: false, error: 'No autenticado' }` si no hay user
    2. Fetch la plantilla: `supabase.from('nutrition_plans' as any).select('id, name, trainer_id, kcal_target, protein_target_g, carbs_target_g, fat_target_g, diet_type, meals_count').eq('id', templatePlanId).eq('trainer_id', user.id).eq('is_template', true).single()` — return error si no existe
    3. Desactivar planes activos del cliente: `supabase.from('nutrition_plans' as any).update({ active: false }).eq('client_id', clientId)`
    4. Insert nuevo plan: `is_template: false`, `active: true`, `created_at: new Date(startDate).toISOString()`, copiar name/kcal_target/protein_target_g/carbs_target_g/fat_target_g/diet_type/meals_count del template
    5. Clone comidas: `supabase.from('nutrition_plan_meals' as any).select('name, kcal_per_100g, protein_per_100g, carbs_per_100g, fat_per_100g, default_grams, meal_time, order_index').eq('plan_id', templatePlanId).order('order_index', { ascending: true })` → insert con `plan_id: newPlan.id`
    6. `revalidatePath(`/clients/${clientId}`)` — NO llamar redirect()
    7. Return `{ success: true }`

    Usar `as any` en los from() igual que lo hace `saveNutritionPlanAction` existente (el tipo no está en el schema automático).

    Añadir también la exportación del tipo:
    ```typescript
    export type NutritionTemplate = {
      id: string
      name: string | null
      kcal_target: number | null
      protein_target_g: number | null
      carbs_target_g: number | null
      fat_target_g: number | null
      meals_count: number | null
      diet_type: string | null
    }
    ```
  </action>
  <verify>
    Verificar que el archivo compila sin errores: `cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "nutrition-actions" | head -20`
  </verify>
  <done>
    nutrition-actions.ts exporta `assignNutritionTemplateToClientAction` y `NutritionTemplate`. La función retorna `{ success: boolean; error?: string }` (no redirect). Sin errores de TypeScript en el archivo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Client Component — AssignNutritionPlanModal</name>
  <files>app/(trainer)/clients/[id]/assign-nutrition-plan-modal.tsx</files>
  <action>
    Crear nuevo archivo `assign-nutrition-plan-modal.tsx` en `app/(trainer)/clients/[id]/`.

    Directivas y estructura:
    ```
    'use client'

    import { useState, useTransition } from 'react'
    import { useRouter } from 'next/navigation'
    import { X, Loader2, UtensilsCrossed, ExternalLink } from 'lucide-react'
    import { Button } from '@/components/ui/button'
    import { assignNutritionTemplateToClientAction, NutritionTemplate } from './nutrition-actions'
    ```

    Props interface:
    ```typescript
    interface AssignNutritionPlanModalProps {
      clientId: string
      clientName: string
      templates: NutritionTemplate[]
    }
    ```

    Estado local:
    - `open: boolean` — controla visibilidad del modal
    - `selectedTemplateId: string | null` — plantilla seleccionada
    - `startDate: string` — fecha inicio, default `new Date().toISOString().split('T')[0]`
    - `error: string | null` — mensaje de error
    - `pending, startTransition` — de useTransition

    Comportamiento de `handleAssign()`:
    1. Guard: si no hay `selectedTemplateId`, return
    2. `setError(null)`
    3. `startTransition(async () => { result = await assignNutritionTemplateToClientAction(selectedTemplateId, clientId, clientName, startDate); if (!result.success) { setError(result.error ?? 'Error al asignar'); return }; router.refresh(); setOpen(false) })`

    UI del botón trigger (fuera del overlay):
    ```tsx
    <Button variant="ghost" size="sm" onClick={() => setOpen(true)}
      className="flex items-center gap-1.5 text-sm font-medium px-3 py-2 rounded-xl border transition-colors hover:bg-[var(--bg-overlay)] bg-[var(--bg-elevated)] border-[var(--border)] text-[var(--text-secondary)] hover:text-[var(--text-primary)]">
      <UtensilsCrossed className="w-4 h-4" />
      Asignar plan nutricional
    </Button>
    ```

    UI del overlay (replicar patrón de edit-nutrition-plan-modal.tsx):
    - Overlay: `fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/60 backdrop-blur-sm animate-fade-in`
    - Panel: `bg-[var(--bg-surface)] border border-[var(--border)] rounded-xl w-full max-w-lg max-h-[90vh] overflow-y-auto shadow-2xl flex flex-col`
    - Header sticky con título "Asignar plan nutricional" y botón X
    - Body `p-5 space-y-5`:
      - Si `templates.length === 0`: empty state con mensaje "No tienes plantillas de nutrición. Crea una plantilla primero." y Link a `/nutrition-plans` con icono ExternalLink
      - Si hay templates: lista de opciones (div clickable por cada template) con:
        - Borde azul si seleccionado: `border-[var(--accent)] bg-[var(--accent)]/5` vs `border-[var(--border)] bg-[var(--bg-base)]`
        - Nombre del template en `text-sm font-semibold text-[var(--text-primary)]`
        - Macros en `text-xs text-[var(--text-secondary)]`: `{kcal_target} kcal · P:{protein_target_g}g · C:{carbs_target_g}g · G:{fat_target_g}g`
      - Label + input type="date" para fecha de inicio con estilos CSS vars (ver research para el className exacto)
      - Error message si existe: `text-sm text-[var(--danger)] bg-[var(--danger)]/10 p-3 rounded-md`
    - Footer con Button "Asignar" disabled si `!selectedTemplateId || pending`, con Loader2 si pending

    Exportar: `export function AssignNutritionPlanModal(...)`
  </action>
  <verify>
    Verificar compilación: `cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "assign-nutrition-plan-modal" | head -20`
  </verify>
  <done>
    Archivo creado y exporta `AssignNutritionPlanModal`. El componente acepta `{ clientId, clientName, templates }` como props. Sin errores de TypeScript.
  </done>
</task>

<task type="auto">
  <name>Task 3: Actualizar page.tsx — fetch templates + render modal</name>
  <files>app/(trainer)/clients/[id]/page.tsx</files>
  <action>
    Modificar `app/(trainer)/clients/[id]/page.tsx` para:

    1. Añadir import al inicio:
    ```typescript
    import { AssignNutritionPlanModal } from './assign-nutrition-plan-modal'
    import type { NutritionTemplate } from './nutrition-actions'
    ```

    2. Añadir `nutritionTemplatesRes` a la destructuración del `Promise.all` existente (línea ~44). Añadir como último elemento:
    ```typescript
    supabase
      .from('nutrition_plans' as any)
      .select('id, name, kcal_target, protein_target_g, carbs_target_g, fat_target_g, meals_count, diet_type')
      .eq('trainer_id', user.id)
      .eq('is_template', true)
      .order('created_at', { ascending: false })
    ```

    3. Extraer el resultado del Promise.all y tipar:
    ```typescript
    const nutritionTemplates = (nutritionTemplatesRes.data ?? []) as NutritionTemplate[]
    ```

    4. En el header de la página (sección `<div className="flex gap-2 flex-shrink-0">`), añadir `AssignNutritionPlanModal` junto al botón de Revisiones y `AssignPlanButton`:
    ```tsx
    <div className="flex gap-2 flex-wrap flex-shrink-0">
      <Link href={`/clients/${id}/revisions`} ...>Revisiones</Link>
      <AssignNutritionPlanModal
        clientId={rawClient.id}
        clientName={clientName}
        templates={nutritionTemplates}
      />
      <AssignPlanButton clientId={rawClient.id} plans={trainerPlans} />
    </div>
    ```

    Mantener el `UtensilsCrossed` import si ya no se usa en otro lado, o añadirlo si no está. Verificar que `ArrowLeft, Flame, ChevronRight, ClipboardList` siguen importados.

    NO modificar ninguna otra lógica del archivo.
  </action>
  <verify>
    Compilación completa sin errores en el archivo: `cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "clients/\[id\]" | head -20`
    Y verificar que el archivo contiene AssignNutritionPlanModal: `grep -n "AssignNutritionPlanModal" "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi/app/(trainer)/clients/[id]/page.tsx"`
  </verify>
  <done>
    page.tsx importa y renderiza AssignNutritionPlanModal. La query de templates está en el Promise.all. El header tiene los tres elementos de acción: Revisiones, AssignNutritionPlanModal, AssignPlanButton. Sin errores de TypeScript.
  </done>
</task>

</tasks>

<verification>
Verificación global de la fase:
```bash
cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "error TS" | head -30
```

Verificar archivos creados/modificados:
```bash
ls "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi/app/(trainer)/clients/[id]/"
grep -n "AssignNutritionPlanModal\|assignNutritionTemplateToClientAction" "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi/app/(trainer)/clients/[id]/page.tsx"
```
</verification>

<success_criteria>
1. `assign-nutrition-plan-modal.tsx` existe y exporta `AssignNutritionPlanModal`
2. `nutrition-actions.ts` exporta `assignNutritionTemplateToClientAction` (sin redirect, retorna `{ success: boolean }`)
3. `page.tsx` fetcha templates de nutrition_plans con `is_template=true` y pasa el array al modal
4. El botón "Asignar plan nutricional" aparece en el header del detalle del cliente
5. Sin errores de TypeScript en los tres archivos modificados
</success_criteria>

<output>
Tras completar, crear `.planning/phases/19-trainer-settings-modals/19-01-SUMMARY.md` con:
- Archivos creados/modificados
- Decisiones tomadas (tipos usados, patrón overlay)
- Estado final verificado
</output>
