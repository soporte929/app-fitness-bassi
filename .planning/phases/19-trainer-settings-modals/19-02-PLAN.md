---
phase: 19-trainer-settings-modals
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(trainer)/settings/page.tsx
  - components/trainer/sidebar.tsx
autonomous: true
requirements:
  - V41-07

must_haves:
  truths:
    - "La ruta /settings existe y es accesible para el entrenador sin errores 404"
    - "El sidebar del trainer muestra un link 'Ajustes' que navega a /settings"
    - "La página /settings muestra el nombre y email real del entrenador desde la tabla profiles"
    - "La página /settings incluye el ThemeToggle ya existente en la sección de Apariencia"
    - "La página está envuelta en PageTransition y usa CSS vars exclusivamente"
  artifacts:
    - path: "app/(trainer)/settings/page.tsx"
      provides: "Server Component — Settings Hub con secciones Cuenta y Apariencia"
      exports: ["default SettingsPage"]
    - path: "components/trainer/sidebar.tsx"
      provides: "Sidebar actualizado con Settings item en sección Principal"
      contains: "Ajustes"
  key_links:
    - from: "components/trainer/sidebar.tsx"
      to: "/settings"
      via: "navigation array Principal section"
      pattern: "href.*settings"
    - from: "app/(trainer)/settings/page.tsx"
      to: "profiles"
      via: "supabase query por user.id"
      pattern: "from.*profiles.*eq.*id.*user.id"
---

<objective>
Creates the trainer Settings Hub route at /settings and adds the sidebar navigation entry. The settings page shows the trainer's real account info (name, email from profiles) and the existing ThemeToggle for UI preferences.

Purpose: Provides a central place for the trainer to manage account info and UI preferences — closes the V41-07 requirement.
Output: New page app/(trainer)/settings/page.tsx + sidebar updated with Settings entry.
</objective>

<execution_context>
@/Users/jofreatanet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jofreatanet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-trainer-settings-modals/19-RESEARCH.md

<interfaces>
<!-- Interfaces existentes que el executor necesita. Extraídas del codebase. -->

From components/trainer/sidebar.tsx ('use client' — MODIFICAR):
```typescript
// Navigation array actual — añadir Settings en sección Principal:
const navigation = [
  {
    section: "Principal",
    items: [
      { label: "Dashboard", href: "/dashboard", icon: LayoutDashboard },
      { label: "Clientes", href: "/clients", icon: Users },
      // AÑADIR: { label: "Ajustes", href: "/settings", icon: Settings },
    ],
  },
  {
    section: "Herramientas",
    items: [
      { label: "Rutinas", href: "/routines-templates", icon: ClipboardList },
      { label: "Planes", href: "/plans", icon: Layers },
      { label: "Nutrición", href: "/nutrition-plans", icon: UtensilsCrossed },
      { label: "Ejercicios", href: "/exercises", icon: Dumbbell },
    ],
  },
]

// Imports existentes (añadir Settings):
import { LayoutDashboard, Users, Dumbbell, ClipboardList, Layers, UtensilsCrossed, LogOut, PanelLeftClose, PanelLeftOpen } from 'lucide-react'
// → añadir Settings al destructuring
```

From app/(trainer)/dashboard/page.tsx (patrón Server Component a seguir):
```typescript
// Patrón auth + query:
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) redirect('/login')

const { data: profile } = await supabase
  .from('profiles')
  .select('full_name, email')
  .eq('id', user.id)
  .single()
```

From components/ui/theme-toggle.tsx (componente ya existente a reutilizar):
```typescript
// ThemeToggle ya existe — importar y usar directamente
import { ThemeToggle } from '@/components/ui/theme-toggle'
```

From components/ui/card.tsx (componente UI disponible):
```typescript
import { Card, CardContent, CardHeader } from '@/components/ui/card'
```

Tipografía del design system (para consistencia):
```
Título de página:    text-2xl font-bold text-[var(--text-primary)] tracking-tight
Título de sección:   text-base font-semibold text-[var(--text-primary)]
Label uppercase:     text-xs font-medium text-[var(--text-secondary)] uppercase tracking-wider
Body dato:           text-sm text-[var(--text-secondary)]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear Settings Hub page.tsx</name>
  <files>app/(trainer)/settings/page.tsx</files>
  <action>
    Crear el directorio `app/(trainer)/settings/` y el archivo `page.tsx` como Server Component.

    Estructura completa:
    ```typescript
    import { createClient } from '@/lib/supabase/server'
    import { redirect } from 'next/navigation'
    import { PageTransition } from '@/components/ui/page-transition'
    import { Card, CardContent, CardHeader } from '@/components/ui/card'
    import { ThemeToggle } from '@/components/ui/theme-toggle'
    import { User, Palette } from 'lucide-react'

    export default async function SettingsPage() {
      const supabase = await createClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) redirect('/login')

      const { data: profile } = await supabase
        .from('profiles')
        .select('full_name, email')
        .eq('id', user.id)
        .single()

      return (
        <PageTransition>
          <div className="p-5 lg:p-8 w-full max-w-2xl">
            <h1 className="text-2xl font-bold text-[var(--text-primary)] tracking-tight mb-6">Ajustes</h1>

            <div className="space-y-4">
              {/* Sección: Cuenta */}
              <Card>
                <CardHeader>
                  <div className="flex items-center gap-2">
                    <User className="w-4 h-4 text-[var(--text-secondary)]" />
                    <p className="text-base font-semibold text-[var(--text-primary)]">Cuenta</p>
                  </div>
                </CardHeader>
                <CardContent>
                  <div className="space-y-3">
                    <div>
                      <p className="text-xs font-medium text-[var(--text-secondary)] uppercase tracking-wider mb-1">Nombre</p>
                      <p className="text-sm text-[var(--text-primary)]">{profile?.full_name ?? '—'}</p>
                    </div>
                    <div>
                      <p className="text-xs font-medium text-[var(--text-secondary)] uppercase tracking-wider mb-1">Email</p>
                      <p className="text-sm text-[var(--text-primary)]">{profile?.email ?? user.email ?? '—'}</p>
                    </div>
                    <div>
                      <p className="text-xs font-medium text-[var(--text-secondary)] uppercase tracking-wider mb-1">Rol</p>
                      <p className="text-sm text-[var(--text-primary)]">Entrenador</p>
                    </div>
                  </div>
                </CardContent>
              </Card>

              {/* Sección: Apariencia */}
              <Card>
                <CardHeader>
                  <div className="flex items-center gap-2">
                    <Palette className="w-4 h-4 text-[var(--text-secondary)]" />
                    <p className="text-base font-semibold text-[var(--text-primary)]">Apariencia</p>
                  </div>
                </CardHeader>
                <CardContent>
                  <div className="flex items-center justify-between">
                    <div>
                      <p className="text-sm font-medium text-[var(--text-primary)]">Modo de color</p>
                      <p className="text-xs text-[var(--text-secondary)] mt-0.5">Claro u oscuro según tu preferencia</p>
                    </div>
                    <ThemeToggle />
                  </div>
                </CardContent>
              </Card>
            </div>
          </div>
        </PageTransition>
      )
    }
    ```

    El layout del trainer (`app/(trainer)/layout.tsx`) ya envuelve todas las rutas con el sidebar — no es necesario añadir nada al layout.
  </action>
  <verify>
    Verificar que el archivo existe y compila:
    `ls "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi/app/(trainer)/settings/page.tsx" && cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "settings" | head -10`
  </verify>
  <done>
    app/(trainer)/settings/page.tsx existe. Es Server Component. Fetcha el perfil del trainer desde Supabase. Muestra nombre, email, rol. Incluye ThemeToggle. Sin errores de TypeScript.
  </done>
</task>

<task type="auto">
  <name>Task 2: Añadir entrada Ajustes al sidebar del trainer</name>
  <files>components/trainer/sidebar.tsx</files>
  <action>
    Modificar `components/trainer/sidebar.tsx` para añadir "Ajustes" a la sección Principal del navigation array.

    1. Añadir `Settings` al import de lucide-react (línea ~11 del archivo):
    ```typescript
    import {
      LayoutDashboard,
      Users,
      Dumbbell,
      ClipboardList,
      Layers,
      UtensilsCrossed,
      LogOut,
      PanelLeftClose,
      PanelLeftOpen,
      Settings,
    } from "lucide-react";
    ```

    2. Añadir el item a la sección "Principal" del array `navigation` (línea ~22):
    ```typescript
    const navigation = [
      {
        section: "Principal",
        items: [
          { label: "Dashboard", href: "/dashboard", icon: LayoutDashboard },
          { label: "Clientes", href: "/clients", icon: Users },
          { label: "Ajustes", href: "/settings", icon: Settings },
        ],
      },
      {
        section: "Herramientas",
        // ... sin cambios
      },
    ]
    ```

    NO modificar ninguna otra parte del archivo (lógica de collapsed, backdrop, ThemeToggle, user section).
  </action>
  <verify>
    Verificar el contenido del sidebar:
    `grep -n "Ajustes\|Settings\|settings" "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi/components/trainer/sidebar.tsx"`

    Verificar compilación:
    `cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "sidebar" | head -10`
  </verify>
  <done>
    sidebar.tsx incluye `{ label: "Ajustes", href: "/settings", icon: Settings }` en la sección Principal. El import de `Settings` desde lucide-react está añadido. Sin errores de TypeScript.
  </done>
</task>

</tasks>

<verification>
Verificación global:
```bash
cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "error TS" | head -20
```

Verificar estructura de archivos:
```bash
ls "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi/app/(trainer)/settings/"
grep -c "Ajustes" "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi/components/trainer/sidebar.tsx"
```
</verification>

<success_criteria>
1. `app/(trainer)/settings/page.tsx` existe como Server Component con secciones Cuenta y Apariencia
2. `components/trainer/sidebar.tsx` incluye `{ label: "Ajustes", href: "/settings", icon: Settings }` en sección Principal
3. La página fetcha el perfil real del trainer desde `profiles` (no hardcoded)
4. `ThemeToggle` está integrado en la sección Apariencia
5. Sin errores de TypeScript en ambos archivos
</success_criteria>

<output>
Tras completar, crear `.planning/phases/19-trainer-settings-modals/19-02-SUMMARY.md` con:
- Archivos creados/modificados
- Decisiones tomadas (secciones del Settings Hub, posición en sidebar)
- Estado final verificado
</output>
