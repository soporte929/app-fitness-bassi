---
phase: 28-progress-page-full-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(client)/progress/page.tsx
  - components/trainer/dashboard-charts/phase-distribution-chart.tsx
autonomous: true
requirements:
  - PROG-01
  - PROG-02
  - PROG-03

must_haves:
  truths:
    - "Cuando una query de Supabase falla en /progress, el usuario ve un mensaje de error descriptivo en pantalla (no página en blanco)"
    - "El gráfico PhaseDistributionChart en el dashboard del trainer muestra el pie y la leyenda completos sin recorte en resoluciones estrechas"
    - "Los arrays vacíos en ProgressCharts no producen crash ni pantalla en blanco (comportamiento ya existente — no regresión)"
  artifacts:
    - path: "app/(client)/progress/page.tsx"
      provides: "Error rendering visible cuando measurementsResult.error o sessionsResult.error"
      contains: "return con JSX de error cuando hay fallo de query"
    - path: "components/trainer/dashboard-charts/phase-distribution-chart.tsx"
      provides: "PhaseDistributionChart sin overflowX:hidden y con height 320px"
      contains: "ResponsiveContainer width=\"100%\" height={320}"
  key_links:
    - from: "progress/page.tsx"
      to: "ProgressCharts"
      via: "props weightLogs=[], measurements=[] cuando query error"
      pattern: "measurementsResult\\.error"
    - from: "phase-distribution-chart.tsx"
      to: "ResponsiveContainer"
      via: "wrapper div sin overflowX:hidden"
      pattern: "height=\\{320\\}"
---

<objective>
Corrección de dos bugs de presentación independientes: (1) progress/page.tsx suprime errores de Supabase silenciosamente con solo console.error — hay que renderizar un estado de error visible al usuario. (2) PhaseDistributionChart recorta la leyenda por overflowX:hidden en el wrapper y height insuficiente.

Purpose: Que el usuario vea feedback real cuando hay problemas de datos, y que el gráfico de distribución por fase sea completamente visible.
Output: page.tsx actualizado con early return de error; phase-distribution-chart.tsx con dimensiones corregidas.
</objective>

<execution_context>
@/Users/jofreatanet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jofreatanet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-progress-page-full-fix/28-RESEARCH.md

<interfaces>
<!-- Interfaces clave extraídas de los archivos fuente. El ejecutor no necesita explorar el codebase. -->

Desde app/(client)/progress/page.tsx (estado actual):
```tsx
// Queries actuales — CORRECTO, no cambiar el filtro
const [measurementsResult, sessionsResult] = await Promise.all([
  supabase
    .from('client_measurements')
    .select('measured_at, weight_kg, body_fat_pct, waist_cm, hip_cm, chest_cm, arm_cm, thigh_cm')
    .eq('client_id', client.id)
    .order('measured_at', { ascending: true }),
  supabase
    .from('workout_sessions')
    .select(`id, started_at, finished_at, completed, set_logs!set_logs_session_id_fkey (...)`)
    .eq('client_id', client.id)
    .order('started_at', { ascending: true }),
])

// Error handling ACTUAL (solo console.error — hay que reemplazar):
if (measurementsResult.error) {
  console.error('Measurements query error:', measurementsResult.error)
}
if (sessionsResult.error) {
  console.error('Sessions query error:', sessionsResult.error)
}
```

Desde components/trainer/dashboard-charts/phase-distribution-chart.tsx (estado actual):
```tsx
// ACTUAL — wrapper con overflowX:hidden recorta la leyenda:
<div style={{ width: '100%', overflowX: 'hidden' }}>
  <ResponsiveContainer width="100%" height={280}>
    <PieChart margin={{ top: 0, right: 0, bottom: 0, left: 0 }}>
```

Imports disponibles en progress/page.tsx:
```tsx
import { PageTransition } from '@/components/ui/page-transition'
// CSS vars en uso: var(--text-primary), var(--text-secondary), var(--danger)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Error rendering visible en progress/page.tsx</name>
  <files>app/(client)/progress/page.tsx</files>
  <action>
Reemplazar los dos bloques `if (measurementsResult.error) { console.error(...) }` por un único bloque de early return con JSX de error visible. El bloque debe ir justo después del `await Promise.all([...])` y antes de la transformación de datos.

El early return debe seguir exactamente este patrón (ya probado en nutrition/page.tsx):

```tsx
if (measurementsResult.error || sessionsResult.error) {
  const errorMsg = measurementsResult.error?.message ?? sessionsResult.error?.message
  return (
    <PageTransition>
      <div className="px-4 pt-6 pb-24">
        <h1 className="text-2xl font-bold text-[var(--text-primary)] tracking-tight mb-5">Progreso</h1>
        <div className="rounded-xl border border-[var(--danger)]/20 bg-[var(--danger)]/5 p-4 text-center">
          <p className="text-sm font-medium text-[var(--danger)] mb-1">Error cargando datos</p>
          <p className="text-xs text-[var(--text-secondary)]">{errorMsg}</p>
        </div>
      </div>
    </PageTransition>
  )
}
```

NO cambiar nada más del archivo: las queries `.eq('client_id', client.id)` son correctas. NO cambiar ProgressCharts ni sus props. Solo reemplazar el error handling.

Eliminar los dos `console.error` separados — el early return los reemplaza completamente.
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | grep "progress/page"</automated>
  </verify>
  <done>progress/page.tsx tiene un único early return con JSX de error visible cuando measurementsResult.error o sessionsResult.error son truthy. Los console.error separados han sido eliminados. Sin errores TypeScript en ese archivo.</done>
</task>

<task type="auto">
  <name>Task 2: Corregir recorte de leyenda en PhaseDistributionChart</name>
  <files>components/trainer/dashboard-charts/phase-distribution-chart.tsx</files>
  <action>
En el wrapper `<div>` que rodea `<ResponsiveContainer>`:
1. Eliminar `overflowX: 'hidden'` del `style` del div — es lo que recorta la leyenda
2. Cambiar `height={280}` a `height={320}` en `ResponsiveContainer` — da espacio para pie (cy="42%") + leyenda en bottom
3. Añadir márgenes laterales al PieChart: `margin={{ top: 0, right: 16, bottom: 0, left: 16 }}`

Cambio específico (el div actual tiene `style={{ width: '100%', overflowX: 'hidden' }}`):
- Antes: `<div style={{ width: '100%', overflowX: 'hidden' }}>`
- Después: `<div style={{ width: '100%' }}>`

- Antes: `<ResponsiveContainer width="100%" height={280}>`
- Después: `<ResponsiveContainer width="100%" height={320}>`

- Antes: `<PieChart margin={{ top: 0, right: 0, bottom: 0, left: 0 }}>`
- Después: `<PieChart margin={{ top: 0, right: 16, bottom: 0, left: 16 }}>`

NO cambiar ningún otro aspecto: colores, dataKey, nameKey, Tooltip, Legend formatter, Cell mapping. Solo las tres líneas especificadas.
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | grep "phase-distribution-chart"</automated>
  </verify>
  <done>phase-distribution-chart.tsx tiene div wrapper sin overflowX:hidden, ResponsiveContainer con height={320}, PieChart con margin lateral de 16px. Sin errores TypeScript.</done>
</task>

</tasks>

<verification>
Después de ejecutar ambas tareas:

```bash
cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | tail -5
```

Debe retornar 0 errores TS en los archivos modificados. El build completo puede tener errores pre-existentes (ej: mini-chart.tsx) — ignorar errores pre-existentes no relacionados con esta fase.
</verification>

<success_criteria>
1. progress/page.tsx: early return con JSX de error cuando measurementsResult.error || sessionsResult.error (no console.error silencioso)
2. phase-distribution-chart.tsx: wrapper sin overflowX:hidden, height=320, margins laterales 16px
3. Sin errores TypeScript nuevos introducidos por esta fase
</success_criteria>

<output>
Después de completar, crear `.planning/phases/28-progress-page-full-fix/28-01-SUMMARY.md` con:
- Archivos modificados y qué cambió
- Comportamiento antes/después para cada fix
- Cualquier divergencia del plan
</output>
