---
phase: 47-ajustes-cliente-desactivar-opciones-no-funcionales
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(client)/profile/page.tsx
autonomous: false
requirements:
  - P47-01
  - P47-02

must_haves:
  truths:
    - "Los 5 items de settings (Datos personales, Notificaciones, Unidades de medida, Mis revisiones, Privacidad) aparecen visibles pero con opacity reducida y sin ChevronRight"
    - "Ninguno de los 5 items disabled es navegable — no funciona como Link al hacer tap/clic"
    - "El código de todos los items permanece en el array settingsSections (no se elimina)"
    - "EditProfileForm, LogoutButton y stats siguen funcionando sin cambios"
  artifacts:
    - path: "app/(client)/profile/page.tsx"
      provides: "MenuItem type con disabled?: boolean + lógica de render condicional"
      contains: "disabled?: boolean"
  key_links:
    - from: "settingsSections array"
      to: "render de items"
      via: "item.disabled check antes de decidir Link vs div"
      pattern: "item\\.disabled"
---

<objective>
Deshabilitar visualmente los 5 items de settings en /profile que no tienen funcionalidad implementada en v1: Datos personales, Notificaciones, Unidades de medida, Mis revisiones y Privacidad.

Purpose: El usuario cliente ve opciones que no hacen nada al tocarlas (algunos ni siquiera navegan, otros navegan a páginas dead-end). Deshabilitar estas opciones con un indicador visual claro evita la confusión y comunica que la feature está planificada pero no disponible aún.

Output: app/(client)/profile/page.tsx actualizado con tipo MenuItem extendido, items marcados con disabled: true, y lógica de render que muestra items disabled como divs con opacity-40 y sin ChevronRight.
</objective>

<execution_context>
@/Users/jofreatanet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jofreatanet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-ajustes-cliente-desactivar-opciones-no-funcionales/47-RESEARCH.md

<interfaces>
<!-- Código actual relevante extraído de app/(client)/profile/page.tsx -->
<!-- El executor modifica este archivo directamente — no explorar el codebase -->

Tipo actual (líneas 47-51):
```tsx
type MenuItem = {
  label: string
  icon: React.ElementType
  href?: string
}
```

Array actual settingsSections (líneas 58-74):
```tsx
const settingsSections: SettingsSection[] = [
  {
    title: "Cuenta",
    items: [
      { label: "Datos personales", icon: User },
      { label: "Notificaciones", icon: Bell },
      { label: "Unidades de medida", icon: Ruler },
    ],
  },
  {
    title: "App",
    items: [
      { label: "Mis revisiones", icon: ClipboardList, href: "/revisions" },
      { label: "Privacidad", icon: Shield, href: undefined },
    ],
  },
]
```

Lógica de render actual (líneas 219-246):
```tsx
{section.items.map((item, i) => {
  const Icon = item.icon;
  const inner = (
    <>
      <div className="w-8 h-8 rounded-xl bg-[var(--bg-elevated)] flex items-center justify-center flex-shrink-0">
        <Icon className="w-4 h-4 text-[var(--text-secondary)]" />
      </div>
      <span className="flex-1 text-sm font-medium text-[var(--text-primary)]">
        {item.label}
      </span>
      <ChevronRight className="w-4 h-4 text-[var(--text-muted)]" />
    </>
  );
  const rowClass = `flex items-center gap-3 px-5 min-h-[52px] ${i < section.items.length - 1 ? "border-b border-[var(--border)]" : ""}`;
  return item.href ? (
    <Link key={item.label} href={item.href} className={rowClass}>{inner}</Link>
  ) : (
    <div key={item.label} className={rowClass}>{inner}</div>
  );
})}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender MenuItem y deshabilitar los 5 items no funcionales</name>
  <files>app/(client)/profile/page.tsx</files>
  <action>
Modificar únicamente app/(client)/profile/page.tsx con los siguientes cambios quirúrgicos:

**1. Extender el tipo MenuItem** (línea 47-51) — añadir `disabled?: boolean`:
```tsx
type MenuItem = {
  label: string
  icon: React.ElementType
  href?: string
  disabled?: boolean  // v1: items sin funcionalidad implementada
}
```

**2. Marcar los 5 items como disabled en settingsSections** (líneas 58-74):
```tsx
const settingsSections: SettingsSection[] = [
  {
    title: "Cuenta",
    items: [
      { label: "Datos personales", icon: User, disabled: true },
      { label: "Notificaciones", icon: Bell, disabled: true },
      { label: "Unidades de medida", icon: Ruler, disabled: true },
    ],
  },
  {
    title: "App",
    items: [
      { label: "Mis revisiones", icon: ClipboardList, href: "/revisions", disabled: true },
      { label: "Privacidad", icon: Shield, href: undefined, disabled: true },
    ],
  },
]
```
IMPORTANTE: conservar `href: "/revisions"` en Mis revisiones — el href se guarda como dato para el futuro, aunque no se use en el render disabled.

**3. Actualizar la lógica de render** (líneas 219-246) — añadir rama para items disabled ANTES de la lógica actual:
```tsx
{section.items.map((item, i) => {
  const Icon = item.icon;
  const rowClass = `flex items-center gap-3 px-5 min-h-[52px] ${i < section.items.length - 1 ? "border-b border-[var(--border)]" : ""}`;

  // Items sin funcionalidad en v1: visible pero no interactivo
  if (item.disabled) {
    return (
      <div key={item.label} className={`${rowClass} opacity-40 cursor-default`}>
        <div className="w-8 h-8 rounded-xl bg-[var(--bg-elevated)] flex items-center justify-center flex-shrink-0">
          <Icon className="w-4 h-4 text-[var(--text-secondary)]" />
        </div>
        <span className="flex-1 text-sm font-medium text-[var(--text-primary)]">
          {item.label}
        </span>
        {/* Sin ChevronRight — ausencia del chevron indica que no es navegable */}
      </div>
    );
  }

  const inner = (
    <>
      <div className="w-8 h-8 rounded-xl bg-[var(--bg-elevated)] flex items-center justify-center flex-shrink-0">
        <Icon className="w-4 h-4 text-[var(--text-secondary)]" />
      </div>
      <span className="flex-1 text-sm font-medium text-[var(--text-primary)]">
        {item.label}
      </span>
      <ChevronRight className="w-4 h-4 text-[var(--text-muted)]" />
    </>
  );
  return item.href ? (
    <Link key={item.label} href={item.href} className={rowClass}>{inner}</Link>
  ) : (
    <div key={item.label} className={rowClass}>{inner}</div>
  );
})}
```

NO tocar ningún otro bloque de la página. NO modificar EditProfileForm, LogoutButton, stats cards ni queries de Supabase.
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | grep -E "profile/page" || echo "No TS errors in profile/page.tsx"</automated>
  </verify>
  <done>
- `disabled?: boolean` existe en el tipo MenuItem
- Los 5 items del array tienen `disabled: true`
- El render muestra items disabled como div con `opacity-40 cursor-default` y sin ChevronRight
- Sin errores TypeScript en el archivo
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Items de settings en /profile deshabilitados visualmente: Datos personales, Notificaciones, Unidades de medida, Mis revisiones y Privacidad aparecen con opacity reducida y sin flecha de navegación.
  </what-built>
  <how-to-verify>
    1. Abrir la app como usuario cliente y navegar a /profile
    2. Bajar hasta la sección "Cuenta" — verificar que los 3 items (Datos personales, Notificaciones, Unidades de medida) aparecen con opacity reducida y sin ChevronRight
    3. Bajar hasta la sección "App" — verificar que los 2 items (Mis revisiones, Privacidad) aparecen igual
    4. Intentar tocar/clicar cada uno de los 5 items — confirmar que no navegan a ninguna página
    5. Verificar que EditProfileForm (editar nombre y foto) sigue funcionando
    6. Verificar que el botón de logout sigue funcionando
    7. Verificar que las stats (racha, entrenos, adherencia) siguen mostrando datos
  </how-to-verify>
  <resume-signal>Escribe "approved" si todo funciona, o describe cualquier problema</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compila sin errores en profile/page.tsx
- Los 5 items no funcionales muestran opacity-40 y sin ChevronRight
- Ningún item disabled navega al hacer clic/tap
- El código de los items permanece en el array (no eliminado)
- EditProfileForm, LogoutButton y stats sin cambios
</verification>

<success_criteria>
La página /profile muestra los 5 items de settings con indicador visual de estado disabled (opacity reducida, sin flecha de navegación). Ninguno de los 5 items es navegable. El código de todos los items se conserva en el array settingsSections con disabled: true para facilitar su reactivación futura.
</success_criteria>

<output>
After completion, create `.planning/phases/47-ajustes-cliente-desactivar-opciones-no-funcionales/47-01-SUMMARY.md`
</output>
