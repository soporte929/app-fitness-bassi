---
phase: 23-calc-audit-traceability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CALC-01, CALC-02, CALC-03, CALC-04, CALC-05, PROG-01, PROG-02, PROG-03]

must_haves:
  truths:
    - "CALC-01 through CALC-05 show [x] in REQUIREMENTS.md"
    - "PROG-01, PROG-02, PROG-03 show [x] in REQUIREMENTS.md (already correct — verified, not re-edited)"
    - "A named V41 requirements section exists in REQUIREMENTS.md with [x] definitions for V41-01 through V41-07"
    - "V41-01, V41-02, V41-03 show 'Complete' in the traceability table (Phase 21 completed them)"
    - "Coverage count and Last updated timestamp in REQUIREMENTS.md reflect current state"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Authoritative requirement tracking with accurate checkbox state and full V41 definitions"
      contains: "CALC-01.*\\[x\\]"
  key_links:
    - from: "REQUIREMENTS.md CALC checkboxes"
      to: "lib/calculations/nutrition.ts lines 147-225"
      via: "Manual audit already completed in RESEARCH.md — formulas confirmed matching"
      pattern: "calculateTMB|calculateTDEE|calculateTargetCalories|calculateMacros"
---

<objective>
Close the final documentation gaps in REQUIREMENTS.md: mark CALC-01-05 as implemented (formulas verified in research), add a V41 requirements section with named definitions, correct V41-01/02/03 traceability status from Pending to Complete, and refresh coverage metadata.

Purpose: Phase 23 success criteria require REQUIREMENTS.md to accurately reflect the implementation state — CALC-01-05 are coded and working but still show unchecked, and V41 requirements have no named definitions in the requirements body.
Output: Updated REQUIREMENTS.md with all correct checkbox states, complete V41 section, accurate traceability, and refreshed coverage count.
</objective>

<execution_context>
@/Users/jofreatanet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jofreatanet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-calc-audit-traceability/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark CALC-01-05 as implemented in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    The research phase already confirmed all five formulas are correctly implemented in `lib/calculations/nutrition.ts`. The code evidence is in 23-RESEARCH.md (Audit Findings Summary section). Do NOT re-inspect the source file — use the verified research findings.

    Edit REQUIREMENTS.md lines 17-21 (the "Cálculo Nutricional (CALC)" section):

    Change each `[ ]` to `[x]` for CALC-01 through CALC-05:

    - `- [ ] **CALC-01**:` → `- [x] **CALC-01**:`
    - `- [ ] **CALC-02**:` → `- [x] **CALC-02**:`
    - `- [ ] **CALC-03**:` → `- [x] **CALC-03**:`
    - `- [ ] **CALC-04**:` → `- [x] **CALC-04**:`
    - `- [ ] **CALC-05**:` → `- [x] **CALC-05**:`

    Do NOT change any other checkboxes. PROG-01, PROG-02, PROG-03 are already `[x]` — leave them as-is.

    Code evidence for each (from RESEARCH.md):
    - CALC-01: `calculateTMB()` with `fatPercent !== undefined` → `Math.round(370 + 21.6 * ffm)` — matches `370 + 21.6×FFM`
    - CALC-02: `calculateTMB()` else branch → `10 * weightKg + 6.25 * heightCm - 5 * age` ±5/161 — matches Mifflin-St Jeor
    - CALC-03: `calculateTDEE()` with `ACTIVITY_FACTORS` {sedentary:1.2, light:1.375, moderate:1.55, active:1.725, very_active:1.9} — matches all 5 multipliers
    - CALC-04: `calculateTargetCalories()` with `GOAL_FACTORS` {deficit:0.85, maintenance:1.0, volume:1.075} — matches all three values
    - CALC-05: `calculateMacros()` with PROTEIN_FACTORS {deficit:2.2, recomposition:2.0, volume:1.8} and FAT_FACTORS {deficit:0.8, recomposition:0.9, volume:1.0}, carbos from remainder — matches all per-phase values
  </action>
  <verify>grep -c '\[x\] \*\*CALC' /Users/jofreatanet/Desktop/Fitness\ bassi/app-fitness-bassi/.planning/REQUIREMENTS.md</verify>
  <done>Output is 5 — all five CALC requirements show [x]</done>
</task>

<task type="auto">
  <name>Task 2: Add V41 requirements section + fix traceability + refresh metadata</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Three sub-edits to REQUIREMENTS.md in this task:

    **Sub-edit A — Add V41 requirements section:**
    After the "### AI Nutrition — Claude API (AI)" section and before "### Trainer — Completar (TRN)", insert a new section "### v4.1 Polish & Settings (V41)" with these named definitions (all marked [x] — all are implemented per Phase 21 VERIFICATION.md and Phase 16-19 completion):

    ```markdown
    ### v4.1 Polish & Settings (V41)

    - [x] **V41-01**: En el sidebar del trainer aparece "FITNESS BASSI" usando la fuente Anton con color correcto
    - [x] **V41-02**: Las gráficas del dashboard (Adherencia, Progreso) tienen márgenes correctos sin recortes
    - [x] **V41-03**: `ThemeProvider` de `next-themes` integrado; `ThemeToggle` funcional en app cliente y trainer; preferencia persistida en localStorage sin hydration mismatch
    - [x] **V41-04**: El cliente puede subir foto de perfil; imagen almacenada en bucket `avatars` de Supabase Storage y `avatar_url` en `profiles` actualizado
    - [x] **V41-05**: El checklist nutricional diario en `/nutrition` muestra comidas interactivas con checkbox, macros y gramos
    - [x] **V41-06**: En detalle del cliente, el botón "Asignar plan nutricional" abre un Dialog que lista plantillas y permite asignar con fecha de inicio
    - [x] **V41-07**: Existe la ruta `/settings` (Settings Hub) para el entrenador con entrada en el sidebar
    ```

    Place this section between the AI section and TRN section. Maintain the existing section order.

    **Sub-edit B — Fix V41-01, V41-02, V41-03 traceability status:**
    In the Traceability table, find the rows for V41-01, V41-02, V41-03 and change their Status from "Pending" to "Complete":

    - `| V41-01 | Phase 21 | Pending |` → `| V41-01 | Phase 21 | Complete |`
    - `| V41-02 | Phase 21 | Pending |` → `| V41-02 | Phase 21 | Complete |`
    - `| V41-03 | Phase 21 | Pending |` → `| V41-03 | Phase 21 | Complete |`

    Leave V41-04 through V41-07 as-is (they already show "Complete").

    **Sub-edit C — Update coverage count and Last updated:**
    Find the Coverage block at the bottom of the file (currently ending with the "Last updated" line) and update:
    1. The `Last updated` line: change date from `2026-03-10` to `2026-03-11` and update description to: `Phase 23 complete — CALC-01-05 marked [x] (formulas verified), V41 definitions added, V41-01/02/03 traceability corrected to Complete`
    2. Verify the count line still says "38 total" — adding V41 descriptions does not change the count (they were already counted). If it is correct, leave it. Do not change numbers unless they are wrong.
  </action>
  <verify>grep -c '\[x\] \*\*V41' /Users/jofreatanet/Desktop/Fitness\ bassi/app-fitness-bassi/.planning/REQUIREMENTS.md && grep 'V41-01.*Complete\|V41-02.*Complete\|V41-03.*Complete' /Users/jofreatanet/Desktop/Fitness\ bassi/app-fitness-bassi/.planning/REQUIREMENTS.md | wc -l</verify>
  <done>First command outputs 7 (all V41 definitions present). Second command outputs 3 (V41-01, 02, 03 show Complete in traceability).</done>
</task>

</tasks>

<verification>
After both tasks complete, verify REQUIREMENTS.md integrity:

```bash
# All CALC requirements checked
grep '\[x\] \*\*CALC' .planning/REQUIREMENTS.md | wc -l   # expect 5

# All PROG requirements still checked (not accidentally changed)
grep '\[x\] \*\*PROG' .planning/REQUIREMENTS.md | wc -l   # expect 3

# V41 definitions section exists
grep '\*\*V41-01\*\*' .planning/REQUIREMENTS.md            # expect match

# V41-01/02/03 traceability corrected
grep 'V41-0[123].*Complete' .planning/REQUIREMENTS.md | wc -l  # expect 3

# Last updated timestamp updated
grep '2026-03-11' .planning/REQUIREMENTS.md                # expect match
```
</verification>

<success_criteria>
1. REQUIREMENTS.md has CALC-01 through CALC-05 marked `[x]` (was `[ ]`)
2. REQUIREMENTS.md has a "### v4.1 Polish & Settings (V41)" section with 7 named `[x]` definitions (V41-01 through V41-07)
3. Traceability table rows for V41-01, V41-02, V41-03 show "Complete" (were "Pending")
4. Coverage `Last updated` timestamp updated to 2026-03-11 with accurate description
5. PROG-01, PROG-02, PROG-03 remain `[x]` — unchanged
6. No other content in REQUIREMENTS.md was modified
</success_criteria>

<output>
After completion, create `.planning/phases/23-calc-audit-traceability/23-01-SUMMARY.md` with:
- What was changed in REQUIREMENTS.md (checkboxes updated, section added, traceability corrected)
- Confirmation that CALC-01-05 verified against lib/calculations/nutrition.ts
- Confirmation that PROG-01-03 were already correct
- Final coverage state
</output>
