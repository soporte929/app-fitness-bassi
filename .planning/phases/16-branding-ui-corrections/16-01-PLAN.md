---
phase: 16
plan: 1
wave: 1
---

# Plan 16.1: Branding & Report Margins

## Objective
Añadir la fuente Anton global, incluir el título del trainer en su sidebar, y arreglar los problemas de recortes visuales de los gráficos del dashboard por márgenes negativos.

## Context
- .planning/ROADMAP.md (Phase 16)
- .planning/phases/16-branding-ui-corrections/RESEARCH.md
- app/layout.tsx
- tailwind.config.ts
- components/trainer/sidebar.tsx
- components/trainer/dashboard-charts/adherence-chart.tsx
- components/trainer/dashboard-charts/weight-trend-chart.tsx

## Tasks

<task type="auto">
  <name>Global Font Setup</name>
  <files>
    app/layout.tsx
    tailwind.config.ts
  </files>
  <action>
    - Load the `Anton` font from `next/font/google` in `app/layout.tsx` (weight: "400").
    - In `app/layout.tsx`, define `anton` with `variable: "--font-anton"`, `display: "swap"`, and add it to the `html` container alongside `geistSans` and `geistMono`.
    - Update `tailwind.config.ts` to add `"anton": ["var(--font-anton)"]` to `theme.extend.fontFamily`.
  </action>
  <verify>Check `app/layout.tsx` and `tailwind.config.ts` for Anton definition</verify>
  <done>The font Anton is universally loaded and available via Tailwind CSS helper `font-anton`</done>
</task>

<task type="auto">
  <name>Trainer Branding Sidebar</name>
  <files>components/trainer/sidebar.tsx</files>
  <action>
    - Modify the `TrainerSidebar` around line 100 inside the main logo `div`.
    - Where `(!isMobile || !collapsed)` renders the `<Image>` block, modify it to wrap the image and a new text node inside a `<div className="flex flex-col items-center w-full">`.
    - Add a `<span>` saying "FITNESS BASSI" beneath the image using styles `font-anton text-[#F5C518] text-xl tracking-wide mt-1 text-center w-full`.
    - Ensure it hides cleanly when sidebar is collapsed on desktop, and displays correctly on collapsed/expanded views as required.
  </action>
  <verify>Check `components/trainer/sidebar.tsx`</verify>
  <done>Title FITNESS BASSI is visibly added under the logo with the Anton font and adapts correctly in collapsed/expanded states</done>
</task>

<task type="auto">
  <name>Fix Dashboard Chart Margins</name>
  <files>
    components/trainer/dashboard-charts/adherence-chart.tsx
    components/trainer/dashboard-charts/weight-trend-chart.tsx
  </files>
  <action>
    - In `adherence-chart.tsx`, change `margin={{ top: 8, right: 8, left: -16, bottom: 60 }}` in `<BarChart>` to `margin={{ top: 8, right: 16, left: 0, bottom: 60 }}`.
    - In `weight-trend-chart.tsx`, change `margin={{ top: 8, right: 8, left: -16, bottom: 28 }}` in `<LineChart>` to `margin={{ top: 8, right: 16, left: 0, bottom: 28 }}`.
    - Double check that wrappers use `w-full` instead of fixed pixel widths for responsiveness across 375px, 768px, and desktop views.
  </action>
  <verify>Verify code changes avoiding fixed widths in charts</verify>
  <done>Charts use `w-full`, contain no fixed widths breaking responsiveness, and no longer use negative left margins</done>
</task>

## Success Criteria
- [ ] The Anton font is accessible throughout the application as a utility class `font-anton`.
- [ ] Trainer sidebar displays the text "FITNESS BASSI" in the Anton font directly below the main logo and is correctly responsive in collapsed/expanded states.
- [ ] The dashboard charts (`AdherenceChart` and `WeightTrendChart`) are responsive across mobile (375px), tablet (768px) and desktop avoiding fixed widths, and negative cut-offs are eliminated.
