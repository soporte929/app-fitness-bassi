---
phase: 08-database-foundation-formulas
plan: "03"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - scripts/seed-foods.ts
autonomous: true
requirements:
  - INFRA-02

must_haves:
  truths:
    - "La tabla `foods` contiene exactamente los 13 alimentos base del seed (pollo, huevos, atún, ternera, salmón, arroz, pasta, patata, avena, pan, aceite oliva, aguacate, frutos secos)"
    - "El script de seed puede ejecutarse con `npx tsx scripts/seed-foods.ts` sin errores"
    - "Los 13 alimentos son consultables desde cualquier componente con `supabase.from('foods').select(...)`"
  artifacts:
    - path: "scripts/seed-foods.ts"
      provides: "Script de seed idempotente para los 13 alimentos base"
      contains: "pollo, huevos, atún, ternera, salmón, arroz, pasta, patata, avena, pan, aceite oliva, aguacate, frutos secos"
  key_links:
    - from: "scripts/seed-foods.ts"
      to: "supabase foods table"
      via: "createClient (service role) → upsert"
      pattern: "from.*foods.*upsert"
---

<objective>
Crear un script de seed idempotente que inserte los 13 alimentos base en la tabla `foods` usando el cliente de Supabase con service role key (para bypass de RLS). El script usa `upsert` con `onConflict: 'name'` para ser seguro de ejecutar múltiples veces.

Purpose: Phase 11 (client nutrition view) y Phase 9 (trainer plan form) necesitan alimentos disponibles en la BD para mostrar comidas. Sin seed, las pantallas estarían vacías.
Output: `scripts/seed-foods.ts` ejecutable con `npx tsx scripts/seed-foods.ts`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/nutrition-specs.md
@.planning/milestones/v4.0-phases/08-database-foundation-formulas/08-01-SUMMARY.md

<interfaces>
<!-- Tipo Food de lib/supabase/types.ts (añadido en Plan 01) -->
```typescript
type FoodInsert = Database["public"]["Tables"]["foods"]["Insert"]
// {
//   id?: string;
//   name: string;
//   category: string;
//   kcal_per_100g: number;
//   protein_per_100g: number;
//   carbs_per_100g: number;
//   fat_per_100g: number;
//   created_at?: string;
// }
```

Variables de entorno necesarias (ya existen en .env.local):
- NEXT_PUBLIC_SUPABASE_URL
- SUPABASE_SERVICE_ROLE_KEY  (para bypass RLS — igual que se usa en clients actions)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear scripts/seed-foods.ts con los 13 alimentos base</name>
  <files>scripts/seed-foods.ts</files>
  <action>
Crear el directorio `scripts/` si no existe y escribir `scripts/seed-foods.ts`.

El script debe:
1. Importar `createClient` de `@supabase/supabase-js` directamente (no el helper de app — este es un script standalone)
2. Crear cliente con service role key para bypass RLS
3. Definir el array de 13 alimentos con macros realistas por 100g
4. Usar `upsert` con `onConflict: 'name'` (idempotente — se puede re-ejecutar)
5. Loggear cuántos alimentos se insertaron/actualizaron

**Valores nutricionales por 100g (fuente: tablas BEDCA / FoodData Central estándar):**

| Alimento | Categoría | kcal | Proteína | Carbs | Grasas |
|----------|-----------|------|----------|-------|--------|
| Pollo (pechuga) | proteína | 165 | 31 | 0 | 3.6 |
| Huevos | proteína | 155 | 13 | 1.1 | 11 |
| Atún (en lata, escurrido) | proteína | 132 | 29 | 0 | 1.0 |
| Ternera (magra) | proteína | 143 | 26 | 0 | 4.0 |
| Salmón | proteína | 208 | 20 | 0 | 13 |
| Arroz (cocido) | carbohidrato | 130 | 2.7 | 28 | 0.3 |
| Pasta (cocida) | carbohidrato | 131 | 5 | 25 | 1.1 |
| Patata (cocida) | carbohidrato | 87 | 2 | 20 | 0.1 |
| Avena | carbohidrato | 389 | 17 | 66 | 7 |
| Pan integral | carbohidrato | 247 | 9 | 41 | 3.5 |
| Aceite de oliva | grasa | 884 | 0 | 0 | 100 |
| Aguacate | grasa | 160 | 2 | 9 | 15 |
| Frutos secos (mezcla) | grasa | 607 | 20 | 13 | 54 |

Nombres exactos a usar en el campo `name` (para consistencia con la UI):
"Pollo (pechuga)", "Huevos", "Atún (lata)", "Ternera (magra)", "Salmón", "Arroz (cocido)", "Pasta (cocida)", "Patata (cocida)", "Avena", "Pan integral", "Aceite de oliva", "Aguacate", "Frutos secos"

**Estructura del script:**

```typescript
import { createClient } from '@supabase/supabase-js'
import type { Database } from '../lib/supabase/types'

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!
const serviceKey = process.env.SUPABASE_SERVICE_ROLE_KEY!

if (!supabaseUrl || !serviceKey) {
  console.error('Missing env vars: NEXT_PUBLIC_SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY required')
  process.exit(1)
}

const supabase = createClient<Database>(supabaseUrl, serviceKey)

type FoodInsert = Database['public']['Tables']['foods']['Insert']

const foods: FoodInsert[] = [
  // ... los 13 alimentos
]

async function seedFoods() {
  console.log(`Seeding ${foods.length} foods...`)

  const { data, error } = await supabase
    .from('foods')
    .upsert(foods, { onConflict: 'name', ignoreDuplicates: false })
    .select('id, name')

  if (error) {
    console.error('Seed error:', error.message)
    process.exit(1)
  }

  console.log(`Done. ${data?.length ?? 0} rows upserted.`)
  data?.forEach(f => console.log(` - ${f.name}`))
}

seedFoods()
```

El script carga las variables de entorno desde `.env.local` usando dotenv si está instalado, o bien las lee directamente de `process.env`. Comprobar si dotenv está disponible: si `@/` no funciona en scripts standalone, importar con ruta relativa `../lib/supabase/types`.

Para cargar .env.local sin dotenv, añadir al inicio:
```typescript
// Cargar variables de entorno manualmente para scripts standalone
import { config } from 'dotenv'
config({ path: '.env.local' })
```

Si dotenv no está disponible como dependencia, usar solo `process.env` y documentar que hay que pasar las vars como `NEXT_PUBLIC_SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... npx tsx scripts/seed-foods.ts`.

Verificar si dotenv está en package.json antes de importarlo. Si no está, el script documenta el comando completo.
  </action>
  <verify>
    <automated>test -f "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi/scripts/seed-foods.ts" && grep -c "Pollo\|Huevos\|Atún\|Salmón\|Aguacate" "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi/scripts/seed-foods.ts"</automated>
  </verify>
  <done>
- `scripts/seed-foods.ts` existe y contiene los 13 alimentos con macros correctos
- El script usa upsert con onConflict: 'name' (idempotente)
- Usa service role key para bypass RLS
- `npx tsc --noEmit` no reporta errores en el script (puede requerir ajuste de tsconfig paths)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Script de seed para los 13 alimentos base del módulo nutrición</what-built>
  <how-to-verify>
1. Ejecutar el script desde la raíz del proyecto:
   ```
   NEXT_PUBLIC_SUPABASE_URL=tu_url SUPABASE_SERVICE_ROLE_KEY=tu_key npx tsx scripts/seed-foods.ts
   ```
   O si tienes .env.local con las variables:
   ```
   npx tsx scripts/seed-foods.ts
   ```
2. El output debe mostrar "Done. 13 rows upserted." con los 13 nombres de alimentos
3. Verificar en Supabase Dashboard → Table Editor → foods que aparecen los 13 alimentos
4. Ejecutar el script una segunda vez y confirmar que no duplica (upsert idempotente)
  </how-to-verify>
  <resume-signal>Escribe "seed completado" cuando los 13 alimentos estén en la tabla foods</resume-signal>
</task>

</tasks>

<verification>
- `scripts/seed-foods.ts` existe con los 13 alimentos y sus macros
- Tabla `foods` en Supabase contiene 13 filas con nombres y valores nutricionales correctos
- El script es idempotente (segunda ejecución no duplica filas)
- Categorías asignadas: "proteína", "carbohidrato", "grasa"
</verification>

<success_criteria>
- `supabase.from('foods').select('id, name, kcal_per_100g, protein_per_100g, carbs_per_100g, fat_per_100g')` retorna 13 filas
- Los nombres de alimentos coinciden con: Pollo (pechuga), Huevos, Atún (lata), Ternera (magra), Salmón, Arroz (cocido), Pasta (cocida), Patata (cocida), Avena, Pan integral, Aceite de oliva, Aguacate, Frutos secos
- Las fases de nutrición posteriores pueden consultar alimentos con type safety total
</success_criteria>

<output>
After completion, create `.planning/milestones/v4.0-phases/08-database-foundation-formulas/08-03-SUMMARY.md`
</output>
