---
phase: 08-database-foundation-formulas
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/migrations/08-nutrition-tables.sql
  - lib/supabase/types.ts
autonomous: false
requirements:
  - INFRA-01

must_haves:
  truths:
    - "Un desarrollador puede importar `foods`, `food_equivalences`, `saved_dishes`, `meal_plan_items`, `food_log` y `client_measurements` desde `lib/supabase/types.ts` con tipos completos"
    - "No existe ningún `as any` workaround en los tipos de las 6 nuevas tablas"
    - "El archivo SQL de migración existe y puede ejecutarse en Supabase SQL Editor sin errores"
  artifacts:
    - path: ".planning/migrations/08-nutrition-tables.sql"
      provides: "SQL CREATE TABLE statements para las 6 tablas nuevas con RLS policies"
    - path: "lib/supabase/types.ts"
      provides: "TypeScript types para foods, food_equivalences, saved_dishes, meal_plan_items, food_log, client_measurements"
      contains: "foods, food_equivalences, saved_dishes, meal_plan_items, food_log, client_measurements"
  key_links:
    - from: "lib/supabase/types.ts"
      to: "supabase public schema"
      via: "Database interface tables"
      pattern: "foods.*Row.*Insert.*Relationships"
---

<objective>
Crear el archivo SQL de migración para las 6 nuevas tablas del módulo nutrición y añadir sus tipos completos a `lib/supabase/types.ts`. El usuario ejecuta el SQL en Supabase, y las fases posteriores disponen de type safety total sin workarounds.

Purpose: Todas las fases del módulo nutrición (9-13) dependen de que estas tablas y sus tipos existan. Este plan establece esa fundación.
Output: Archivo `.planning/migrations/08-nutrition-tables.sql` + tipos en `lib/supabase/types.ts`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/nutrition-specs.md

<interfaces>
<!-- Tipos actuales relevantes de lib/supabase/types.ts para referencia -->
<!-- El executor debe seguir el mismo patrón: Row + Insert + Update + Relationships -->

Patrón de tabla existente:
```typescript
// Cada tabla tiene: Row, Insert, Update (= Partial<Insert>), Relationships: []
clients: {
  Row: { id: string; ... };
  Insert: { ... };
  Update: Partial<Database["public"]["Tables"]["clients"]["Insert"]>;
  Relationships: [
    {
      foreignKeyName: "clients_profile_id_fkey";
      columns: ["profile_id"];
      isOneToOne: true;
      referencedRelation: "profiles";
      referencedColumns: ["id"];
    }
  ];
};
```

Tipos enum existentes disponibles (no redefinir):
```typescript
export type ActivityLevel = "sedentary" | "light" | "moderate" | "active" | "very_active";
export type Phase = "recomposition" | "deficit" | "volume" | "maintenance" | "surplus";
export type Goal = "deficit" | "maintenance" | "surplus";
```

CRÍTICO — pattern de Views/Functions (NO usar Record<string, never>):
```typescript
Views: { [_ in never]: never };
Functions: { ... };  // solo las funciones existentes, no tocar
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear SQL de migración para las 6 tablas</name>
  <files>.planning/migrations/08-nutrition-tables.sql</files>
  <action>
Crear el directorio `.planning/migrations/` si no existe, luego escribir el archivo `08-nutrition-tables.sql` con los CREATE TABLE completos.

Las 6 tablas a crear son:

**foods** — catálogo de alimentos base:
```sql
CREATE TABLE IF NOT EXISTS foods (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  category text NOT NULL,
  kcal_per_100g numeric NOT NULL,
  protein_per_100g numeric NOT NULL,
  carbs_per_100g numeric NOT NULL,
  fat_per_100g numeric NOT NULL,
  created_at timestamptz NOT NULL DEFAULT now()
);
```

**food_equivalences** — alimentos intercambiables (ej: pollo ↔ atún):
```sql
CREATE TABLE IF NOT EXISTS food_equivalences (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  food_id uuid NOT NULL REFERENCES foods(id) ON DELETE CASCADE,
  equivalent_food_id uuid NOT NULL REFERENCES foods(id) ON DELETE CASCADE,
  factor numeric NOT NULL DEFAULT 1.0,
  created_at timestamptz NOT NULL DEFAULT now()
);
```

**saved_dishes** — platos compuestos guardados por el entrenador (suma de macros pre-calculada):
```sql
CREATE TABLE IF NOT EXISTS saved_dishes (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  trainer_id uuid NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  name text NOT NULL,
  kcal_per_100g numeric NOT NULL,
  protein_per_100g numeric NOT NULL,
  carbs_per_100g numeric NOT NULL,
  fat_per_100g numeric NOT NULL,
  created_at timestamptz NOT NULL DEFAULT now()
);
```

**meal_plan_items** — items de comidas en planes nutricionales (vincula plan con alimentos/platos):
```sql
CREATE TABLE IF NOT EXISTS meal_plan_items (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  plan_id uuid NOT NULL REFERENCES nutrition_plans(id) ON DELETE CASCADE,
  meal_number integer NOT NULL,
  food_id uuid REFERENCES foods(id),
  dish_id uuid REFERENCES saved_dishes(id),
  option_slot text,
  grams numeric NOT NULL DEFAULT 100,
  created_at timestamptz NOT NULL DEFAULT now(),
  CONSTRAINT meal_plan_items_food_or_dish CHECK (
    (food_id IS NOT NULL AND dish_id IS NULL) OR
    (food_id IS NULL AND dish_id IS NOT NULL)
  )
);
```

**food_log** — registro diario del cliente (lo que come cada día):
```sql
CREATE TABLE IF NOT EXISTS food_log (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  client_id uuid NOT NULL REFERENCES clients(id) ON DELETE CASCADE,
  food_id uuid REFERENCES foods(id),
  dish_id uuid REFERENCES saved_dishes(id),
  logged_date date NOT NULL DEFAULT CURRENT_DATE,
  grams numeric NOT NULL DEFAULT 100,
  meal_number integer,
  created_at timestamptz NOT NULL DEFAULT now()
);
```

**client_measurements** — medidas corporales del cliente por fecha:
```sql
CREATE TABLE IF NOT EXISTS client_measurements (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  client_id uuid NOT NULL REFERENCES clients(id) ON DELETE CASCADE,
  measured_at date NOT NULL DEFAULT CURRENT_DATE,
  weight_kg numeric,
  body_fat_pct numeric,
  waist_cm numeric,
  hip_cm numeric,
  chest_cm numeric,
  arm_cm numeric,
  thigh_cm numeric,
  notes text,
  created_at timestamptz NOT NULL DEFAULT now()
);
```

Añadir también al final del archivo:
1. Habilitar RLS en todas las tablas: `ALTER TABLE foods ENABLE ROW LEVEL SECURITY;` etc.
2. Policy pública de lectura para `foods`: `CREATE POLICY "foods_read_all" ON foods FOR SELECT USING (true);`
3. Comentario claro con la fecha y fase: `-- Migración Phase 8: Módulo Nutrición -- 2026-03-09`

Nota: `nutrition_plans` ya existe en la BD — `meal_plan_items` hace referencia a ella. No recrear `nutrition_plans`.
  </action>
  <verify>
    <automated>test -f "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi/.planning/migrations/08-nutrition-tables.sql" && grep -c "CREATE TABLE" "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi/.planning/migrations/08-nutrition-tables.sql"</automated>
  </verify>
  <done>El archivo `.planning/migrations/08-nutrition-tables.sql` existe y contiene exactamente 6 bloques CREATE TABLE (foods, food_equivalences, saved_dishes, meal_plan_items, food_log, client_measurements)</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Archivo SQL con las 6 nuevas tablas del módulo nutrición</what-built>
  <how-to-verify>
1. Abre Supabase Dashboard → SQL Editor
2. Copia el contenido de `.planning/migrations/08-nutrition-tables.sql`
3. Pega y ejecuta en el SQL Editor
4. Verifica que no hay errores en la consola
5. Ve a Table Editor y confirma que aparecen: foods, food_equivalences, saved_dishes, meal_plan_items, food_log, client_measurements
  </how-to-verify>
  <resume-signal>Escribe "migración ejecutada" cuando las 6 tablas estén creadas en Supabase</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Añadir tipos TypeScript para las 6 tablas en lib/supabase/types.ts</name>
  <files>lib/supabase/types.ts</files>
  <action>
Insertar las 6 nuevas definiciones de tabla dentro del objeto `Tables` en `lib/supabase/types.ts`, justo antes del cierre de `Tables` (antes de la línea que cierra `};` de revision_photos). Seguir exactamente el patrón existente: Row + Insert + Update (Partial de Insert) + Relationships.

**foods**:
```typescript
foods: {
  Row: {
    id: string;
    name: string;
    category: string;
    kcal_per_100g: number;
    protein_per_100g: number;
    carbs_per_100g: number;
    fat_per_100g: number;
    created_at: string;
  };
  Insert: {
    id?: string;
    name: string;
    category: string;
    kcal_per_100g: number;
    protein_per_100g: number;
    carbs_per_100g: number;
    fat_per_100g: number;
    created_at?: string;
  };
  Update: Partial<Database["public"]["Tables"]["foods"]["Insert"]>;
  Relationships: [];
};
```

**food_equivalences**:
```typescript
food_equivalences: {
  Row: {
    id: string;
    food_id: string;
    equivalent_food_id: string;
    factor: number;
    created_at: string;
  };
  Insert: {
    id?: string;
    food_id: string;
    equivalent_food_id: string;
    factor?: number;
    created_at?: string;
  };
  Update: Partial<Database["public"]["Tables"]["food_equivalences"]["Insert"]>;
  Relationships: [
    {
      foreignKeyName: "food_equivalences_food_id_fkey";
      columns: ["food_id"];
      isOneToOne: false;
      referencedRelation: "foods";
      referencedColumns: ["id"];
    },
    {
      foreignKeyName: "food_equivalences_equivalent_food_id_fkey";
      columns: ["equivalent_food_id"];
      isOneToOne: false;
      referencedRelation: "foods";
      referencedColumns: ["id"];
    }
  ];
};
```

**saved_dishes**:
```typescript
saved_dishes: {
  Row: {
    id: string;
    trainer_id: string;
    name: string;
    kcal_per_100g: number;
    protein_per_100g: number;
    carbs_per_100g: number;
    fat_per_100g: number;
    created_at: string;
  };
  Insert: {
    id?: string;
    trainer_id: string;
    name: string;
    kcal_per_100g: number;
    protein_per_100g: number;
    carbs_per_100g: number;
    fat_per_100g: number;
    created_at?: string;
  };
  Update: Partial<Database["public"]["Tables"]["saved_dishes"]["Insert"]>;
  Relationships: [
    {
      foreignKeyName: "saved_dishes_trainer_id_fkey";
      columns: ["trainer_id"];
      isOneToOne: false;
      referencedRelation: "profiles";
      referencedColumns: ["id"];
    }
  ];
};
```

**meal_plan_items**:
```typescript
meal_plan_items: {
  Row: {
    id: string;
    plan_id: string;
    meal_number: number;
    food_id: string | null;
    dish_id: string | null;
    option_slot: string | null;
    grams: number;
    created_at: string;
  };
  Insert: {
    id?: string;
    plan_id: string;
    meal_number: number;
    food_id?: string | null;
    dish_id?: string | null;
    option_slot?: string | null;
    grams?: number;
    created_at?: string;
  };
  Update: Partial<Database["public"]["Tables"]["meal_plan_items"]["Insert"]>;
  Relationships: [
    {
      foreignKeyName: "meal_plan_items_plan_id_fkey";
      columns: ["plan_id"];
      isOneToOne: false;
      referencedRelation: "nutrition_plans";
      referencedColumns: ["id"];
    },
    {
      foreignKeyName: "meal_plan_items_food_id_fkey";
      columns: ["food_id"];
      isOneToOne: false;
      referencedRelation: "foods";
      referencedColumns: ["id"];
    },
    {
      foreignKeyName: "meal_plan_items_dish_id_fkey";
      columns: ["dish_id"];
      isOneToOne: false;
      referencedRelation: "saved_dishes";
      referencedColumns: ["id"];
    }
  ];
};
```

**food_log**:
```typescript
food_log: {
  Row: {
    id: string;
    client_id: string;
    food_id: string | null;
    dish_id: string | null;
    logged_date: string;
    grams: number;
    meal_number: number | null;
    created_at: string;
  };
  Insert: {
    id?: string;
    client_id: string;
    food_id?: string | null;
    dish_id?: string | null;
    logged_date?: string;
    grams?: number;
    meal_number?: number | null;
    created_at?: string;
  };
  Update: Partial<Database["public"]["Tables"]["food_log"]["Insert"]>;
  Relationships: [
    {
      foreignKeyName: "food_log_client_id_fkey";
      columns: ["client_id"];
      isOneToOne: false;
      referencedRelation: "clients";
      referencedColumns: ["id"];
    },
    {
      foreignKeyName: "food_log_food_id_fkey";
      columns: ["food_id"];
      isOneToOne: false;
      referencedRelation: "foods";
      referencedColumns: ["id"];
    },
    {
      foreignKeyName: "food_log_dish_id_fkey";
      columns: ["dish_id"];
      isOneToOne: false;
      referencedRelation: "saved_dishes";
      referencedColumns: ["id"];
    }
  ];
};
```

**client_measurements**:
```typescript
client_measurements: {
  Row: {
    id: string;
    client_id: string;
    measured_at: string;
    weight_kg: number | null;
    body_fat_pct: number | null;
    waist_cm: number | null;
    hip_cm: number | null;
    chest_cm: number | null;
    arm_cm: number | null;
    thigh_cm: number | null;
    notes: string | null;
    created_at: string;
  };
  Insert: {
    id?: string;
    client_id: string;
    measured_at?: string;
    weight_kg?: number | null;
    body_fat_pct?: number | null;
    waist_cm?: number | null;
    hip_cm?: number | null;
    chest_cm?: number | null;
    arm_cm?: number | null;
    thigh_cm?: number | null;
    notes?: string | null;
    created_at?: string;
  };
  Update: Partial<Database["public"]["Tables"]["client_measurements"]["Insert"]>;
  Relationships: [
    {
      foreignKeyName: "client_measurements_client_id_fkey";
      columns: ["client_id"];
      isOneToOne: false;
      referencedRelation: "clients";
      referencedColumns: ["id"];
    }
  ];
};
```

Insertar las 6 definiciones inmediatamente antes del cierre de `revision_photos` (antes de `};` que cierra el bloque `Tables`). No modificar ninguna tabla existente.

Verificar que `Views: { [_ in never]: never }` sigue intacto — NO cambiar a `Record<string, never>`.
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- `lib/supabase/types.ts` contiene las 6 nuevas tablas con Row + Insert + Update + Relationships
- `npx tsc --noEmit` no muestra nuevos errores relacionados con los 6 tipos añadidos
- Se puede hacer `type FoodRow = Database["public"]["Tables"]["foods"]["Row"]` sin error
  </done>
</task>

</tasks>

<verification>
- `.planning/migrations/08-nutrition-tables.sql` existe con 6 CREATE TABLE statements
- Las 6 tablas están creadas en Supabase (confirmado por el usuario)
- `lib/supabase/types.ts` contiene `foods`, `food_equivalences`, `saved_dishes`, `meal_plan_items`, `food_log`, `client_measurements`
- `npx tsc --noEmit` no reporta errores nuevos
</verification>

<success_criteria>
- Un desarrollador puede escribir `const { data } = await supabase.from('foods').select('id, name, kcal_per_100g')` y TypeScript infiere el tipo correcto sin `as any`
- Las 6 tablas existen en Supabase y son accesibles
- Tipos siguen el patrón Row/Insert/Update/Relationships de las tablas existentes
</success_criteria>

<output>
After completion, create `.planning/milestones/v4.0-phases/08-database-foundation-formulas/08-01-SUMMARY.md`
</output>
