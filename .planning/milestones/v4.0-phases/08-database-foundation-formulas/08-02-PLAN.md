---
phase: 08-database-foundation-formulas
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/calculations/nutrition.ts
autonomous: true
requirements:
  - CALC-01
  - CALC-02
  - CALC-03
  - CALC-04
  - CALC-05

must_haves:
  truths:
    - "Llamar `calculateTMB({ weightKg: 80, fatPercent: 15 })` retorna el resultado Katch-McArdle: 370 + 21.6 × (80×0.85) = ~1840 kcal"
    - "Llamar `calculateTMB({ weightKg: 80, heightCm: 175, age: 30, sex: 'male' })` (sin fatPercent) retorna el resultado Mifflin-St Jeor correcto"
    - "Llamar `calculateTDEE(1800, 'moderate')` retorna 1800 × 1.55 = 2790"
    - "Llamar `calculateTargetCalories(2790, 'deficit')` retorna Math.round(2790 × 0.85) = 2372"
    - "Llamar `calculateMacros({ weightKg: 80, phase: 'deficit', targetCalories: 2372 })` retorna protein 2.2×80=176g, fat 0.8×80=64g, carbs del resto"
    - "Las funciones anteriores son exportadas correctamente y las funciones existentes de calculateNutrition() siguen funcionando"
  artifacts:
    - path: "lib/calculations/nutrition.ts"
      provides: "Todas las funciones de cálculo nutricional para el módulo v4.0"
      exports:
        - calculateTMB
        - calculateTDEE
        - calculateTargetCalories
        - calculateMacros
        - calculateNutrition
        - ACTIVITY_FACTORS
        - ACTIVITY_LABELS
  key_links:
    - from: "lib/calculations/nutrition.ts"
      to: "app/(trainer)/nutrition/plan/page.tsx (Phase 9)"
      via: "import { calculateTMB, calculateTDEE, calculateTargetCalories, calculateMacros }"
      pattern: "export function calculateTMB"
---

<objective>
Extender `lib/calculations/nutrition.ts` con las 4 nuevas funciones de cálculo: `calculateTMB` (Katch-McArdle / Mifflin-St Jeor según disponibilidad de %grasa), `calculateTDEE`, `calculateTargetCalories` y `calculateMacros`. Las funciones existentes (`calculateNutrition`, `ACTIVITY_FACTORS`, `ACTIVITY_LABELS`) deben mantenerse intactas.

Purpose: Todas las pantallas del módulo nutrición (Phase 9 trainer plan form, Phase 11 client view) consumirán estas funciones. Deben estar completamente especificadas antes de que empiecen.
Output: `lib/calculations/nutrition.ts` con 4 nuevas funciones exportadas + tipos asociados
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/nutrition-specs.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Estado actual de lib/calculations/nutrition.ts — leer el archivo antes de modificar -->

Exports actuales que DEBEN mantenerse:
```typescript
export type ActivityLevel = "sedentary" | "light" | "moderate" | "active" | "very_active";
export interface NutritionInput { weightKg, bodyFatPct, activityLevel, dailySteps?, goal }
export interface NutritionResult { ffm, tmb_cunningham, tmb_tinsley, tmb_recommended, get, stepsBonus, targetCalories, macros }
export function calculateNutrition(input: NutritionInput): NutritionResult
export { ACTIVITY_LABELS, ACTIVITY_FACTORS }
```

Fórmulas exactas de nutrition-specs.md:
- Katch-McArdle: TMB = 370 + (21.6 × FFM)  donde FFM = peso × (1 - %grasa/100)
- Mifflin-St Jeor Hombre: TMB = (10×W) + (6.25×H) - (5×A) + 5
- Mifflin-St Jeor Mujer: TMB = (10×W) + (6.25×H) - (5×A) - 161
- TDEE = TMB × FA (1.2 / 1.375 / 1.55 / 1.725 / 1.9)
- Calorías objetivo: déficit ×0.85, mantenimiento ×1.0, volumen ×1.075
- Macros: proteínas déficit 2.2g/kg | recomposición 2.0g/kg | volumen 1.8g/kg
           grasas déficit 0.8g/kg | recomposición 0.9g/kg | volumen 1.0g/kg
           carbs = (targetCalories - proteínas×4 - grasas×9) / 4

ACTIVITY_FACTORS existentes (NO redefinir, ya existe en el archivo):
sedentary: 1.2, light: 1.375, moderate: 1.55, active: 1.725, very_active: 1.9
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Añadir calculateTMB, calculateTDEE, calculateTargetCalories y calculateMacros</name>
  <files>lib/calculations/nutrition.ts</files>
  <behavior>
    - calculateTMB con fatPercent=15 y weight=80: retorna ~1840 (Katch-McArdle: 370 + 21.6×68)
    - calculateTMB sin fatPercent, male, weight=80 height=175 age=30: retorna (10×80)+(6.25×175)-(5×30)+5 = 800+1093.75-150+5 = 1748.75 → redondeado a 1749
    - calculateTMB sin fatPercent, female, weight=60 height=165 age=25: retorna (10×60)+(6.25×165)-(5×25)-161 = 600+1031.25-125-161 = 1345.25 → 1345
    - calculateTDEE(1800, 'moderate'): retorna Math.round(1800 × 1.55) = 2790
    - calculateTDEE(2000, 'sedentary'): retorna Math.round(2000 × 1.2) = 2400
    - calculateTargetCalories(2790, 'deficit'): retorna Math.round(2790 × 0.85) = 2372
    - calculateTargetCalories(2790, 'maintenance'): retorna 2790
    - calculateTargetCalories(2790, 'surplus'): retorna Math.round(2790 × 1.075) = 2999
    - calculateMacros(80, 'deficit', 2372): protein=176g (2.2×80), fat=64g (0.8×80), carbs=(2372-176×4-64×9)/4 = (2372-704-576)/4 = 1092/4 = 273g
    - calculateMacros(80, 'volume', 2999): protein=144g (1.8×80), fat=80g (1.0×80), carbs=(2999-144×4-80×9)/4 = (2999-576-720)/4 = 1703/4 = 425g
    - calculateMacros(80, 'recomposition', 2500): protein=160g (2.0×80), fat=72g (0.9×80), carbs=(2500-160×4-72×9)/4 = (2500-640-648)/4 = 1212/4 = 303g
  </behavior>
  <action>
Leer el archivo completo `lib/calculations/nutrition.ts` antes de modificarlo. Añadir al final del archivo (antes o después del export al final, preservando los exports existentes) los nuevos tipos e interfaces y las 4 funciones.

**Nuevos tipos a añadir:**

```typescript
export type NutritionPhase = "deficit" | "recomposition" | "volume" | "maintenance";
export type Sex = "male" | "female";

export interface TMBInput {
  weightKg: number;
  fatPercent?: number;     // Si se pasa → Katch-McArdle. Si no → Mifflin-St Jeor
  heightCm?: number;       // Requerido si no hay fatPercent
  age?: number;            // Requerido si no hay fatPercent
  sex?: Sex;               // Requerido si no hay fatPercent, default 'male'
}

export interface MacrosResult {
  protein: { g: number; kcal: number };
  fat: { g: number; kcal: number };
  carbs: { g: number; kcal: number };
}
```

**calculateTMB** — Katch-McArdle si hay fatPercent, Mifflin-St Jeor si no:
```typescript
export function calculateTMB(input: TMBInput): number {
  if (input.fatPercent !== undefined) {
    // Katch-McArdle: 370 + 21.6 × FFM
    const ffm = input.weightKg * (1 - input.fatPercent / 100);
    return Math.round(370 + 21.6 * ffm);
  } else {
    // Mifflin-St Jeor
    const { weightKg, heightCm = 170, age = 30, sex = 'male' } = input;
    const base = 10 * weightKg + 6.25 * heightCm - 5 * age;
    return Math.round(sex === 'male' ? base + 5 : base - 161);
  }
}
```

**calculateTDEE** — TMB × factor actividad:
```typescript
export function calculateTDEE(tmb: number, activityLevel: ActivityLevel): number {
  return Math.round(tmb * ACTIVITY_FACTORS[activityLevel]);
}
```

**calculateTargetCalories** — TDEE × factor objetivo:
```typescript
const GOAL_FACTORS: Record<NutritionPhase, number> = {
  deficit: 0.85,
  maintenance: 1.0,
  recomposition: 1.0,  // mismas kcal que mantenimiento, distinto reparto de macros
  volume: 1.075,
};

export function calculateTargetCalories(tdee: number, phase: NutritionPhase): number {
  return Math.round(tdee * GOAL_FACTORS[phase]);
}
```

**calculateMacros** — Distribución por fase:
```typescript
const PROTEIN_FACTORS: Record<NutritionPhase, number> = {
  deficit: 2.2,
  recomposition: 2.0,
  volume: 1.8,
  maintenance: 2.0,
};

const FAT_FACTORS: Record<NutritionPhase, number> = {
  deficit: 0.8,
  recomposition: 0.9,
  volume: 1.0,
  maintenance: 0.9,
};

export function calculateMacros(
  weightKg: number,
  phase: NutritionPhase,
  targetCalories: number
): MacrosResult {
  const proteinG = Math.round(PROTEIN_FACTORS[phase] * weightKg);
  const fatG = Math.round(FAT_FACTORS[phase] * weightKg);
  const proteinKcal = proteinG * 4;
  const fatKcal = fatG * 9;
  const carbsKcal = Math.max(0, targetCalories - proteinKcal - fatKcal);
  const carbsG = Math.round(carbsKcal / 4);

  return {
    protein: { g: proteinG, kcal: proteinKcal },
    fat: { g: fatG, kcal: fatKcal },
    carbs: { g: carbsG, kcal: carbsKcal },
  };
}
```

Notas de implementación:
- `GOAL_FACTORS` y `PROTEIN_FACTORS` y `FAT_FACTORS` son constantes internas, NO exportarlas (no son necesarias externamente).
- `NutritionPhase`, `Sex`, `TMBInput`, `MacrosResult` SÍ exportarlos.
- Los `export { ACTIVITY_LABELS, ACTIVITY_FACTORS }` al final del archivo deben mantenerse.
- `recomposition` en `NutritionPhase` coincide con el tipo `Phase` de types.ts ("recomposition") — no crear confusión con "maintenance" vs "surplus". Nota: `Goal` en types.ts tiene "deficit" | "maintenance" | "surplus" mientras que `NutritionPhase` aquí usa "recomposition" en lugar de "surplus". Ambos coexisten.

Escribir primero los casos de test como comentarios inline o como funciones de prueba simple al final del archivo dentro de un bloque `if (process.env.NODE_ENV === 'test')` — NO: en realidad dado que este proyecto no tiene jest configurado para este archivo, los tests son verificados con los valores esperados en la sección `<behavior>`. Implementar directamente las funciones sin archivo de test separado.
  </action>
  <verify>
    <automated>cd "/Users/jofreatanet/Desktop/Fitness bassi/app-fitness-bassi" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- `lib/calculations/nutrition.ts` exporta `calculateTMB`, `calculateTDEE`, `calculateTargetCalories`, `calculateMacros`, `NutritionPhase`, `Sex`, `TMBInput`, `MacrosResult`
- `calculateNutrition`, `ACTIVITY_FACTORS`, `ACTIVITY_LABELS` siguen exportados sin cambios
- `npx tsc --noEmit` no reporta errores nuevos en este archivo
- Los valores de los casos en `<behavior>` son matemáticamente correctos según las fórmulas
  </done>
</task>

</tasks>

<verification>
Verificación manual de fórmulas antes de marcar como completo:

1. Katch-McArdle: peso=80kg, grasa=15% → FFM=68kg → TMB=370+(21.6×68)=370+1468.8=1838.8 → 1839
2. Mifflin male: peso=80, altura=175, edad=30 → (800)+(1093.75)-(150)+5=1748.75 → 1749
3. TDEE: 1839 × 1.55 = 2850.45 → 2850
4. Target deficit: 2850 × 0.85 = 2422.5 → 2423
5. Macros déficit peso=80: protein=176g(704kcal), fat=64g(576kcal), carbs=(2423-704-576)/4=1143/4=285g
</verification>

<success_criteria>
- Las 4 nuevas funciones están exportadas desde `lib/calculations/nutrition.ts`
- Las fórmulas siguen exactamente la especificación de `.planning/nutrition-specs.md`
- `npx tsc --noEmit` pasa sin errores nuevos
- La función `calculateNutrition()` existente sigue funcionando (no se ha roto)
</success_criteria>

<output>
After completion, create `.planning/milestones/v4.0-phases/08-database-foundation-formulas/08-02-SUMMARY.md`
</output>
